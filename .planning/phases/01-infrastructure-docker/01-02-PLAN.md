---
phase: 01-infrastructure-docker
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/raw/.gitkeep
  - data/raw/README.md
  - data/processed/.gitkeep
  - data/external/.gitkeep
  - notebooks/.gitkeep
  - reports/.gitkeep
  - models/.gitkeep
autonomous: true
requirements: [INFR-04, INFR-05]
must_haves:
  truths:
    - "All six DoML directories exist: data/raw, data/processed, data/external, notebooks, reports, models"
    - "Each directory contains a .gitkeep file so git tracks the empty dir"
    - "data/raw/README.md documents the immutability convention"
    - "Directory layout matches the volume mounts declared in docker-compose.yml"
  artifacts:
    - path: "data/raw/.gitkeep"
      provides: "Git-tracked raw data directory"
    - path: "data/raw/README.md"
      provides: "Immutability convention documentation"
      contains: "immutable"
    - path: "data/processed/.gitkeep"
      provides: "Git-tracked processed data directory"
    - path: "data/external/.gitkeep"
      provides: "Git-tracked external reference data directory"
    - path: "notebooks/.gitkeep"
      provides: "Git-tracked notebooks directory"
    - path: "reports/.gitkeep"
      provides: "Git-tracked HTML reports directory"
    - path: "models/.gitkeep"
      provides: "Git-tracked model artifacts directory"
  key_links:
    - from: "data/raw/"
      to: "docker-compose.yml volume mount"
      via: "bind mount with :ro flag"
      pattern: "data/raw.*:ro"
---

# Plan 01-02: Project Directory Scaffold

**Phase:** 1 — Infrastructure & Docker
**Goal:** Create the six canonical DoML project directories (data/raw, data/processed, data/external, notebooks, reports, models) with .gitkeep markers, plus a README in data/raw documenting the immutability convention that INFR-05 enforces.
**Requirements:** INFR-04, INFR-05

## Context

DoML projects rely on a fixed directory layout. Every analysis notebook, every DuckDB query, every HTML report template resolves paths relative to this structure. Without it, `docker compose up` mounts from nonexistent host paths (Docker creates them as root-owned empty dirs, breaking jovyan writes).

This plan creates the directories with `.gitkeep` files so git tracks them while the contents (raw data, generated artifacts) stay out of version control per the .gitignore rules defined in Plan 01-03.

Immutability (INFR-05) is enforced at three layers: (1) Docker `:ro` mount in 01-01, (2) .gitignore rules in 01-03, (3) convention documented in `data/raw/README.md` by this plan.

<objective>
Scaffold the DoML project directory tree with git-trackable placeholders and immutability documentation.

Purpose: Provide the on-disk scaffolding that the Docker volume mounts require and that every downstream DoML phase reads from/writes to.
Output: Six directories, seven files (six .gitkeep + one README).
</objective>

<execution_context>
@/home/bill/source/DoML/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-infrastructure-docker/01-RESEARCH.md
@.planning/phases/01-infrastructure-docker/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create six DoML directories with .gitkeep markers</name>
  <files>data/raw/.gitkeep, data/processed/.gitkeep, data/external/.gitkeep, notebooks/.gitkeep, reports/.gitkeep, models/.gitkeep</files>
  <action>
Create the canonical DoML directory tree at the project root. Each directory MUST contain a `.gitkeep` file (empty file) so git tracks the directory itself.

Directories to create (satisfies INFR-04):

```
data/raw/          # immutable source data — read-only mount (INFR-05)
data/processed/    # cleaned/transformed datasets — written by EDA phase
data/external/     # reference data (lookups, dictionaries) — read-only mount
notebooks/         # Jupyter notebooks by phase (01_business_understanding.ipynb, etc.)
reports/           # stakeholder HTML reports (nbconvert output)
models/            # serialized model artifacts (.pkl) + metadata JSON
```

Implementation (use shell / mkdir via Bash tool):

```bash
mkdir -p data/raw data/processed data/external notebooks reports models
touch data/raw/.gitkeep data/processed/.gitkeep data/external/.gitkeep
touch notebooks/.gitkeep reports/.gitkeep models/.gitkeep
```

All six `.gitkeep` files MUST be empty (zero bytes). This matches the host-side paths that `docker-compose.yml` bind-mounts — if these directories don't exist before `docker compose up`, Docker creates them as root-owned and jovyan cannot write.
  </action>
  <verify>
    <automated>test -d data/raw && test -d data/processed && test -d data/external && test -d notebooks && test -d reports && test -d models && test -f data/raw/.gitkeep && test -f data/processed/.gitkeep && test -f data/external/.gitkeep && test -f notebooks/.gitkeep && test -f reports/.gitkeep && test -f models/.gitkeep && echo OK</automated>
  </verify>
  <done>All six directories exist; each contains a .gitkeep file; validation command returns OK.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write data/raw/README.md documenting immutability convention</name>
  <files>data/raw/README.md</files>
  <action>
Create `data/raw/README.md` to document the immutability convention at the convention layer (INFR-05 defense #3, complementing the `:ro` Docker mount and .gitignore rules).

Content:

```markdown
# data/raw/ — Immutable Source Data

**Convention: Raw data in this directory is IMMUTABLE.**

## Rules

1. **Never modify files in this directory in place.** Raw data is the single source of truth for reproducibility.
2. **Never overwrite a raw file.** If source data is updated, add a new dated file (e.g., `sales_2026-03-01.csv` → `sales_2026-04-01.csv`) rather than replacing.
3. **Never commit raw data to git.** Raw data files are excluded via `.gitignore` (only `.gitkeep` is tracked). Use external storage (S3, shared drive, DVC) for distribution.

## Enforcement Layers

DoML enforces immutability at three layers:

| Layer | Mechanism |
|-------|-----------|
| OS / Docker | `data/raw` is mounted read-only (`:ro`) inside the container — writes from notebooks will fail with "Read-only file system" |
| Git | `.gitignore` excludes `data/raw/*` (except `.gitkeep`) so accidental commits are prevented |
| Convention | This README + DoML's CLAUDE.md instructions remind users not to mutate raw data |

## Workflow

1. Drop source files into `data/raw/` on the **host machine** (Docker bind mount reflects them read-only inside container).
2. Analysis notebooks read from `data/raw/` via DuckDB or pandas.
3. Cleaned/transformed datasets are written to `data/processed/` — that directory is mutable.
4. Never edit files here. Ever.

## Why This Matters

Reproducibility. A DoML analysis must be re-runnable from the same raw inputs months later. If raw data is mutated in place, prior analysis results become unverifiable and the audit trail breaks.
```

This README is the documentation arm of INFR-05. The Docker `:ro` mount provides the technical guarantee; this file provides the human-readable explanation.
  </action>
  <verify>
    <automated>test -f data/raw/README.md && grep -q 'immutable' data/raw/README.md && grep -qi 'read-only' data/raw/README.md && echo OK</automated>
  </verify>
  <done>data/raw/README.md exists; documents the immutability convention; references read-only enforcement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → raw data | User may accidentally overwrite source data |
| container → raw data | Container processes attempt writes to read-only mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | data/raw/ contents | mitigate | Triple defense: Docker `:ro` mount (Plan 01-01) + `.gitignore` (Plan 01-03) + README convention (this plan) |
| T-01-07 | Repudiation | Raw data provenance | mitigate | Immutability convention ensures raw inputs are traceable for audit / reproducibility |
</threat_model>

## Verification

```bash
# All six directories exist (INFR-04)
test -d data/raw && test -d data/processed && test -d data/external && \
  test -d notebooks && test -d reports && test -d models && echo "dirs OK"

# All six .gitkeep files exist
for d in data/raw data/processed data/external notebooks reports models; do
  test -f "$d/.gitkeep" || echo "MISSING: $d/.gitkeep"
done

# README documents immutability (INFR-05)
test -f data/raw/README.md && grep -q 'immutable' data/raw/README.md && echo "README OK"

# Read-only mount integration (cross-plan check with 01-01)
docker compose config | grep -A2 'data/raw' | grep -q 'ro' && echo "RO mount OK"
```

## Success Criteria

- [ ] All six directories exist: `data/raw`, `data/processed`, `data/external`, `notebooks`, `reports`, `models` (INFR-04)
- [ ] Each directory contains an empty `.gitkeep` file
- [ ] `data/raw/README.md` exists and documents the immutability convention (INFR-05)
- [ ] Layout matches the volume mounts declared in Plan 01-01's docker-compose.yml

## Files Modified

- data/raw/.gitkeep
- data/raw/README.md
- data/processed/.gitkeep
- data/external/.gitkeep
- notebooks/.gitkeep
- reports/.gitkeep
- models/.gitkeep

<output>
After completion, create `.planning/phases/01-infrastructure-docker/01-02-SUMMARY.md` noting the directory tree created and confirming all .gitkeep files are empty.
</output>
