---
phase: 02-framework-skeleton
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - .claude/doml/templates/PROJECT.md
  - .claude/doml/templates/ROADMAP.md
  - .claude/doml/templates/STATE.md
  - .claude/doml/templates/config.json
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04]
must_haves:
  truths:
    - ".claude/doml/templates/PROJECT.md exists with placeholder sections for business question, problem type, and dataset context"
    - ".claude/doml/templates/ROADMAP.md exists with the standard DoML 5-phase analysis pipeline"
    - ".claude/doml/templates/STATE.md exists with YAML frontmatter and session continuity structure"
    - ".claude/doml/templates/config.json exists with language, problem_type, time_factor, and workflow toggles"
    - "STATE.md template has a Decisions section for recording cross-session context"
  artifacts:
    - path: ".claude/doml/templates/PROJECT.md"
      provides: "Template for capturing business question, dataset, stakeholder, and ML problem type"
      contains: "Business Question"
    - path: ".claude/doml/templates/ROADMAP.md"
      provides: "Template roadmap with 5 standard DoML analysis phases"
      contains: "Business Understanding"
    - path: ".claude/doml/templates/STATE.md"
      provides: "Template for session continuity state with YAML frontmatter"
      contains: "current_focus"
    - path: ".claude/doml/templates/config.json"
      provides: "Template for project preferences: language, problem_type, time_factor"
      contains: "language"
---

# Plan 02-03: Planning Artifact Templates

**Phase:** 2 — Framework Skeleton
**Goal:** Create the four planning artifact templates that `/doml-new-project` stamps into a fresh analysis project — `PROJECT.md`, `ROADMAP.md`, `STATE.md`, and `config.json` — providing DoML analysis projects the same artifact structure the framework itself uses.
**Requirements:** PLAN-01, PLAN-02, PLAN-03, PLAN-04

## Context

When a data scientist runs `/doml-new-project` on a new dataset, the workflow copies these templates into `.planning/` and populates them with interview answers. The templates therefore define what a DoML analysis project looks like structurally.

These are **analysis project templates** — not the planning artifacts for the DoML framework project itself (those already exist in `.planning/` at the project root). They describe what ends up in a new user project's `.planning/` folder.

Key design decisions from STATE.md:
- `config.json` must store `language` (Python default, R opt-in), `problem_type`, and `time_factor`
- `STATE.md` must persist current phase and key decisions across Claude Code sessions (PLAN-03)
- `ROADMAP.md` maps the 5 standard DoML analysis phases (not the framework's own phases)

The 5 standard **analysis** phases (what gets templated into user projects):
1. Business Understanding — context, inventory, problem framing
2. Data Understanding — EDA, statistical analysis, tidy validation
3. Data Preparation — feature engineering, transformations (Milestone 2)
4. Modelling — algorithm selection, training, evaluation (Milestone 2)
5. Evaluation & Deployment — results, reports, handoff (Milestone 2)

For Milestone 1, the template roadmap includes all 5 phases but marks 3–5 as "Milestone 2".

<objective>
Create four planning artifact templates for DoML analysis projects.

Purpose: `/doml-new-project` has templates to stamp into new projects; data scientists have a consistent state-tracking structure.
Output: `.claude/doml/templates/PROJECT.md`, `ROADMAP.md`, `STATE.md`, `config.json`
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create config.json template</name>
  <files>.claude/doml/templates/config.json</files>
  <action>
Create `.claude/doml/templates/config.json`. This file is read by every DoML workflow to determine language preference, problem type, and feature flags.

```json
{
  "language": "python",
  "problem_type": "unknown",
  "time_factor": false,
  "dataset": {
    "path": "data/raw/",
    "format": "unknown",
    "target_column": null
  },
  "workflow": {
    "research_enabled": true,
    "plan_check_enabled": true,
    "auto_advance": false,
    "html_reports": true
  },
  "reproducibility": {
    "seed": 42,
    "strip_outputs_on_commit": true
  },
  "milestone": "v1.0",
  "created": "YYYY-MM-DD",
  "last_updated": "YYYY-MM-DD"
}
```

Field definitions (for the workflow that populates this at kickoff):
- `language`: `"python"` (default) or `"r"` — set during interview (INTV-05)
- `problem_type`: `"regression"` | `"classification"` | `"clustering"` | `"time_series"` | `"dimensionality_reduction"` | `"unknown"` — set during interview
- `time_factor`: `true` | `false` — set during interview (INTV-02); triggers time series EDA and forecasting phase
- `dataset.path`: relative path to raw data directory
- `dataset.format`: `"csv"` | `"parquet"` | `"excel"` | `"mixed"` | `"unknown"` — detected during data validation (INTV-03)
- `dataset.target_column`: column name for supervised problems; `null` for unsupervised
- `workflow.html_reports`: whether to generate nbconvert HTML after each phase (always true for Milestone 1)
- `reproducibility.seed`: random seed for all notebooks (always 42 per REPR-01)
  </action>
  <verify>
    <automated>python3 -c "import json; cfg=json.load(open('.claude/doml/templates/config.json')); assert 'language' in cfg; assert 'problem_type' in cfg; assert 'time_factor' in cfg; print('OK')"</automated>
  </verify>
  <done>config.json template is valid JSON with language, problem_type, time_factor, dataset, workflow, and reproducibility sections.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create STATE.md template</name>
  <files>.claude/doml/templates/STATE.md</files>
  <action>
Create `.claude/doml/templates/STATE.md`. This template is the session continuity artifact — it persists current phase, decisions, and session context across Claude Code sessions (PLAN-03).

```markdown
---
doml_state_version: 1.0
project: PROJECT_NAME
milestone: v1.0
status: initialized
current_phase: 1
current_focus: "Phase 1 — Business Understanding"
last_updated: "YYYY-MM-DDThh:mm:ssZ"
last_activity: YYYY-MM-DD
progress:
  total_phases: 5
  completed_phases: 0
  percent: 0
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated YYYY-MM-DD)

**Business question:** [to be filled during kickoff interview]
**Current focus:** Phase 1 — Business Understanding

## Current Position

Phase: 1 (business-understanding) — NOT STARTED
Status: Ready to begin

Progress: [░░░░░░░░░░] 0%

## Phases

| Phase | Name | Status | Completed |
|-------|------|--------|-----------|
| 1 | Business Understanding | Not started | — |
| 2 | Data Understanding | Not started | — |
| 3 | Data Preparation | Not started (M2) | — |
| 4 | Modelling | Not started (M2) | — |
| 5 | Evaluation | Not started (M2) | — |

## Accumulated Context

### Decisions

- [Decisions recorded here during analysis phases]

### Pending Todos

None yet.

### Blockers/Concerns

None yet.

## Session Continuity

Last session: YYYY-MM-DDThh:mm:ssZ
Stopped at: Project initialized
Resume file: None
```
  </action>
  <verify>
    <automated>test -f .claude/doml/templates/STATE.md && grep -q "current_focus" .claude/doml/templates/STATE.md && grep -q "doml_state_version" .claude/doml/templates/STATE.md && echo OK</automated>
  </verify>
  <done>STATE.md template exists with YAML frontmatter (doml_state_version, current_focus, progress), phase table, Decisions section, and Session Continuity section.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create PROJECT.md template</name>
  <files>.claude/doml/templates/PROJECT.md</files>
  <action>
Create `.claude/doml/templates/PROJECT.md`. This template captures the business context established during the kickoff interview (PLAN-01, INTV-01–05). It is the human-readable record of what the analysis is trying to achieve.

```markdown
---
project: PROJECT_NAME
created: YYYY-MM-DD
last_updated: YYYY-MM-DD
dataset: DATASET_FILE_OR_FOLDER
problem_type: unknown
language: python
---

# PROJECT_NAME

## Business Context

**Business question:**
[What decision or action will this analysis inform?]

**Stakeholder:**
[Who will act on the findings from this analysis?]

**Expected outcome:**
[What does success look like? What will the stakeholder do differently after seeing results?]

## Dataset

**Source:** data/raw/DATASET_FILE
**Format:** CSV / Parquet / Excel
**Description:** [Brief description of what the data represents]
**Collection period:** [Date range or recency of data]
**Known biases or limitations:** [Any known issues with data quality, coverage, or representativeness]

## Problem Framing

**ML problem type:** [regression / classification / clustering / time_series / dimensionality_reduction]
**Target variable:** [Column name, or "none" for unsupervised]
**Key features:** [List of columns expected to be predictive, if known]
**Time factor:** [Yes / No — Is time a relevant dimension in this dataset?]

**Decision framing:**
> We are trying to [decision] using [metric/outcome column] as a proxy.

## Language Preference

**Analysis language:** Python (default) / R (opt-in)
**Reason:** [Why this preference was selected, if non-default]

## Analysis Scope

**In scope:**
- [ ] Business Understanding notebook
- [ ] Data Understanding EDA notebook
- [ ] Stakeholder HTML reports

**Out of scope for this analysis:**
- [List any exclusions agreed with stakeholder]

## Assumptions and Constraints

- [List assumptions made about the data, problem, or domain]
- [List constraints: time, resources, stakeholder requirements]

## Caveats

- Correlation is not causation — all findings are observational unless a controlled experiment was run
- [Add domain-specific caveats here]
```
  </action>
  <verify>
    <automated>test -f .claude/doml/templates/PROJECT.md && grep -q "Business question" .claude/doml/templates/PROJECT.md && grep -q "problem_type" .claude/doml/templates/PROJECT.md && echo OK</automated>
  </verify>
  <done>PROJECT.md template exists with frontmatter (problem_type, language, dataset), Business Context section, Problem Framing section, and Caveats section.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create ROADMAP.md template</name>
  <files>.claude/doml/templates/ROADMAP.md</files>
  <action>
Create `.claude/doml/templates/ROADMAP.md`. This template maps the standard 5-phase CRISP-DM–inspired DoML analysis pipeline. Phases 3–5 are marked as Milestone 2 (not yet in scope for the framework), but the full pipeline is visible so data scientists understand where the analysis is going.

```markdown
# Analysis Roadmap: PROJECT_NAME

**Dataset:** DATASET_FILE
**Problem type:** unknown (set during kickoff interview)
**Created:** YYYY-MM-DD

---

## Milestone 1 — Foundation + EDA

**Goal:** Establish business context, inventory the dataset, and produce a full exploratory data analysis with stakeholder reports.

### Phase 1: Business Understanding

**Goal:** Document business question, data inventory, ML problem type, and dataset provenance. Produce a stakeholder HTML report framing the problem.

**Requirements:** BU-01, BU-02, BU-03, BU-04, BU-05, OUT-01, OUT-02, OUT-03

**Success Criteria:**
1. `notebooks/01_business_understanding.ipynb` generated with narrative, data inventory, problem type, provenance
2. `reports/business_summary.html` generated with code hidden and executive narrative
3. HTML report includes caveats section (correlation ≠ causation)

**Output:**
- `notebooks/01_business_understanding.ipynb`
- `reports/business_summary.html`

---

### Phase 2: Data Understanding

**Goal:** Full EDA using DuckDB for large-file profiling, statistical tests, missing value analysis, tidy validation, and (if time_factor=true) stationarity tests. Produce stakeholder HTML report.

**Requirements:** EDA-01 through EDA-10, OUT-01, OUT-02, OUT-03

**Success Criteria:**
1. `notebooks/02_data_understanding.ipynb` generated with all EDA sections
2. DuckDB used for initial profiling — no full pandas load for large files
3. Distribution, correlation, and missing value analyses included
4. Time series stationarity tests included if time_factor=true
5. Tidy data violations flagged before feature engineering
6. Cleaned dataset written to `data/processed/`
7. `reports/eda_report.html` generated with code hidden and Claude narrative

**Output:**
- `notebooks/02_data_understanding.ipynb`
- `data/processed/[dataset]_cleaned.[ext]`
- `reports/eda_report.html`

---

## Milestone 2 — Modelling + Forecasting *(planned)*

### Phase 3: Data Preparation *(Milestone 2)*

**Goal:** Feature engineering, encoding, scaling, and train/test split — producing a modelling-ready dataset.

**Output:**
- `notebooks/03_data_preparation.ipynb`
- `data/processed/[dataset]_features.[ext]`

---

### Phase 4: Modelling *(Milestone 2)*

**Goal:** Algorithm selection, training, leaderboard, SHAP explainability, and Optuna tuning.

**Output:**
- `notebooks/04_modelling.ipynb`
- `models/best_model.pkl`
- `models/leaderboard.csv`
- `reports/model_report.html`

---

### Phase 5: Evaluation *(Milestone 2)*

**Goal:** Final model evaluation, forecast generation (if time_factor=true), stakeholder-facing report with business interpretation.

**Output:**
- `notebooks/05_evaluation.ipynb`
- `reports/final_report.html`

---

## Progress

| Phase | Milestone | Status | Completed |
|-------|-----------|--------|-----------|
| 1. Business Understanding | M1 | Not started | — |
| 2. Data Understanding | M1 | Not started | — |
| 3. Data Preparation | M2 | Not started | — |
| 4. Modelling | M2 | Not started | — |
| 5. Evaluation | M2 | Not started | — |
```
  </action>
  <verify>
    <automated>test -f .claude/doml/templates/ROADMAP.md && grep -q "Business Understanding" .claude/doml/templates/ROADMAP.md && grep -q "Data Understanding" .claude/doml/templates/ROADMAP.md && grep -q "Milestone 2" .claude/doml/templates/ROADMAP.md && echo OK</automated>
  </verify>
  <done>ROADMAP.md template exists with all 5 phases, Milestone 1/2 split, success criteria per phase, and progress table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template → new project | Templates are copied into user analysis projects and may contain sensitive framing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | PROJECT.md template | accept | Template contains only placeholder text; no real data or credentials; user fills it in |
| T-02-07 | Tampering | config.json seed value | mitigate | Template hardcodes seed=42 per REPR-01; workflows must not override this value |
| T-02-08 | Spoofing | Caveats section | mitigate | ROADMAP.md and PROJECT.md both include caveats template text per OUT-03 requirement |
</threat_model>

## Verification

```bash
# All four templates exist
for tmpl in PROJECT.md ROADMAP.md STATE.md config.json; do
  test -f .claude/doml/templates/$tmpl && echo "$tmpl OK" || echo "$tmpl MISSING"
done

# config.json is valid JSON with required keys
python3 -c "
import json
cfg = json.load(open('.claude/doml/templates/config.json'))
assert 'language' in cfg, 'missing language'
assert 'problem_type' in cfg, 'missing problem_type'
assert 'time_factor' in cfg, 'missing time_factor'
assert cfg['reproducibility']['seed'] == 42, 'wrong seed'
print('config.json OK')
"

# STATE.md has YAML frontmatter with required keys
grep -q "doml_state_version" .claude/doml/templates/STATE.md && echo "STATE.md frontmatter OK"
grep -q "current_focus" .claude/doml/templates/STATE.md && echo "STATE.md current_focus OK"

# PROJECT.md has required sections
grep -q "Business question" .claude/doml/templates/PROJECT.md && echo "PROJECT.md business question OK"
grep -q "Caveats" .claude/doml/templates/PROJECT.md && echo "PROJECT.md caveats OK"

# ROADMAP.md covers all 5 phases
grep -c "Phase [1-5]:" .claude/doml/templates/ROADMAP.md | grep -q "^5$" && echo "ROADMAP.md 5 phases OK"
```

## Success Criteria

- [ ] `.claude/doml/templates/config.json` is valid JSON with `language`, `problem_type`, `time_factor`, `reproducibility.seed=42`
- [ ] `.claude/doml/templates/STATE.md` has YAML frontmatter with `doml_state_version`, `current_focus`, `progress`
- [ ] `.claude/doml/templates/PROJECT.md` has Business Context, Problem Framing, and Caveats sections
- [ ] `.claude/doml/templates/ROADMAP.md` has all 5 analysis phases with M1/M2 split and success criteria
- [ ] All templates use placeholder text only — no hardcoded project-specific values

## Files Modified

- .claude/doml/templates/config.json (new)
- .claude/doml/templates/STATE.md (new)
- .claude/doml/templates/PROJECT.md (new)
- .claude/doml/templates/ROADMAP.md (new)

<output>
After completion, create `.planning/phases/02-framework-skeleton/02-03-SUMMARY.md` documenting any deviations from this plan and confirming the templates are suitable for use by /doml-new-project.
</output>
