---
phase: 03-kickoff-interview
plan: 01
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - .claude/doml/workflows/new-project.md
autonomous: false
requirements:
  - INTV-01
  - INTV-02
  - INTV-04
  - INTV-05

must_haves:
  truths:
    - "Running /doml-new-project shows DuckDB scan output (file list + row x col + column names/dtypes) before any interview question is asked"
    - "Interview asks all five required questions: business decision, stakeholder, expected outcome, dataset description, time factor"
    - "Claude infers ML problem type from free text, explains reasoning, and asks user to confirm or correct"
    - "If time_factor=true and problem_type=regression, workflow offers upgrade to time_series"
    - "Claude offers optional domain research step; user can accept or skip; domain summary appears in PROJECT.md if accepted"
    - "After full interview, PROJECT.md is written atomically - all placeholders filled, Problem Framing section complete, framing sentence present"
    - "After full interview, config.json is updated with analysis_language, problem_type, and time_factor"
    - "Empty /data/raw/ produces the hard-stop error from scan_data_folder - no partial writes"
  artifacts:
    - path: ".claude/doml/workflows/new-project.md"
      provides: "Replaced Step 2 and Step 3 stubs with full implementations"
      contains: "scan_data_folder"
    - path: ".planning/PROJECT.md"
      provides: "Filled template output - produced at interview completion in user's analysis project"
      contains: "We are trying to"
    - path: ".planning/config.json"
      provides: "Updated with analysis_language, problem_type, time_factor - produced at interview completion"
      contains: "analysis_language"
  key_links:
    - from: ".claude/doml/workflows/new-project.md Step 2"
      to: "doml/data_scan.py"
      via: "Python Bash call: from doml.data_scan import scan_data_folder, format_scan_report"
      pattern: "scan_data_folder"
    - from: ".claude/doml/workflows/new-project.md Step 3"
      to: ".planning/PROJECT.md"
      via: "Edit tool - fills placeholders atomically after all answers collected"
      pattern: "We are trying to"
    - from: ".claude/doml/workflows/new-project.md Step 3"
      to: ".planning/config.json"
      via: "Edit tool - updates analysis_language, problem_type, time_factor fields"
      pattern: "analysis_language"
---

<objective>
Replace the Phase 2 stubs in `new-project.md` (Steps 2 and 3) with full implementations: DuckDB data scan display and the guided kickoff interview.

Purpose: This is the primary user-facing deliverable of Phase 3. When a user runs `/doml-new-project`, they get a grounded, educational interview that infers their ML problem type, confirms time-factor, offers domain research, and writes a complete PROJECT.md + config.json before any analysis begins.

Output:
- `new-project.md` with Steps 2 and 3 fully implemented (Steps 4+5 already work — do not change them)
- When executed: a filled PROJECT.md with Problem Framing section and framing sentence
- When executed: config.json updated with analysis_language, problem_type, time_factor
</objective>

<execution_context>
@/home/bill/source/DoML/.claude/get-shit-done/workflows/execute-plan.md
@/home/bill/source/DoML/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/bill/source/DoML/.planning/PROJECT.md
@/home/bill/source/DoML/.planning/ROADMAP.md
@/home/bill/source/DoML/.planning/STATE.md
@/home/bill/source/DoML/.planning/phases/03-kickoff-interview/03-CONTEXT.md
@/home/bill/source/DoML/.planning/phases/03-kickoff-interview/03-RESEARCH.md
@/home/bill/source/DoML/.planning/phases/03-kickoff-interview/03-02-SUMMARY.md
@/home/bill/source/DoML/CLAUDE.md

<interfaces>
<!-- Key contracts the executor must know. Extracted from upstream plan outputs. -->

From doml/data_scan.py (Plan 03-02 output):
```python
def scan_data_folder(data_dir: Path) -> list:
    """Returns list of dicts: {path, format, row_count, col_count, columns: [{name, dtype}]}
    Raises ValueError with actionable message if dir missing, empty, or .xls found."""

def format_scan_report(results: list) -> str:
    """Returns formatted string: file list with format, row x col count, column names+dtypes."""
```

From .claude/doml/templates/PROJECT.md (template placeholders to fill):
  Frontmatter fields: project, created, last_updated, dataset, problem_type, language
  Body placeholders:
    [What decision or action will this analysis inform?]    → Q1 (business question)
    [Who will act on the findings from this analysis?]      → Q2 (stakeholder)
    [What does success look like? ...]                      → Q3 (expected outcome)
    [Brief description of what the data represents]         → Q4 (dataset description)
    [Column name, or "none" for unsupervised]               → derived from scan + type detection
    [regression / classification / ...]                     → confirmed problem type
    [Yes / No — Is time a relevant dimension?]             → Q5 (time factor)
    We are trying to [decision] using [metric/outcome column] as a proxy.  → drafted by Claude + confirmed
    Python (default) / R (opt-in)                           → Q8 (language preference)

From .planning/config.json (fields to update after interview):
  "language": "python"           → leave as-is (set at init)
  "analysis_language": "python"  → INTV-05: set by interview (add this key if missing)
  "problem_type": "unknown"      → set to confirmed problem type
  "time_factor": false           → set to true/false from Q5
  "dataset.path": "data/raw/"    → leave as-is
  "dataset.format": "unknown"    → set to primary format from scan (CSV/PARQUET/XLSX)

AskUserQuestion pattern (from doml-new-project SKILL.md allowed-tools):
  AskUserQuestion is already in the allowed-tools list — use it for all user-facing questions.
  If response is empty or whitespace-only, retry once, then fall back to plain-text prompt.

Steps 4+5 of new-project.md are already implemented — do NOT change them.
Step 1 (existing project detection) is already implemented — do NOT change it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Step 2 stub — DuckDB data scan and display</name>
  <files>
    .claude/doml/workflows/new-project.md
  </files>
  <read_first>
    - /home/bill/source/DoML/.claude/doml/workflows/new-project.md — read full current content before editing; understand exactly where Step 2 stub begins and ends
    - /home/bill/source/DoML/.planning/phases/03-kickoff-interview/03-02-SUMMARY.md — check for any deviations from the scan_data_folder contract (e.g., excel extension workaround)
    - /home/bill/source/DoML/.planning/phases/03-kickoff-interview/03-RESEARCH.md — Pattern 2 (empty /data/ hard stop), anti-patterns section (don't run interview before scan)
    - /home/bill/source/DoML/CLAUDE.md — REPR-02 (PROJECT_ROOT), INFR-05 (data/raw read-only)
  </read_first>
  <action>
Replace the Step 2 section of new-project.md. The current stub reads:

  **Phase 2 stub:** Check that `data/raw/` exists. If not, display: ...

Replace it with:



### Step 2 — Data folder validation and scan

Run a DuckDB scan of `data/raw/` to detect file formats and display schema context before the interview begins.

**Implementation:**

Use the Bash tool to run the following Python snippet. This calls the `doml.data_scan` module built in Phase 3 Plan 02.

```python
import os
from pathlib import Path
from doml.data_scan import scan_data_folder, format_scan_report

PROJECT_ROOT = Path(os.environ.get('PROJECT_ROOT', '.'))
data_dir = PROJECT_ROOT / 'data' / 'raw'

try:
    scan_results = scan_data_folder(data_dir)
    report = format_scan_report(scan_results)
    print(report)
except ValueError as e:
    print(f"\nError: {e}\n")
    raise SystemExit(1)
```

If the scan raises ValueError (missing directory, empty directory, or .xls file), print the error message and STOP. Do not write any planning artifacts. Do not proceed to Step 3.

If the scan succeeds, display the `format_scan_report` output to the user. Store `scan_results` in memory — it will be used to auto-populate dataset fields in Step 3.

**No partial writes.** If Step 2 fails, Steps 3–5 do not run.



Keep Step 1 (existing project detection) and Steps 4–5 (artifact generation and summary) exactly as they are.
  </action>
  <verify>
    <automated>grep -n "scan_data_folder" /home/bill/source/DoML/.claude/doml/workflows/new-project.md</automated>
  </verify>
  <done>
    - `grep -n "scan_data_folder" .claude/doml/workflows/new-project.md` returns at least one match
    - `grep -n "Phase 2 stub" .claude/doml/workflows/new-project.md` returns zero matches in Step 2 (stub text removed)
    - `grep -n "SystemExit" .claude/doml/workflows/new-project.md` returns a match (hard stop on error)
    - `grep -n "PROJECT_ROOT" .claude/doml/workflows/new-project.md` returns a match (REPR-02 path convention)
    - Step 2 section references `format_scan_report` output display before proceeding to Step 3
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace Step 3 stub — guided kickoff interview and output writing</name>
  <files>
    .claude/doml/workflows/new-project.md
  </files>
  <read_first>
    - /home/bill/source/DoML/.claude/doml/workflows/new-project.md — read current content after Task 1 edit; Step 3 stub text starts at "Phase 2 stub: Display: Kickoff interview not yet implemented"
    - /home/bill/source/DoML/.claude/doml/templates/PROJECT.md — exact placeholder strings to replace
    - /home/bill/source/DoML/.claude/doml/templates/config.json — exact JSON structure; fields to update
    - /home/bill/source/DoML/.planning/phases/03-kickoff-interview/03-CONTEXT.md — Decision 1 (inference+confirm), Decision 2 (atomic write to PROJECT.md), Decision 3 (scan first), Decision 4 (hard stop)
    - /home/bill/source/DoML/.planning/phases/03-kickoff-interview/03-RESEARCH.md — Pattern 3 (AskUserQuestion), Pattern 4 (question sequence), Pattern 5 (writing output), pitfall 3 (no partial writes), pitfall 4 (regression+time_factor upgrade), pitfall 5 (empty AskUserQuestion response)
  </read_first>
  <action>
Replace the Step 3 section of new-project.md. The current stub reads:

  **Phase 2 stub:** Display: Kickoff interview not yet implemented (Phase 3).

Replace it with the full interview implementation below:



### Step 3 — Kickoff interview

Conduct the guided business context interview. Collect all answers before writing any files. Write PROJECT.md and config.json atomically after the user confirms the decision framing sentence.

**Before asking questions**, display the scan report from Step 2 (already printed, but re-confirm it is visible).

**Question sequence:**

Pose each question using AskUserQuestion. After each response, check: if the response is empty or whitespace-only, retry once with the same question. If still empty, ask the user to type their answer in plain text.

**Q1 — Business question (INTV-01)**
"What business decision or action will this analysis inform? (Describe what you want to know or do.)"

**Q2 — Stakeholder (INTV-01)**
"Who is the stakeholder — who will act on the findings? (A person, team, or role.)"

**Q3 — Expected outcome (INTV-01)**
"What does success look like for them? What will they do differently after seeing the results?"

**Q4 — Dataset description (INTV-01)**
"Describe the dataset: what does each row represent? (Reference the column names shown above if helpful.)"

**Q5 — Time factor (INTV-02)**
"Is time a relevant dimension in this data? For example: does the order of rows matter, are there timestamps you want to use as a sequence, or do you need to forecast future values? (yes / no)"

Set `time_factor = True` if the user answers yes or describes temporal ordering.

**Problem type inference (INTV-04)**

After Q1–Q5, infer the ML problem type from the answers collected. Use this heuristic:
- Predict a continuous value → regression
- Predict a category or yes/no outcome → classification
- Discover natural groups, no known labels → clustering
- Ordered time observations + forecasting → time_series (especially if time_factor=True)
- Reduce features or visualize high-dimensional data → dimensionality_reduction

Explain your reasoning to the user, then ask:
"Based on your description, this looks like a **[inferred_type]** problem — [one-sentence reasoning].
Does that match your understanding, or should I reconsider?
  → Yes, that's right
  → Actually, it's [other type] — [brief reason]
  → I'm not sure — let's discuss"

Use AskUserQuestion for this. Accept the user's correction if they provide one.

**Time-factor upgrade check (Pitfall 4):**
If time_factor=True AND confirmed problem_type="regression", ask:
"Since time is a factor and you're predicting a continuous value over time, this is typically treated as a **time series** problem rather than cross-sectional regression. Does that apply here? (yes / no)"
If yes, change problem_type to "time_series".

**Q6 — Domain research offer (Decision 1)**
"I can look up common success metrics, known data pitfalls, and standard ML approaches for your domain. Would you like me to do a brief domain research step? (yes / no)"

If yes: Research the domain described in Q1–Q4 using your training knowledge. Summarize: (a) 2–3 typical success metrics for this problem type in this domain, (b) 2–3 known data pitfalls in this domain, (c) the standard ML approach. Store this summary as `domain_research_summary` — it will appear in PROJECT.md.

If no: Set `domain_research_summary = None`.

**Q7 — Language preference (INTV-05)**
"Python (default) or R for this project's notebooks? Press Enter for Python."

Default to "python" if the user presses Enter or responds with anything other than "r" or "R".
Set `analysis_language = "python"` or `"r"`.

**Decision framing sentence (INTV-04)**

Draft the framing sentence from the collected answers:
  "We are trying to [decision: what the stakeholder will do] using [metric/target column] as a proxy."

For unsupervised problems (clustering, dimensionality_reduction), use:
  "We are trying to [decision] — unsupervised, no proxy variable."

Show the drafted sentence to the user via AskUserQuestion:
"Here is the decision framing for this project:

  > [drafted framing sentence]

Does this capture the goal accurately? (confirm / revise)"

If the user asks to revise, incorporate their feedback and show the revised sentence. Repeat until confirmed.

**Atomic write — PROJECT.md:**

Only after the framing sentence is confirmed, write the completed PROJECT.md. Use the Edit tool on `.planning/PROJECT.md` to replace each placeholder:

| Placeholder in template | Replace with |
|-------------------------|--------------|
| `PROJECT_NAME` (frontmatter and heading) | directory name (basename of working directory) |
| `YYYY-MM-DD` (created) | today's date (ISO 8601) |
| `YYYY-MM-DD` (last_updated) | today's date (ISO 8601) |
| `DATASET_FILE_OR_FOLDER` | filenames from scan_results (comma-separated if multiple) |
| `problem_type: unknown` | `problem_type: [confirmed_type]` |
| `language: python` | `language: [analysis_language]` |
| `[What decision or action will this analysis inform?]` | Q1 answer |
| `[Who will act on the findings from this analysis?]` | Q2 answer |
| `[What does success look like? ...]` | Q3 answer |
| `[Brief description of what the data represents]` | Q4 answer |
| `[Date range or recency of data]` | leave as-is (unknown at this stage) |
| `[Any known issues with data quality...]` | leave as-is (unknown at this stage) |
| `[regression / classification / ...]` | confirmed problem type |
| `[Column name, or "none" for unsupervised]` | best guess from scan column names, or "unknown" |
| `[List of columns expected to be predictive]` | leave as-is |
| `[Yes / No — Is time a relevant dimension?]` | "Yes" or "No" + one-sentence explanation |
| `We are trying to [decision] using [metric/outcome column] as a proxy.` | confirmed framing sentence |
| `Python (default) / R (opt-in)` | `[analysis_language title-case]` |
| `[Why this preference was selected, if non-default]` | "User default" for python; user's reason for R |

If domain_research_summary is not None, append a new subsection after the Decision Framing line:

```
**Domain research summary:**
[domain_research_summary text here]
```

**Atomic write — config.json:**

After PROJECT.md is written, update `.planning/config.json` using the Edit tool. Modify these fields:
- `"analysis_language"`: add this key (or update if present) with `"python"` or `"r"`
- `"problem_type"`: set to confirmed problem type string
- `"time_factor"`: set to `true` or `false`
- `"dataset.format"`: set to primary format from scan_results (first file's format, lowercase)
- `"last_updated"`: set to today's date

Do not change any other config.json fields. Do not write a partially-updated config — read the full current config.json, modify the specific keys, then write the complete updated JSON.

**No partial writes (Pitfall 3):** If the interview is interrupted before the framing sentence is confirmed, do not write PROJECT.md or config.json. The templates remain in their initial state, which is safe for downstream phases.



Keep Steps 4 and 5 exactly as they are.
  </action>
  <verify>
    <automated>grep -n "AskUserQuestion\|framing sentence\|analysis_language\|time_factor\|domain_research" /home/bill/source/DoML/.claude/doml/workflows/new-project.md</automated>
  </verify>
  <done>
    - `grep -n "AskUserQuestion" .claude/doml/workflows/new-project.md` returns at least 5 matches (one per interview question)
    - `grep -n "analysis_language" .claude/doml/workflows/new-project.md` returns at least 2 matches (Q7 set + config.json write)
    - `grep -n "time_factor" .claude/doml/workflows/new-project.md` returns at least 2 matches (Q5 set + config.json write)
    - `grep -n "We are trying to" .claude/doml/workflows/new-project.md` returns at least 1 match (framing sentence template)
    - `grep -n "domain_research" .claude/doml/workflows/new-project.md` returns at least 2 matches (offer + conditional write)
    - `grep -n "No partial writes\|not write.*PROJECT\|not write.*config" .claude/doml/workflows/new-project.md` returns at least 1 match
    - `grep -n "Kickoff interview not yet implemented" .claude/doml/workflows/new-project.md` returns zero matches (stub removed)
    - Steps 4 and 5 are unchanged — `grep -n "Generate planning artifacts\|Display summary" .claude/doml/workflows/new-project.md` returns matches
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify end-to-end interview flow</name>
  <action>Human verification: run /doml-new-project end-to-end and confirm PROJECT.md and config.json are correctly populated. See how-to-verify instructions below.</action>
  <what-built>
    The complete /doml-new-project workflow:
    - Step 2: DuckDB scan of data/raw/ via scan_data_folder(), shown before interview begins
    - Step 3: 8-question guided interview with ML type inference, time-factor detection, domain research offer, framing sentence confirmation
    - Atomic write of PROJECT.md (all placeholders filled + Problem Framing section) and config.json
    - Hard stop with clear error on missing/empty data/raw/
  </what-built>
  <how-to-verify>
    1. Create a test directory with at least one CSV file in data/raw/:
       mkdir -p /tmp/doml-test/data/raw
       echo "id,value\n1,1.1\n2,2.2" > /tmp/doml-test/data/raw/sales.csv

    2. Run /doml-new-project from that directory. You should see:
       - DuckDB scan output showing sales.csv, 2 rows × 2 columns, id (BIGINT), value (DOUBLE)
       - Interview questions 1–5 asked in order
       - Claude infers problem type and explains reasoning
       - Domain research offer (can skip)
       - Language preference question
       - Drafted framing sentence shown for confirmation

    3. After confirming the framing sentence, check:
       - .planning/PROJECT.md: all [placeholder] tokens replaced, "We are trying to..." present
       - .planning/config.json: analysis_language, problem_type, time_factor all set

    4. Test the error case: remove data/raw/ files and run /doml-new-project again.
       Expected: Clear "No data files found" error, no .planning/ changes written.
  </how-to-verify>
  <resume-signal>Type "approved" if the interview runs correctly end-to-end, or describe what needs fixing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → AskUserQuestion → workflow | User input for all interview answers — free text, not validated before writing to files |
| workflow → PROJECT.md | Interview answers written directly into planning artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | User-supplied framing sentence written to PROJECT.md | accept | This is a local planning tool for the project owner; content is theirs to supply; no injection vector because file is local |
| T-03-01-02 | Denial of Service | AskUserQuestion empty response loop | mitigate | Workflow retries once on empty response, then falls back to plain-text prompt per Pitfall 5 — prevents infinite loop |
| T-03-01-03 | Tampering | config.json partial write | mitigate | Read full config → modify keys → write complete JSON atomically (not append/patch) per Pitfall 3 guidance |
| T-03-01-04 | Information Disclosure | Domain research step reveals training assumptions | accept | User explicitly opts in to domain research; findings are labeled as Claude's training knowledge |
</threat_model>

<verification>
Run after both tasks complete (before checkpoint):

```bash
# Step 2 stub is gone, scan_data_folder call present
grep -n "Phase 2 stub" .claude/doml/workflows/new-project.md | grep -i "step 2" && echo "STUB_STILL_PRESENT" || echo "STUB_REMOVED"
grep -n "scan_data_folder" .claude/doml/workflows/new-project.md

# Step 3 stub is gone, interview logic present
grep -n "Kickoff interview not yet implemented" .claude/doml/workflows/new-project.md && echo "STUB_STILL_PRESENT" || echo "STUB_REMOVED"
grep -n "AskUserQuestion" .claude/doml/workflows/new-project.md | wc -l

# INTV requirements traceable in workflow
grep -n "INTV-01\|INTV-02\|INTV-04\|INTV-05" .claude/doml/workflows/new-project.md

# Steps 4+5 preserved
grep -n "Step 4\|Step 5" .claude/doml/workflows/new-project.md

# REPR-02: PROJECT_ROOT used for paths
grep -n "PROJECT_ROOT" .claude/doml/workflows/new-project.md

# No hardcoded absolute paths
grep -n "/home\|/root\|/opt\|/usr" .claude/doml/workflows/new-project.md && echo "PATH_VIOLATION" || echo "PATHS_CLEAN"
```
</verification>

<success_criteria>
1. `grep -c "AskUserQuestion" .claude/doml/workflows/new-project.md` returns 5 or more (one per interview question + framing confirm + domain offer + language)
2. `grep "We are trying to" .claude/doml/workflows/new-project.md` returns a match (INTV-04 framing sentence)
3. `grep "analysis_language" .claude/doml/workflows/new-project.md` returns matches in both the Q7 set and the config.json write sections (INTV-05)
4. `grep "time_factor" .claude/doml/workflows/new-project.md` returns matches for both the Q5 detection and config.json update (INTV-02)
5. `grep "domain_research" .claude/doml/workflows/new-project.md` returns matches for the offer and the conditional PROJECT.md append
6. `grep "Kickoff interview not yet implemented" .claude/doml/workflows/new-project.md` returns nothing (Phase 2 stub removed)
7. `grep "scan_data_folder" .claude/doml/workflows/new-project.md` returns at least one match (INTV-03 scan integrated)
8. `grep "No partial writes\|not write" .claude/doml/workflows/new-project.md` returns a match (Pitfall 3 guard)
9. Manual checkpoint: end-to-end interview produces filled PROJECT.md with framing sentence and updated config.json
</success_criteria>

<output>
After completion, create `.planning/phases/03-kickoff-interview/03-01-SUMMARY.md` following the summary template at `/home/bill/source/DoML/.claude/get-shit-done/templates/summary.md`.

Key items to document in SUMMARY:
- Whether AskUserQuestion rendered correctly in the test session
- Any edge cases discovered during the manual checkpoint (e.g., empty answer handling, time-factor upgrade path)
- Exact final form of the framing sentence placeholder replacement
- Whether domain research step was exercised and what it produced
- Confirmation that Steps 4+5 of new-project.md were not broken by the edits
</output>
