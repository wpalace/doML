---
phase: 04-business-understanding
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - .claude/doml/workflows/execute-phase.md
  - .claude/doml/workflows/plan-phase.md
autonomous: true
requirements:
  - BU-01
  - BU-02
  - BU-03
  - BU-04
  - OUT-01

must_haves:
  truths:
    - "execute-phase.md Step 2 does not stop for missing PLAN.md when phase argument is 1 or 2 (BU/EDA are self-contained)"
    - "execute-phase.md Step 3 Phase 1 stub is removed and replaced with BU executor instructions"
    - "BU executor instructions: copy template to notebooks/, execute via docker compose exec jupyter, verify notebook exists"
    - "plan-phase.md Step 4 BU planner stub is updated to inform users that /doml-execute-phase 1 is used directly"
    - "grep 'Phase 2 stub' execute-phase.md | grep -i 'phase 1\\|business understanding' returns zero matches"
    - "grep 'business_understanding.ipynb' execute-phase.md returns at least one match"
  artifacts:
    - path: ".claude/doml/workflows/execute-phase.md"
      provides: "Phase 1 (BU) executor — replaces Phase 2 stub; includes copy-template, nbconvert --execute, verify steps"
      contains: "business_understanding.ipynb"
    - path: ".claude/doml/workflows/plan-phase.md"
      provides: "BU planner stub updated — directs users to /doml-execute-phase 1"
      contains: "doml-execute-phase"
  key_links:
    - from: ".claude/doml/workflows/execute-phase.md BU executor"
      to: ".claude/doml/templates/notebooks/business_understanding.ipynb"
      via: "cp .claude/doml/templates/notebooks/business_understanding.ipynb notebooks/"
      pattern: "business_understanding.ipynb"
    - from: ".claude/doml/workflows/execute-phase.md BU executor"
      to: "docker compose exec jupyter"
      via: "docker compose exec jupyter jupyter nbconvert --execute"
      pattern: "nbconvert --execute"
---

<objective>
Implement the Business Understanding executor in `execute-phase.md` — replacing the Phase 2 stub for Phase 1 with full step-by-step instructions that copy the notebook template, execute it inside Docker, and verify output.

Also update `plan-phase.md` to remove the misleading BU planner stub (Business Understanding does not require a separate PLAN.md — the executor in execute-phase.md is self-contained).

Output:
- `execute-phase.md` with Phase 1 (BU) executor implemented (stub removed)
- `plan-phase.md` with BU planner stub updated to redirect to `/doml-execute-phase 1`
</objective>

<execution_context>
@/home/bill/source/DoML/.claude/get-shit-done/workflows/execute-plan.md
@/home/bill/source/DoML/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/bill/source/DoML/.planning/PROJECT.md
@/home/bill/source/DoML/.planning/ROADMAP.md
@/home/bill/source/DoML/.planning/STATE.md
@/home/bill/source/DoML/.planning/phases/04-business-understanding/04-CONTEXT.md
@/home/bill/source/DoML/.planning/phases/04-business-understanding/04-01-SUMMARY.md
@/home/bill/source/DoML/CLAUDE.md

<interfaces>
From execute-phase.md (current content — stub to replace):
  Step 2: "Look for .planning/phases/[N]-*/[N]-*-PLAN.md. If no PLAN.md found: stop."
  → Update Step 2 to skip PLAN.md check for phases 1 and 2 (self-contained executors)

  Step 3: "Phase 2 stub: For unimplemented phases, display: Executor for Phase [N] not yet implemented."
  → Replace with Phase 1 (Business Understanding) executor instructions

From plan-phase.md (current content — stub to update):
  Step 4: "Phase 2 stub: For unimplemented phases, display: Phase [N] planner not yet implemented."
  → Update for Phase 1 to say: BU uses /doml-execute-phase 1 directly; no separate PLAN.md needed

From 04-01-PLAN.md (template location):
  Template: .claude/doml/templates/notebooks/business_understanding.ipynb
  Output:   notebooks/01_business_understanding.ipynb

Docker execution pattern (consistent with existing project conventions):
  docker compose exec jupyter jupyter nbconvert --execute --to notebook --inplace \
    notebooks/01_business_understanding.ipynb \
    --ExecutePreprocessor.timeout=600

Steps 4, 5, 6, 7 of execute-phase.md (not modified in this plan):
  Step 4: Execute PLAN.md tasks (for non-BU/EDA phases — unchanged)
  Step 5: Generate HTML report (implemented in Plan 04-03)
  Step 6: Update STATE.md
  Step 7: Confirm
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update execute-phase.md — Step 2 and Step 3 BU executor</name>
  <files>
    .claude/doml/workflows/execute-phase.md
  </files>
  <read_first>
    - /home/bill/source/DoML/.claude/doml/workflows/execute-phase.md — read full current content; understand exact Step 2 and Step 3 text before editing
    - /home/bill/source/DoML/.planning/phases/04-business-understanding/04-CONTEXT.md — Decision 2 (nbconvert --execute, not papermill), Decision 4 (self-contained executor, no PLAN.md required)
    - /home/bill/source/DoML/.planning/phases/04-business-understanding/04-01-SUMMARY.md — confirm template path (may differ if executor changed the location)
    - /home/bill/source/DoML/CLAUDE.md — REPR-02 (PROJECT_ROOT), INFR-05 (data/raw read-only)
  </read_first>
  <action>
Make two edits to execute-phase.md:

**Edit 1: Update Step 2 to skip PLAN.md check for BU/EDA phases**

Find the current Step 2 text (approximately):
```
### Step 2 — Find PLAN.md

Determine target phase (from argument or STATE.md `current_focus`).
Look for `.planning/phases/[N]-*/[N]-*-PLAN.md`.

If no PLAN.md found:

```
No plan found for Phase [N]. Run /doml-plan-phase [N] first.
```

Stop.
```

Replace it with:
```
### Step 2 — Determine target phase and executor

Determine target phase number from the argument or STATE.md `current_focus`.

**For analysis phases 1 (Business Understanding) and 2 (Data Understanding):** These phases use built-in executors embedded in this workflow (Steps 3–4). No separate PLAN.md is required. Skip directly to Step 3.

**For all other phases:** Look for `.planning/phases/[N]-*/[N]-*-PLAN.md`. If no PLAN.md found:

```
No plan found for Phase [N]. Run /doml-plan-phase [N] first.
```

Stop.
```

**Edit 2: Replace the Phase 2 stub in Step 3 with the Business Understanding executor**

Find the current Step 3 Phase 2 stub text (approximately):
```
**Phase 2 stub:** For unimplemented phases, display:

```
Executor for Phase [N] not yet implemented.
...
```
```

Replace the entire "Phase 2 stub" block with the Phase 1 Business Understanding executor. Keep the existing table and Phase 2 row. The replacement content for the Phase 1 executor:

```
#### Phase 1 — Business Understanding executor

**Pre-condition:** `.planning/config.json` and `.planning/PROJECT.md` must exist (produced by `/doml-new-project`). If either is missing, display:
```
PROJECT.md or config.json not found. Run /doml-new-project first.
```
and stop.

**Step 3a — Copy notebook template**

Copy the BU notebook template into the project's notebooks directory:
```bash
cp .claude/doml/templates/notebooks/business_understanding.ipynb \
   notebooks/01_business_understanding.ipynb
```

If `notebooks/` does not exist, create it first:
```bash
mkdir -p notebooks
```

If `notebooks/01_business_understanding.ipynb` already exists, warn the user:
```
notebooks/01_business_understanding.ipynb already exists. Overwrite? (yes / no)
```
Use AskUserQuestion for this. If the user says no, stop without overwriting.

**Step 3b — Execute the notebook inside Docker**

Run the notebook to populate all cells with output:
```bash
docker compose exec jupyter jupyter nbconvert \
  --execute \
  --to notebook \
  --inplace \
  notebooks/01_business_understanding.ipynb \
  --ExecutePreprocessor.timeout=600
```

If the container is not running, display:
```
Docker container is not running. Start it with:
  docker compose up -d
Then run /doml-execute-phase 1 again.
```
and stop.

On execution failure (non-zero exit code), display the nbconvert error output and stop. Do not proceed to HTML generation.

**Step 3c — Verify notebook output**

After execution, verify that `notebooks/01_business_understanding.ipynb` exists and has cell outputs:
```bash
python3 -c "
import nbformat, json
with open('notebooks/01_business_understanding.ipynb') as f:
    nb = nbformat.read(f, as_version=4)
code_cells_with_output = sum(1 for c in nb.cells if c.cell_type == 'code' and c.outputs)
print(f'Executed notebook: {code_cells_with_output} code cells with output')
assert code_cells_with_output > 0, 'No cell outputs found — execution may have failed'
"
```

If verification fails, report the error and stop. Do not generate the HTML report on an unexecuted notebook.

**Note for Phase 2 (Data Understanding):** Executor will be implemented in DoML Phase 5.
```

Keep all other content in execute-phase.md (Steps 4, 5, 6, 7) exactly as they are.
  </action>
  <verify>
    <automated>grep -n "business_understanding.ipynb\|Phase 2 stub\|Executor for Phase\|nbconvert --execute" /home/bill/source/DoML/.claude/doml/workflows/execute-phase.md</automated>
  </verify>
  <done>
    - `grep -c "business_understanding.ipynb" .claude/doml/workflows/execute-phase.md` returns >= 2 (template source and destination)
    - `grep -c "nbconvert --execute" .claude/doml/workflows/execute-phase.md` returns >= 1
    - `grep "Phase 2 stub" .claude/doml/workflows/execute-phase.md | grep -i "executor\|not yet implemented"` returns zero matches (stub removed)
    - `grep -c "Step 3a\|Step 3b\|Step 3c" .claude/doml/workflows/execute-phase.md` returns 3 (sub-steps present)
    - `grep -c "ExecutePreprocessor.timeout" .claude/doml/workflows/execute-phase.md` returns >= 1
    - `grep -c "docker compose exec jupyter" .claude/doml/workflows/execute-phase.md` returns >= 1
    - Steps 5, 6, 7 are still present: `grep -c "Step 5\|Step 6\|Step 7" .claude/doml/workflows/execute-phase.md` returns 3
  </done>
</task>

<task type="auto">
  <name>Task 2: Update plan-phase.md BU planner stub</name>
  <files>
    .claude/doml/workflows/plan-phase.md
  </files>
  <read_first>
    - /home/bill/source/DoML/.claude/doml/workflows/plan-phase.md — read full current content; understand exact Step 4 stub text before editing
    - /home/bill/source/DoML/.planning/phases/04-business-understanding/04-CONTEXT.md — Decision 4 (no PLAN.md needed for BU)
  </read_first>
  <action>
In plan-phase.md, find Step 4 "Route to phase-specific planner". Currently it has a table and a "Phase 2 stub" block for unimplemented planners.

Update the table to show Phase 1 (BU) as implemented:
```
| Phase | Analysis Phase | Planner |
|-------|---------------|---------|
| 1 | Business Understanding | Built-in executor (DoML Phase 4) — use `/doml-execute-phase 1` directly |
| 2 | Data Understanding | EDA planner (DoML Phase 5) |
```

Update the Phase 2 stub section to add a Phase 1-specific message. When the user runs `/doml-plan-phase 1`, display:

```
Business Understanding (Phase 1) does not require a separate plan.

The executor is built into /doml-execute-phase and runs automatically after the kickoff interview.

To generate the Business Understanding notebook and report, run:
  /doml-execute-phase 1

Prerequisites:
  - /doml-new-project has been run and PROJECT.md + config.json are present
  - Docker container is running (docker compose up -d)
  - data/raw/ contains at least one CSV, Parquet, or Excel file
```

Keep the existing stub for other unimplemented phases (EDA planner, etc.) unchanged.
  </action>
  <verify>
    <automated>grep -n "Business Understanding\|doml-execute-phase 1\|Phase 2 stub" /home/bill/source/DoML/.claude/doml/workflows/plan-phase.md</automated>
  </verify>
  <done>
    - `grep -c "doml-execute-phase 1" .claude/doml/workflows/plan-phase.md` returns >= 1 (redirects user to execute command)
    - `grep -c "Business Understanding" .claude/doml/workflows/plan-phase.md` returns >= 1
    - Phase 2 stub for EDA and other phases is preserved (not removed)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| execute-phase.md → Bash | Docker exec commands run inside the Jupyter container; paths are controlled by the workflow |
| execute-phase.md → filesystem | File copy and nbformat read are local operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | notebooks/01_business_understanding.ipynb overwrite | mitigate | Executor asks user before overwriting existing notebook (AskUserQuestion gate in Step 3a) |
| T-04-02-02 | Denial of Service | nbconvert --execute timeout on large dataset | mitigate | `--ExecutePreprocessor.timeout=600` (10 min) cap; DuckDB COUNT(*) + LIMIT 3 are fast even on large files |
| T-04-02-03 | Elevation of Privilege | docker compose exec grants container shell | accept | This is the intended usage; user already controls docker compose and the container |
| T-04-02-04 | Denial of Service | Container not running causes silent failure | mitigate | Executor checks for non-zero exit code and shows "docker compose up -d" guidance |
</threat_model>

<verification>
```bash
# BU executor is present in execute-phase.md
grep -n "business_understanding.ipynb" .claude/doml/workflows/execute-phase.md
grep -n "nbconvert --execute" .claude/doml/workflows/execute-phase.md
grep -n "Step 3a\|Step 3b\|Step 3c" .claude/doml/workflows/execute-phase.md

# Phase 2 stub for executor is removed
grep "Phase 2 stub" .claude/doml/workflows/execute-phase.md | grep -i "executor\|not yet implemented" && echo "STUB_STILL_PRESENT" || echo "STUB_REMOVED"

# plan-phase.md redirects BU to execute-phase
grep -n "doml-execute-phase 1" .claude/doml/workflows/plan-phase.md

# Steps 5-7 of execute-phase.md preserved
grep -c "Step 5\|Step 6\|Step 7" .claude/doml/workflows/execute-phase.md
```
</verification>

<success_criteria>
1. `grep "business_understanding.ipynb" .claude/doml/workflows/execute-phase.md` returns at least 2 matches (template source + output destination)
2. `grep "nbconvert --execute" .claude/doml/workflows/execute-phase.md` returns at least 1 match
3. `grep "ExecutePreprocessor.timeout" .claude/doml/workflows/execute-phase.md` returns at least 1 match
4. `grep "Phase 2 stub" .claude/doml/workflows/execute-phase.md | grep -c "Executor for Phase"` returns 0 (BU executor stub removed)
5. `grep -c "Step 3a\|Step 3b\|Step 3c" .claude/doml/workflows/execute-phase.md` returns 3 (sub-steps present)
6. `grep -c "docker compose exec jupyter" .claude/doml/workflows/execute-phase.md` returns at least 1
7. `grep "doml-execute-phase 1" .claude/doml/workflows/plan-phase.md` returns at least 1 match (BU redirect)
8. Steps 5, 6, 7 in execute-phase.md are unchanged — `grep -c "Generate HTML\|Update STATE\|Confirm" .claude/doml/workflows/execute-phase.md` returns 3
</success_criteria>

<output>
After completion, create `.planning/phases/04-business-understanding/04-02-SUMMARY.md` following the summary template at `/home/bill/source/DoML/.claude/get-shit-done/templates/summary.md`.

Key items to document:
- Exact wording of the Phase 3a/3b/3c instructions in execute-phase.md (copy verbatim so future phases can follow same pattern)
- Whether the Step 2 edit changed any behavior for non-BU phases
- Confirmation that Steps 5-7 of execute-phase.md were not broken
- What the plan-phase.md BU redirect message says (verbatim)
</output>
