---
phase: 05-data-understanding
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - .claude/doml/workflows/execute-phase.md
autonomous: true
requirements:
  - EDA-01
  - CMD-03

must_haves:
  truths:
    - "execute-phase.md Step 3 Phase 2 (Data Understanding) executor is fully implemented — not a stub"
    - "Phase 2 executor reads analysis_language from .planning/config.json before copying template"
    - "If analysis_language == 'python': copies data_understanding_python.ipynb"
    - "If analysis_language == 'r': copies data_understanding_r.ipynb"
    - "Phase 2 executor has Steps 3d/3e/3f (copy, execute, verify) mirroring Phase 1 Steps 3a/3b/3c"
    - "Phase 2 executor asks before overwriting existing notebooks/02_data_understanding.ipynb"
    - "Phase 2 executor uses docker compose exec jupyter jupyter nbconvert --execute --to notebook --inplace"
    - "grep 'data_understanding_python.ipynb' execute-phase.md returns at least one match"
    - "grep 'data_understanding_r.ipynb' execute-phase.md returns at least one match"
    - "grep 'Note for Phase 2' execute-phase.md | grep 'Executor will be implemented' returns zero matches (stub removed)"
    - "Steps 5a-5d (HTML report), Step 6 (STATE.md), Step 7 (Confirm) are still present and unmodified"
  artifacts:
    - path: ".claude/doml/workflows/execute-phase.md"
      provides: "Phase 2 (EDA) executor — language-adaptive template selection, nbconvert execution, verification"
      contains: "data_understanding_python.ipynb"
  key_links:
    - from: ".claude/doml/workflows/execute-phase.md Phase 2 executor"
      to: ".claude/doml/templates/notebooks/data_understanding_python.ipynb"
      via: "cp .claude/doml/templates/notebooks/data_understanding_python.ipynb notebooks/"
      pattern: "data_understanding_python.ipynb"
    - from: ".claude/doml/workflows/execute-phase.md Phase 2 executor"
      to: ".claude/doml/templates/notebooks/data_understanding_r.ipynb"
      via: "cp .claude/doml/templates/notebooks/data_understanding_r.ipynb notebooks/"
      pattern: "data_understanding_r.ipynb"
    - from: ".claude/doml/workflows/execute-phase.md Phase 2 executor"
      to: ".planning/config.json"
      via: "python3 -c \"import json; c = json.load(open('.planning/config.json')); print(c.get('language','python'))\""
      pattern: "config.json"
---

<objective>
Implement the Phase 2 (Data Understanding / EDA) executor in `execute-phase.md`.

Currently, execute-phase.md has a note: "Note for Phase 2 (Data Understanding): Executor will be implemented in DoML Phase 5." This plan replaces that stub with a complete executor that:
1. Reads `analysis_language` from `.planning/config.json`
2. Copies the appropriate notebook template (Python or R)
3. Executes the notebook inside Docker
4. Verifies the notebook has cell outputs

This mirrors the Phase 1 (BU) executor pattern (Steps 3a/3b/3c) exactly, using Steps 3d/3e/3f numbering.

Output:
- `execute-phase.md` with Phase 2 (EDA) executor fully implemented
</objective>

<execution_context>
@/home/bill/source/DoML/.claude/get-shit-done/workflows/execute-plan.md
@/home/bill/source/DoML/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/bill/source/DoML/.planning/PROJECT.md
@/home/bill/source/DoML/.planning/ROADMAP.md
@/home/bill/source/DoML/.planning/STATE.md
@/home/bill/source/DoML/.planning/phases/05-data-understanding/05-CONTEXT.md
@/home/bill/source/DoML/.planning/phases/05-data-understanding/05-01-SUMMARY.md
@/home/bill/source/DoML/.planning/phases/05-data-understanding/05-02-SUMMARY.md
@/home/bill/source/DoML/CLAUDE.md

<interfaces>
From execute-phase.md current stub text to find and replace:
  "**Note for Phase 2 (Data Understanding):** Executor will be implemented in DoML Phase 5."

Phase 2 executor output:
  notebook:    notebooks/02_data_understanding.ipynb
  report:      reports/eda_report.html (implemented in Plan 05-04)

Phase 1 executor steps to mirror (Steps 3a/3b/3c already in execute-phase.md):
  3a: Copy template → notebooks/01_business_understanding.ipynb
  3b: Execute via docker compose exec jupyter jupyter nbconvert --execute --to notebook --inplace
  3c: Verify notebook has cell outputs

Phase 2 executor steps (use Steps 3d/3e/3f):
  3d: Read analysis_language + copy appropriate template to notebooks/02_data_understanding.ipynb
  3e: Execute via docker compose exec jupyter jupyter nbconvert --execute --to notebook --inplace
  3f: Verify notebook has cell outputs

Template paths:
  Python: .claude/doml/templates/notebooks/data_understanding_python.ipynb
  R:      .claude/doml/templates/notebooks/data_understanding_r.ipynb

Language detection:
  python3 -c "import json; c = json.load(open('.planning/config.json')); print(c.get('language','python'))"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update execute-phase.md — add Phase 2 (EDA) executor</name>
  <files>
    .claude/doml/workflows/execute-phase.md
  </files>
  <read_first>
    - /home/bill/source/DoML/.claude/doml/workflows/execute-phase.md — read FULL current content; identify exact stub text to replace and exact location of Step 3c
    - /home/bill/source/DoML/.planning/phases/05-data-understanding/05-CONTEXT.md — Decision 3 (language-adaptive notebook selection)
    - /home/bill/source/DoML/.planning/phases/04-business-understanding/04-02-SUMMARY.md — exact Steps 3a/3b/3c wording for consistency
  </read_first>
  <action>
Read execute-phase.md in full. Then make two edits:

**Edit 1: Update the routing table in Step 3**

Find the existing table under "Step 3 — Route to phase executor":
```
| Phase | Executor | Notebook | Report |
|-------|----------|----------|--------|
| 1 — Business Understanding | DoML Phase 4 | notebooks/01_business_understanding.ipynb | reports/business_summary.html |
| 2 — Data Understanding | DoML Phase 5 | notebooks/02_data_understanding.ipynb | reports/eda_report.html |
```

This table already exists — confirm it is present. If it is already correct, no edit needed.

**Edit 2: Replace the Phase 2 stub note with the full EDA executor**

Find this text (approximately — read the file to get exact wording):
```
**Note for Phase 2 (Data Understanding):** Executor will be implemented in DoML Phase 5.
```

Replace it with the following Phase 2 executor block. Add it immediately after the Phase 1 executor block ends (after Step 3c):

```markdown
#### Phase 2 — Data Understanding executor

**Pre-condition:** `.planning/config.json` must exist with `language` field. If missing, display:
```
PROJECT.md or config.json not found. Run /doml-new-project first.
```
and stop.

**Step 3d — Detect language + copy notebook template**

Read the `language` field from `.planning/config.json`:
```bash
LANG=$(python3 -c "import json; c = json.load(open('.planning/config.json')); print(c.get('language','python'))")
echo "Analysis language: $LANG"
```

Copy the appropriate template:
```bash
mkdir -p notebooks

if [ "$LANG" = "r" ]; then
  TEMPLATE=".claude/doml/templates/notebooks/data_understanding_r.ipynb"
else
  TEMPLATE=".claude/doml/templates/notebooks/data_understanding_python.ipynb"
fi

echo "Using template: $TEMPLATE"
```

If `notebooks/02_data_understanding.ipynb` already exists, ask before overwriting:
```
notebooks/02_data_understanding.ipynb already exists. Overwrite? (yes / no)
```
Use AskUserQuestion for this. If the user says no, stop without overwriting.

Copy the template:
```bash
cp "$TEMPLATE" notebooks/02_data_understanding.ipynb
```

**Step 3e — Execute the notebook inside Docker**

```bash
docker compose exec jupyter jupyter nbconvert \
  --execute \
  --to notebook \
  --inplace \
  notebooks/02_data_understanding.ipynb \
  --ExecutePreprocessor.timeout=600
```

If the container is not running, display:
```
Docker container is not running. Start it with:
  docker compose up -d
Then run /doml-execute-phase 2 again.
```
and stop.

On execution failure (non-zero exit code), display the nbconvert error output and stop. Do not proceed to HTML generation.

**Step 3f — Verify notebook output**

```bash
python3 -c "
import nbformat
with open('notebooks/02_data_understanding.ipynb') as f:
    nb = nbformat.read(f, as_version=4)
code_cells_with_output = sum(1 for c in nb.cells if c.cell_type == 'code' and c.outputs)
print(f'Executed notebook: {code_cells_with_output} code cells with output')
assert code_cells_with_output > 0, 'No cell outputs found — execution may have failed'
"
```

If verification fails, report the error and stop. Do not generate the HTML report on an unexecuted notebook.
```

Keep all other content in execute-phase.md unchanged (Steps 4, 5a–5d, 6, 7).
  </action>
  <verify>
    <automated>grep -n "data_understanding_python.ipynb\|data_understanding_r.ipynb\|Step 3d\|Step 3e\|Step 3f\|Executor will be implemented" /home/bill/source/DoML/.claude/doml/workflows/execute-phase.md</automated>
  </verify>
  <done>
    - `grep -c "data_understanding_python.ipynb" .claude/doml/workflows/execute-phase.md` returns >= 1
    - `grep -c "data_understanding_r.ipynb" .claude/doml/workflows/execute-phase.md` returns >= 1
    - `grep -c "Step 3d\|Step 3e\|Step 3f" .claude/doml/workflows/execute-phase.md` returns 3
    - `grep "Executor will be implemented" .claude/doml/workflows/execute-phase.md` returns 0 lines (stub removed)
    - `grep -c "docker compose exec jupyter" .claude/doml/workflows/execute-phase.md` returns >= 2 (Phase 1 + Phase 2)
    - Steps 5a-5d, 6, 7 still present: `grep -c "Step 5a\|Step 5b\|Step 5c\|Step 5d\|Step 6\|Step 7" .claude/doml/workflows/execute-phase.md` returns 6
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| execute-phase.md → .planning/config.json | Reads language field — user-supplied; only used to select template path |
| execute-phase.md → Docker | docker compose exec runs nbconvert inside the container — user already controls Docker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | notebooks/02_data_understanding.ipynb overwrite | mitigate | AskUserQuestion gate before overwriting in Step 3d |
| T-05-03-02 | Denial of Service | nbconvert execution timeout on large EDA notebook | mitigate | `--ExecutePreprocessor.timeout=600` cap; 500k row threshold in the notebook itself |
| T-05-03-03 | Elevation of Privilege | docker compose exec grants container access | accept | Intended usage; user already controls compose |
| T-05-03-04 | Tampering | Wrong language selected (Python vs R) | mitigate | Executor reads language from config.json; defaults to python if key missing |
</threat_model>

<verification>
```bash
# Phase 2 executor is present
grep -n "Phase 2 — Data Understanding executor" .claude/doml/workflows/execute-phase.md
grep -n "Step 3d\|Step 3e\|Step 3f" .claude/doml/workflows/execute-phase.md
grep -n "data_understanding_python.ipynb\|data_understanding_r.ipynb" .claude/doml/workflows/execute-phase.md

# Phase 2 stub is removed
grep "Executor will be implemented" .claude/doml/workflows/execute-phase.md && echo "STUB_STILL_PRESENT" || echo "STUB_REMOVED"

# Phase 1 executor and Steps 5-7 preserved
grep -c "Step 3a\|Step 3b\|Step 3c" .claude/doml/workflows/execute-phase.md
grep -c "Step 5a\|Step 5b\|Step 5c\|Step 5d" .claude/doml/workflows/execute-phase.md
grep -c "Step 6\|Step 7" .claude/doml/workflows/execute-phase.md
```
</verification>

<success_criteria>
1. `grep -c "data_understanding_python.ipynb" .claude/doml/workflows/execute-phase.md` returns >= 1
2. `grep -c "data_understanding_r.ipynb" .claude/doml/workflows/execute-phase.md` returns >= 1
3. `grep -c "Step 3d\|Step 3e\|Step 3f" .claude/doml/workflows/execute-phase.md` returns 3
4. `grep "Executor will be implemented" .claude/doml/workflows/execute-phase.md` returns 0 lines (stub removed)
5. `grep -c "docker compose exec jupyter" .claude/doml/workflows/execute-phase.md` returns >= 2 (both phases)
6. `grep -c "Step 5a\|Step 5b\|Step 5c\|Step 5d" .claude/doml/workflows/execute-phase.md` returns 4 (HTML report steps preserved)
7. Steps 6 and 7 preserved: `grep -c "Step 6\|Step 7" .claude/doml/workflows/execute-phase.md` returns 2
8. Phase 2 executor includes language detection reading from config.json
9. Phase 2 executor includes AskUserQuestion gate for overwrite protection
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-understanding/05-03-SUMMARY.md` following the summary template at `/home/bill/source/DoML/.claude/get-shit-done/templates/summary.md`.

Key items to document:
- Exact wording of Steps 3d/3e/3f in execute-phase.md (verbatim, for future reference)
- How language detection is implemented (the python3 -c command used)
- Confirmation that Phase 1 Steps 3a/3b/3c were not modified
- Confirmation that Steps 5a-5d, 6, 7 are preserved
- Whether the routing table was already correct or needed updating
</output>
