---
phase: 05-data-understanding
plan: 04
type: execute
wave: 4
depends_on:
  - "05-03"
files_modified:
  - .claude/doml/workflows/execute-phase.md
autonomous: false
requirements:
  - EDA-09
  - EDA-10
  - OUT-01
  - OUT-02
  - OUT-03

must_haves:
  truths:
    - "execute-phase.md Step 5 EDA report section is implemented — not a stub"
    - "EDA executor Step 5a generates a Claude executive narrative from EDA findings (EDA-10)"
    - "EDA Step 5a narrative avoids technical jargon — targets non-technical stakeholders"
    - "EDA Step 5b creates reports/ directory"
    - "EDA Step 5c runs: jupyter nbconvert --to html --no-input notebooks/02_data_understanding.ipynb --output-dir reports --output eda_report"
    - "EDA Step 5d verifies: reports/eda_report.html exists + code cells hidden + caveats present"
    - "grep 'eda_report.html' execute-phase.md returns at least one match"
    - "grep 'no-input' execute-phase.md returns at least two matches (BU phase + EDA phase)"
    - "grep 'Note for Phase 2.*eda_report' execute-phase.md returns zero matches (stub removed)"
    - "BU Steps 5a-5d (business_summary.html) are still present and unmodified"
    - "Steps 6 and 7 are still present and unmodified"
  artifacts:
    - path: ".claude/doml/workflows/execute-phase.md"
      provides: "EDA HTML report pipeline — executive narrative + nbconvert --no-input → eda_report.html"
      contains: "eda_report.html"
  key_links:
    - from: ".claude/doml/workflows/execute-phase.md EDA Step 5"
      to: "notebooks/02_data_understanding.ipynb"
      via: "jupyter nbconvert --to html --no-input"
      pattern: "no-input"
    - from: ".claude/doml/workflows/execute-phase.md EDA Step 5"
      to: "reports/eda_report.html"
      via: "--output-dir reports --output eda_report"
      pattern: "eda_report.html"
---

<objective>
Implement the EDA HTML report generation step in `execute-phase.md` for Data Understanding (EDA-09, EDA-10, OUT-01, OUT-02, OUT-03).

Currently, execute-phase.md Step 5 has a note: "Note for Phase 2 (Data Understanding): Step 5 for EDA will use `eda_report.html` as output. Implemented in DoML Phase 5."

This plan extends Step 5 with a Phase 2 (EDA) branch that:
1. Generates a Claude executive narrative interpreting key EDA findings in business terms
2. Inserts the narrative as the first Markdown cell in the executed notebook
3. Runs `nbconvert --to html --no-input` to produce `reports/eda_report.html`
4. Verifies the HTML exists, code is hidden, and "correlation is not causation" is present

This plan includes a human checkpoint (Task 2) to verify the HTML report end-to-end.

Output:
- `execute-phase.md` with Step 5 fully implemented for EDA (stub replaced)
- After UAT: `reports/eda_report.html` produced in a test project with code hidden
</objective>

<execution_context>
@/home/bill/source/DoML/.claude/get-shit-done/workflows/execute-plan.md
@/home/bill/source/DoML/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/bill/source/DoML/.planning/PROJECT.md
@/home/bill/source/DoML/.planning/ROADMAP.md
@/home/bill/source/DoML/.planning/STATE.md
@/home/bill/source/DoML/.planning/phases/05-data-understanding/05-CONTEXT.md
@/home/bill/source/DoML/.planning/phases/05-data-understanding/05-03-SUMMARY.md
@/home/bill/source/DoML/CLAUDE.md

<interfaces>
Current Step 5 stub text to find and replace (EDA section only):
  "**Note for Phase 2 (Data Understanding):** Step 5 for EDA will use `eda_report.html` as output. Implemented in DoML Phase 5."

BU Step 5 pattern to mirror (already implemented in execute-phase.md):
  Step 5a: Write executive narrative → insert as first Markdown cell via nbformat
  Step 5b: mkdir -p reports
  Step 5c: jupyter nbconvert --to html --no-input --output-dir reports --output [name]
  Step 5d: Verify file exists + code hidden + caveats + executive summary

EDA-specific values:
  notebook: notebooks/02_data_understanding.ipynb
  report:   reports/eda_report.html (--output eda_report)

EDA-10: Executive narrative for EDA must:
  - Summarise key findings: dataset size, top correlated features, missing value counts
  - Note whether distributions are normal or skewed (with named columns)
  - State time series stationarity result if time_factor=true
  - Flag any tidy violations found
  - Be 2–3 short paragraphs, non-technical

Sources for EDA narrative:
  - .planning/PROJECT.md — business question, stakeholder
  - .planning/config.json — problem_type, time_factor
  - notebooks/02_data_understanding.ipynb — executed cell outputs (DuckDB profiling results,
    normality test table, tidy violations flagged in outputs)

nbformat insertion pattern (same as BU):
  python3 /tmp/doml_insert_eda_summary.py
  (Write narrative to temp script to avoid special-character quoting issues)

OUT-02 (code cells hidden):
  nbconvert --no-input strips all code cells
  Verify: HTML must NOT contain '<div class="input">'

OUT-03 (caveats):
  "correlation is not causation" must appear in HTML
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update execute-phase.md Step 5 with EDA HTML report pipeline</name>
  <files>
    .claude/doml/workflows/execute-phase.md
  </files>
  <read_first>
    - /home/bill/source/DoML/.claude/doml/workflows/execute-phase.md — read FULL content after Plan 05-03 edits; find exact current stub text and the BU Step 5 section structure
    - /home/bill/source/DoML/.planning/phases/05-data-understanding/05-CONTEXT.md — Decision 3 (language-adaptive, affects report generation)
    - /home/bill/source/DoML/.planning/phases/04-business-understanding/04-03-SUMMARY.md — exact BU Step 5 pattern wording to mirror
    - /home/bill/source/DoML/CLAUDE.md — OUT-03 (caveats requirement)
  </read_first>
  <action>
Read execute-phase.md in full. Find the Step 5 section. It currently contains:
- BU-specific sub-steps (Step 5a through 5d) for `business_summary.html`
- A note: "Note for Phase 2 (Data Understanding): Step 5 for EDA will use `eda_report.html` as output. Implemented in DoML Phase 5."

Replace that note with the EDA-specific pipeline. The replacement content to insert after the BU Step 5d verification block:

```markdown
#### Phase 2 — Data Understanding HTML report

**Step 5e — Write EDA executive narrative**

Before converting to HTML, generate a 2–3 paragraph executive summary interpreting EDA findings for non-technical stakeholders.

Read the following to inform the narrative:
- `.planning/PROJECT.md` — business question, stakeholder, decision framing
- `.planning/config.json` — problem_type, time_factor
- The executed notebook `notebooks/02_data_understanding.ipynb` — cell outputs showing DuckDB profiling results, normality tests, correlation values, and tidy violations

The EDA narrative must:
- Avoid technical jargon (no "DuckDB", "Shapiro-Wilk", "nbformat", "ADF")
- State dataset size in plain language (e.g., "The dataset contains 12,000 observations and 15 features")
- Highlight top correlated features in business terms
- Note whether key numeric features are normally distributed or skewed — use plain language ("most values cluster near the middle" vs "most values are low with a long tail of high values")
- If time_factor is true: state in plain language whether the series appears stationary or trending
- Flag any data quality issues found (missing values, tidy violations) in plain language
- Be 2–3 short paragraphs

Write the narrative to a temporary Python script to avoid special-character quoting issues, then run it:

```python
# /tmp/doml_insert_eda_summary.py
import nbformat, uuid

NARRATIVE = """[WRITE YOUR 2-3 PARAGRAPH EDA EXECUTIVE SUMMARY HERE]"""

with open('notebooks/02_data_understanding.ipynb') as f:
    nb = nbformat.read(f, as_version=4)

summary_cell = nbformat.v4.new_markdown_cell(source='## Executive Summary\n\n' + NARRATIVE)
summary_cell['id'] = uuid.uuid4().hex[:8]
nb.cells.insert(0, summary_cell)

with open('notebooks/02_data_understanding.ipynb', 'w') as f:
    nbformat.write(nb, f)

print('EDA executive narrative inserted as cell 0')
```

Write the actual narrative into `NARRATIVE`, then run:
```bash
python3 /tmp/doml_insert_eda_summary.py
```

**Step 5f — Create reports/ directory**

```bash
mkdir -p reports
```

**Step 5g — Convert EDA notebook to HTML (code cells hidden)**

```bash
docker compose exec jupyter jupyter nbconvert \
  --to html \
  --no-input \
  notebooks/02_data_understanding.ipynb \
  --output-dir reports \
  --output eda_report
```

This produces `reports/eda_report.html`. The `--no-input` flag hides all code cells (OUT-02).

**If Docker is not running**, run on host as fallback:
```bash
jupyter nbconvert \
  --to html \
  --no-input \
  notebooks/02_data_understanding.ipynb \
  --output-dir reports \
  --output eda_report
```

**Step 5h — Verify EDA HTML report**

```bash
# Check 1: File exists
test -f reports/eda_report.html && echo "REPORT_EXISTS" || echo "REPORT_MISSING"

# Check 2: Code cells are hidden (OUT-02) — expected: 0 matches
grep -c 'class="input"' reports/eda_report.html && echo "CODE_VISIBLE_VIOLATION" || echo "CODE_HIDDEN_OK"

# Check 3: Caveats section present (OUT-03) — expected: >= 1 match
grep -ci "correlation is not causation" reports/eda_report.html && echo "CAVEATS_OK" || echo "CAVEATS_MISSING"

# Check 4: Executive summary present — expected: >= 1 match
grep -c "Executive Summary" reports/eda_report.html && echo "EXEC_SUMMARY_OK" || echo "EXEC_SUMMARY_MISSING"
```

If any check fails, report which check failed. Do not suppress failures.
```

Keep all other content in execute-phase.md unchanged (BU Steps 5a-5d, Step 6, Step 7).
  </action>
  <verify>
    <automated>grep -n "eda_report.html\|Step 5e\|Step 5f\|Step 5g\|Step 5h\|Note for Phase 2.*eda_report" /home/bill/source/DoML/.claude/doml/workflows/execute-phase.md</automated>
  </verify>
  <done>
    - `grep -c "eda_report.html" .claude/doml/workflows/execute-phase.md` returns >= 2 (in nbconvert command + in verification)
    - `grep -c "Step 5e\|Step 5f\|Step 5g\|Step 5h" .claude/doml/workflows/execute-phase.md` returns 4
    - `grep "Note for Phase 2.*eda_report" .claude/doml/workflows/execute-phase.md` returns 0 lines (stub removed)
    - `grep -c "no-input" .claude/doml/workflows/execute-phase.md` returns >= 2 (BU + EDA)
    - `grep -c "business_summary.html" .claude/doml/workflows/execute-phase.md` returns >= 1 (BU steps preserved)
    - Steps 6 and 7 preserved: `grep -c "Step 6\|Step 7" .claude/doml/workflows/execute-phase.md` returns 2
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end EDA HTML report verification</name>
  <action>Human verification: run the full EDA phase pipeline and confirm reports/eda_report.html is produced with code hidden, executive summary present, and caveats visible.</action>
  <what-built>
    Complete Data Understanding phase pipeline triggered by `/doml-execute-phase 2`:
    - Step 3d: Reads analysis_language from config.json, copies appropriate template to notebooks/
    - Step 3e: Executes notebook inside Docker (nbconvert --execute --inplace)
    - Step 3f: Verifies notebook has cell outputs
    - Step 5e: Claude generates and inserts EDA executive narrative as first Markdown cell
    - Step 5f: Creates reports/ directory
    - Step 5g: Runs nbconvert --to html --no-input → reports/eda_report.html
    - Step 5h: Verifies HTML exists, code hidden, caveats present, executive summary present
  </what-built>
  <how-to-verify>
    Prerequisites:
    - A DoML analysis project with /doml-new-project completed (PROJECT.md + config.json present)
    - data/raw/ contains at least one CSV, Parquet, or Excel file
    - Docker container is running: docker compose up -d
    - Phase 1 (Business Understanding) already executed (optional but recommended for continuity)

    Verification steps:
    1. Run /doml-execute-phase 2 in the analysis project directory
    2. Confirm the executor reads analysis_language from config.json and selects the correct template
    3. Confirm notebook execution succeeds (check for DuckDB profiling output, distribution plots, etc.)
    4. Open reports/eda_report.html in a browser:
       - First section should be "Executive Summary" with 2-3 plain-language paragraphs
       - No code cells visible (only Markdown narrative + DuckDB output tables + matplotlib/seaborn plots)
       - "DuckDB Data Profiling" section shows schema, null counts
       - "Distribution Analysis" section shows histograms and Q-Q plots
       - "Missing Value Analysis" section shows heatmap and bar chart (if dataset has nulls)
       - "Caveats" section at bottom includes "Correlation is not causation" statement
    5. Run the Step 5h checks:
       grep -c 'class="input"' reports/eda_report.html    # Expected: 0
       grep -ci "correlation is not causation" reports/eda_report.html  # Expected: >= 1

    If the Docker container is not available, test with host-side nbconvert (the workflow has a fallback path).
  </how-to-verify>
  <resume-signal>Type "approved" if the HTML report passes all checks, or describe what needs fixing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| execute-phase.md → Python subprocess | nbformat cell insertion runs locally; narrative is Claude-generated |
| execute-phase.md → filesystem | nbconvert writes HTML to reports/ — local output |
| notebook outputs → HTML | nbconvert renders matplotlib/seaborn plots as inline images — no external requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | EDA executive narrative injected as notebook cell | accept | Narrative is Claude-generated from local project files — no external injection |
| T-05-04-02 | Info Disclosure | HTML exposes schema, sample data, distribution charts | accept | Report is produced for stakeholder consumption — disclosure is the intended use |
| T-05-04-03 | Spoofing | Code cells visible in HTML (OUT-02 violation) | mitigate | Step 5h Check 2 verifies absence of 'class="input"' — failure reported, not silently ignored |
| T-05-04-04 | Tampering | eda_report.html already exists | accept | nbconvert overwrites — acceptable for re-runs; notebook overwrite is gated in Step 3d |
| T-05-04-05 | DoS | Very large number of matplotlib plots in EDA notebook | accept | EDA is run once per project; 10 min timeout in Step 3e covers worst-case plot rendering |
</threat_model>

<verification>
```bash
# EDA Step 5 sub-steps present
grep -n "Step 5e\|Step 5f\|Step 5g\|Step 5h" .claude/doml/workflows/execute-phase.md

# HTML report target present
grep -n "eda_report.html" .claude/doml/workflows/execute-phase.md

# Code-hidden flag present for EDA
grep -c "no-input" .claude/doml/workflows/execute-phase.md

# EDA executive narrative instructions present
grep -n "EDA executive narrative\|EDA.*executive\|executive.*EDA" .claude/doml/workflows/execute-phase.md

# OUT-03 caveats check present
grep -c "correlation is not causation" .claude/doml/workflows/execute-phase.md

# BU Steps 5a-5d and Steps 6-7 preserved
grep -c "Step 5a\|Step 5b\|Step 5c\|Step 5d" .claude/doml/workflows/execute-phase.md
grep -c "Step 6\|Step 7" .claude/doml/workflows/execute-phase.md

# EDA stub removed
grep "Note for Phase 2.*eda_report" .claude/doml/workflows/execute-phase.md && echo "STUB_PRESENT" || echo "STUB_REMOVED"
```
</verification>

<success_criteria>
1. `grep -c "eda_report.html" .claude/doml/workflows/execute-phase.md` returns >= 2
2. `grep -c "Step 5e\|Step 5f\|Step 5g\|Step 5h" .claude/doml/workflows/execute-phase.md` returns 4
3. `grep "Note for Phase 2.*eda_report" .claude/doml/workflows/execute-phase.md` returns 0 lines (stub removed)
4. `grep -c "no-input" .claude/doml/workflows/execute-phase.md` returns >= 2 (both BU and EDA use --no-input)
5. `grep -c "correlation is not causation" .claude/doml/workflows/execute-phase.md` returns >= 1 (OUT-03 verification)
6. BU Steps 5a-5d preserved: `grep -c "Step 5a\|Step 5b\|Step 5c\|Step 5d" .claude/doml/workflows/execute-phase.md` returns 4
7. Steps 6 and 7 preserved: `grep -c "Step 6\|Step 7" .claude/doml/workflows/execute-phase.md` returns 2
8. EDA narrative instructions specify reading notebook outputs and translating to non-technical language (EDA-10)
9. Human checkpoint (Task 2): HTML report passes all 4 checks (file exists, code hidden, caveats present, exec summary present)
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-understanding/05-04-SUMMARY.md` following the summary template at `/home/bill/source/DoML/.claude/get-shit-done/templates/summary.md`.

Key items to document:
- The exact nbconvert command used (flags, --output-dir vs --output, output filename)
- EDA narrative generation instructions (what sources Claude should read, what to include)
- Result of Step 5h verification checks from the UAT run (exact grep counts)
- Whether the HTML rendered correctly in browser (plots visible, code hidden, caveats at bottom)
- Any deviations from the plan (e.g., different nbconvert flag behaviour for R notebooks vs Python)
- UAT sign-off outcome from Task 2
</output>
