---
phase: 06-data-modelling-regression-classification
plan: 05
type: execute
wave: 3
depends_on:
  - "06-04"
files_modified:
  - .claude/skills/doml-iterate-model/SKILL.md
  - .claude/doml/workflows/iterate-model.md
autonomous: true
requirements:
  - CMD-03

must_haves:
  truths:
    - ".claude/skills/doml-iterate-model/SKILL.md exists"
    - "SKILL.md has a TRIGGER section describing when /doml-iterate-model is invoked"
    - "SKILL.md has an objective section describing what the command does"
    - "SKILL.md references .claude/doml/workflows/iterate-model.md"
    - ".claude/doml/workflows/iterate-model.md exists"
    - "iterate-model.md documents that it is a stub — full implementation in Phase 6b planning"
    - "iterate-model.md documents the planned workflow: read prior notebook + interpretation → generate next version → run CV + leaderboard + SHAP + Optuna → append to leaderboard.csv"
    - "iterate-model.md documents the direction argument syntax: /doml-iterate-model 'focus on XGBoost with custom features'"
    - "iterate-model.md documents the version resolution pattern (glob for existing v* notebooks)"
    - "grep 'doml-iterate-model' .claude/skills/doml-iterate-model/SKILL.md returns at least one match"
    - "grep 'iterate-model.md' .claude/skills/doml-iterate-model/SKILL.md returns at least one match"
  artifacts:
    - path: ".claude/skills/doml-iterate-model/SKILL.md"
      provides: "/doml-iterate-model command stub — invocable in Claude Code; routes to iterate-model.md"
      contains: "doml-iterate-model"
    - path: ".claude/doml/workflows/iterate-model.md"
      provides: "iterate-model workflow stub — documents planned behavior; implementation deferred to Phase 6b"
      contains: "direction"
  key_links:
    - from: ".claude/skills/doml-iterate-model/SKILL.md"
      to: ".claude/doml/workflows/iterate-model.md"
      via: "@/home/bill/source/DoML/.claude/doml/workflows/iterate-model.md"
      pattern: "iterate-model.md"
---

<objective>
Create the `/doml-iterate-model` command stub: a SKILL.md entry point and a workflow stub that documents the planned behavior for iterative modelling (Decision 8).

The command is invokable but not fully implemented at Phase 6 planning time. The stub clearly documents:
- What the command is supposed to do
- The input it accepts (optional direction string)
- The planned workflow (read prior notebook + interpretation → generate v{N+1} → run pipeline)
- That full implementation is deferred to Phase 6b planning

This mirrors the pattern used by other DoML skills (e.g., `doml-execute-phase` references `execute-phase.md`).

Decision 8 reference: "Plan 06-0x includes a stub for the `/doml-iterate-model` skill and workflow. Full implementation deferred to Phase 6b planning."
</objective>

<execution_context>
@/home/bill/source/DoML/.claude/doml/workflows/execute-phase.md
</execution_context>

<context>
@/home/bill/source/DoML/.planning/PROJECT.md
@/home/bill/source/DoML/.planning/phases/06-data-modelling-regression-classification/06-CONTEXT.md
@/home/bill/source/DoML/CLAUDE.md

<interfaces>
Existing skill to mirror for structure:
  .claude/skills/doml-execute-phase/SKILL.md  — read for SKILL.md format conventions
  .claude/skills/doml-plan-phase/SKILL.md     — read for another example

Workflow stub location:
  .claude/doml/workflows/iterate-model.md     — new file; stub only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /doml-iterate-model SKILL.md</name>
  <files>
    .claude/skills/doml-iterate-model/SKILL.md
  </files>
  <read_first>
    - /home/bill/source/DoML/.claude/skills/doml-execute-phase/SKILL.md — read full file; use as structural template for SKILL.md format
    - /home/bill/source/DoML/.claude/skills/doml-plan-phase/SKILL.md — read for second example of SKILL.md format
    - /home/bill/source/DoML/.planning/phases/06-data-modelling-regression-classification/06-CONTEXT.md — Decision 8 (full iterate-model spec)
  </read_first>
  <action>
Create `.claude/skills/doml-iterate-model/SKILL.md` following the existing DoML skill format. Model it on `doml-execute-phase/SKILL.md`.

The SKILL.md should include:
- **TRIGGER** section: when a user runs `/doml-iterate-model` (optionally with a direction string in quotes)
- **Objective** section: what the command does — generates a new modelling iteration notebook incorporating analyst direction, runs the full pipeline (CV + leaderboard + SHAP + Optuna), appends results to `models/leaderboard.csv`
- **Arguments** section:
  - Optional: direction string — e.g., `"focus on XGBoost with custom feature engineering"` or `"try Lasso with polynomial features"`
  - If no direction given: defaults to re-running the same pipeline on the same problem type
- **Process** section: reference to `@.claude/doml/workflows/iterate-model.md`
- **Status note**: `⚠️ This command is a stub. Full implementation is scheduled for Phase 6b planning. Currently documents planned behavior.`

Example invocations:
```
/doml-iterate-model
/doml-iterate-model "focus on XGBoost with deeper hyperparameter tuning"
/doml-iterate-model "try Lasso and Ridge with polynomial features for linear baseline"
```
  </action>
  <verify>
    <automated>cat /home/bill/source/DoML/.claude/skills/doml-iterate-model/SKILL.md | grep -c "doml-iterate-model\|iterate-model.md\|direction"</automated>
  </verify>
  <done>
    - File exists: `test -f .claude/skills/doml-iterate-model/SKILL.md`
    - `grep -c "doml-iterate-model" .claude/skills/doml-iterate-model/SKILL.md` returns >= 1
    - `grep -c "iterate-model.md" .claude/skills/doml-iterate-model/SKILL.md` returns >= 1
    - `grep -c "direction" .claude/skills/doml-iterate-model/SKILL.md` returns >= 1
    - `grep -ci "stub\|not yet implemented\|deferred" .claude/skills/doml-iterate-model/SKILL.md` returns >= 1
  </done>
</task>

<task type="auto">
  <name>Task 2: Create iterate-model.md workflow stub</name>
  <files>
    .claude/doml/workflows/iterate-model.md
  </files>
  <read_first>
    - /home/bill/source/DoML/.claude/doml/workflows/execute-phase.md — read for workflow file format conventions (headings, Purpose, Invoked by, Implementation Status, Workflow steps)
    - /home/bill/source/DoML/.planning/phases/06-data-modelling-regression-classification/06-CONTEXT.md — Decision 8 (full planned behavior of /doml-iterate-model)
    - /home/bill/source/DoML/.planning/phases/06-data-modelling-regression-classification/06-02-SUMMARY.md — leaderboard schema (column names) for accurate stub documentation
  </read_first>
  <action>
Create `.claude/doml/workflows/iterate-model.md` as a workflow stub.

Follow the format of `execute-phase.md` (Purpose, Invoked by, Implementation Status table, then stub workflow).

The stub must document:

**Purpose:** Generate a new modelling iteration notebook incorporating analyst-supplied direction, run the full CV + leaderboard + SHAP + Optuna pipeline, append results to `models/leaderboard.csv` for cross-iteration comparison.

**Invoked by:** `/doml-iterate-model [optional direction string]`

**Implementation Status:**

```
| Step | Implemented | Phase |
|------|-------------|-------|
| Read prior notebook + interpretation cell | Stub | Phase 6b |
| Generate next version notebook | Stub | Phase 6b |
| Run CV + leaderboard + SHAP + Optuna | Stub | Phase 6b |
| Append results to leaderboard.csv | Stub | Phase 6b |
| Write new interpretation cell | Stub | Phase 6b |
```

**Planned Workflow (stub):**

```
## ⚠️ Stub — Full Implementation Deferred to Phase 6b Planning

This workflow documents planned behavior. The command is registered but not yet executable.

### Planned Step 1 — Identify prior notebook
Find the most recent executed modelling notebook in notebooks/:
  notebooks/0{N}_modelling_{problem_type}_v{M}.ipynb  (highest M)
Read the final Markdown cell (Claude interpretation from execute-phase.md Step 3m).

### Planned Step 2 — Resolve next version number
Increment M → M+1. New notebook path:
  notebooks/0{N}_modelling_{problem_type}_v{M+1}.ipynb

### Planned Step 3 — Generate modified notebook
Copy the template for the problem_type (regression or classification).
Incorporate the direction argument:
- If direction contains specific model names → focus those models in the models dict
- If direction mentions feature engineering → add cells before the CV loop for the specified engineering
- If direction mentions hyperparameter focus → widen the Optuna search space for that model

### Planned Step 4 — Execute the new iteration notebook
Same execution pattern as execute-phase.md Step 3k:
  docker compose exec jupyter jupyter nbconvert --execute --to notebook --inplace [new_notebook]
  Timeout: 1200s

### Planned Step 5 — Append results to leaderboard.csv
The modelling notebook already handles append-only leaderboard writes.
After execution, display a leaderboard summary showing all iterations:
  All models across all version notebooks, sorted by primary metric.

### Planned Step 6 — Write Claude interpretation
Same as execute-phase.md Step 3m — append an interpretation cell to the new notebook.
Compare v{M+1} results to v{M} and note whether the direction improved performance.

### Planned Step 7 — Display summary
Show:
  Previous best (v{M}):  {model_name}  CV {metric}={score}
  New best (v{M+1}):     {model_name}  CV {metric}={score}
  Delta: {+/-X.XXXX}
  Full leaderboard: models/leaderboard.csv ({N} total entries)
```

Include a note at the bottom:
```
## Implementation Note

Full implementation requires:
1. Parsing direction string to identify model/feature/hyperparameter focus
2. Dynamic notebook cell modification (insert feature engineering cells, modify models dict)
3. Cross-iteration leaderboard summary display

Planned for Phase 6b. Until then, analysts can manually:
1. Copy notebooks/04_modelling_{type}_v{N}.ipynb to v{N+1}
2. Edit the models dict or add cells manually
3. Re-run the notebook in JupyterLab
```
  </action>
  <verify>
    <automated>cat /home/bill/source/DoML/.claude/doml/workflows/iterate-model.md | grep -c "direction\|version\|leaderboard\|Stub"</automated>
  </verify>
  <done>
    - File exists: `test -f .claude/doml/workflows/iterate-model.md`
    - `grep -c "direction" .claude/doml/workflows/iterate-model.md` returns >= 1
    - `grep -c "version\|v{M}" .claude/doml/workflows/iterate-model.md` returns >= 1
    - `grep -c "leaderboard" .claude/doml/workflows/iterate-model.md` returns >= 1
    - `grep -c "Stub\|stub\|deferred" .claude/doml/workflows/iterate-model.md` returns >= 1
    - `grep -c "Step" .claude/doml/workflows/iterate-model.md` returns >= 5
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md → iterate-model.md | Internal skill routing — no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Spoofing | Stub invoked as real command | accept | Stub clearly documents "not yet executable" — user sees immediate status message |
</threat_model>

<verification>
```bash
# SKILL.md exists and has required content
test -f .claude/skills/doml-iterate-model/SKILL.md && echo "SKILL_EXISTS" || echo "SKILL_MISSING"
grep -c "doml-iterate-model" .claude/skills/doml-iterate-model/SKILL.md
grep -c "iterate-model.md" .claude/skills/doml-iterate-model/SKILL.md
grep -ci "stub\|not yet\|deferred" .claude/skills/doml-iterate-model/SKILL.md

# Workflow stub exists and has required content
test -f .claude/doml/workflows/iterate-model.md && echo "WORKFLOW_EXISTS" || echo "WORKFLOW_MISSING"
grep -c "direction" .claude/doml/workflows/iterate-model.md
grep -c "leaderboard" .claude/doml/workflows/iterate-model.md
grep -c "Step" .claude/doml/workflows/iterate-model.md
```
</verification>

<success_criteria>
1. `.claude/skills/doml-iterate-model/SKILL.md` exists with TRIGGER, objective, arguments, and process sections
2. SKILL.md references `iterate-model.md` for implementation
3. SKILL.md clearly marks command as stub / deferred
4. `.claude/doml/workflows/iterate-model.md` exists with Implementation Status table
5. iterate-model.md documents all 7 planned workflow steps
6. iterate-model.md documents the direction argument parsing
7. iterate-model.md documents the manual workaround for analysts until implementation
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-modelling-regression-classification/06-05-SUMMARY.md` documenting:
- Exact file paths created
- SKILL.md format used (sections present)
- iterate-model.md stub structure (planned steps documented)
- What is deferred to Phase 6b (e.g., direction string parsing, dynamic cell modification)
- Confirmation that the skill is registered (file exists, Claude Code will discover it)
</output>
