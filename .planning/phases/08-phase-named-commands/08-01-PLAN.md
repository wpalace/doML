---
phase: 08-phase-named-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/doml-business-understanding/SKILL.md
  - .claude/doml/workflows/business-understanding.md
autonomous: true
requirements:
  - CMD-10
must_haves:
  truths:
    - "doml-business-understanding SKILL.md exists and is invokable"
    - "business-understanding.md workflow is self-contained (no dependency on execute-phase.md)"
    - "Workflow supports optional --guidance parameter passed to interpretation cell"
    - "Workflow contains all steps from execute-phase.md Steps 3a–3f and 5a–5d"
    - "Error messages reference /doml-business-understanding not /doml-execute-phase"
  artifacts:
    - path: ".claude/skills/doml-business-understanding/SKILL.md"
      provides: "doml-business-understanding skill entry point"
    - path: ".claude/doml/workflows/business-understanding.md"
      provides: "Self-contained BU phase workflow"
---

<objective>
Create the `doml-business-understanding` skill and its standalone workflow file.

This skill replaces `doml-execute-phase 1`. It runs the full Business Understanding phase:
copy notebook template → (optional headerless CSV check) → execute notebook in Docker →
write executive narrative → generate HTML report.

The workflow is extracted from `execute-phase.md` Steps 3a–3f and 5a–5d, with all
`/doml-execute-phase` references updated to `/doml-business-understanding`.
</objective>

<context>
@/home/bill/source/DoML/.planning/phases/08-phase-named-commands/08-CONTEXT.md
@/home/bill/source/DoML/.planning/ROADMAP.md
@/home/bill/source/DoML/CLAUDE.md

<interfaces>
<!-- Source: execute-phase.md Steps 3a–3f (BU executor) + Steps 5a–5d (BU HTML report) -->
<!-- All "run /doml-execute-phase 1 again" messages → "run /doml-business-understanding again" -->
<!-- --guidance parameter: if provided, append as a context hint to the interpretation section -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create doml-business-understanding SKILL.md</name>
  <files>.claude/skills/doml-business-understanding/SKILL.md</files>
  <read_first>
    - .claude/skills/doml-execute-phase/SKILL.md (pattern to follow)
    - .planning/phases/08-phase-named-commands/08-CONTEXT.md
  </read_first>
  <action>
Create `.claude/skills/doml-business-understanding/SKILL.md`:

```markdown
---
name: doml-business-understanding
description: "Run the Business Understanding phase end-to-end: generates notebooks/01_business_understanding.ipynb and reports/business_summary.html. Requires /doml-new-project to have been run first and Docker to be running."
argument-hint: "[--guidance \"analyst direction\"]"
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - AskUserQuestion
---

<objective>
Run the DoML Business Understanding phase end-to-end:
1. Copy the BU notebook template to notebooks/01_business_understanding.ipynb
2. Execute the notebook inside Docker
3. Write an executive narrative as the first cell
4. Generate reports/business_summary.html with code hidden

Optional: pass --guidance "your direction" to provide analyst context that shapes
the interpretation cell written after execution.
</objective>

<execution_context>
@.claude/doml/workflows/business-understanding.md
</execution_context>

<context>
Guidance (optional): $ARGUMENTS
</context>

<process>
Execute the business-understanding workflow from @.claude/doml/workflows/business-understanding.md end-to-end.
</process>
```
  </action>
  <acceptance_criteria>
    - File exists at `.claude/skills/doml-business-understanding/SKILL.md`
    - Contains `name: doml-business-understanding`
    - References `@.claude/doml/workflows/business-understanding.md`
    - Contains `--guidance` in argument-hint
  </acceptance_criteria>
  <verify>
    <automated>grep -c "doml-business-understanding" .claude/skills/doml-business-understanding/SKILL.md</automated>
  </verify>
  <done>SKILL.md created with correct name, workflow reference, and --guidance argument hint.</done>
</task>

<task type="auto">
  <name>Task 2: Create business-understanding.md workflow</name>
  <files>.claude/doml/workflows/business-understanding.md</files>
  <read_first>
    - .claude/doml/workflows/execute-phase.md (Steps 3a–3f and 5a–5d to extract)
    - .planning/phases/08-phase-named-commands/08-CONTEXT.md
    - CLAUDE.md
  </read_first>
  <action>
Create `.claude/doml/workflows/business-understanding.md` as a self-contained workflow.

Extract the Business Understanding executor from `execute-phase.md` (Steps 3a–3f and 5a–5d).
The workflow must be fully self-contained — no reference to or dependency on `execute-phase.md`.

Structure:

```markdown
# DoML Business Understanding Workflow

## Purpose
Run the Business Understanding phase end-to-end: copy notebook template, execute in Docker,
write executive narrative, generate HTML report.

## Invoked by: /doml-business-understanding [--guidance "..."]

---

## Reproducibility Constraints (Non-Negotiable)
[Same as execute-phase.md — REPR-01, REPR-02, REPR-03]

---

## Workflow

### Step 1 — Validate project state
[Read .planning/STATE.md. If missing, display error and stop.]

### Step 2 — Read project config
[Read .planning/config.json and .planning/PROJECT.md. If either missing, display error and stop.]
[Read optional --guidance from $ARGUMENTS. If present, save to GUIDANCE variable for use in Step 6.]

### Step 3 — Copy notebook template
[Steps 3a + 3a.1 from execute-phase.md verbatim, with error messages updated to /doml-business-understanding]

### Step 4 — Execute notebook in Docker
[Step 3b from execute-phase.md verbatim, with error messages updated to /doml-business-understanding]

### Step 5 — Verify notebook output
[Step 3c from execute-phase.md verbatim]

### Step 6 — Write executive narrative
[Step 5a from execute-phase.md verbatim]
[Add: if GUIDANCE was provided, append it as a context hint to the interpretation:
 "Analyst guidance: {GUIDANCE}"]

### Step 7 — Generate HTML report
[Steps 5b–5d from execute-phase.md verbatim, with error messages updated to /doml-business-understanding]

### Step 8 — Update STATE.md
Write to `.planning/STATE.md`:
- Update `current_focus` to "Phase 2 — Data Understanding (next)"
- Append decision: "[Phase 08]: doml-business-understanding completed — notebook + HTML report generated"

### Step 9 — Confirm
Display:
```
Business Understanding complete.

  Notebook: notebooks/01_business_understanding.ipynb
  Report:   reports/business_summary.html

Next step: /doml-data-understanding
```
```

Copy all step logic from execute-phase.md exactly — do not paraphrase. Only change:
1. All occurrences of `/doml-execute-phase 1` → `/doml-business-understanding`
2. All occurrences of "run /doml-execute-phase 1 again" → "run /doml-business-understanding again"
3. Add guidance handling in Step 6 as described above
  </action>
  <acceptance_criteria>
    - File exists at `.claude/doml/workflows/business-understanding.md`
    - Does NOT contain any reference to `execute-phase.md` as a dependency
    - Contains `doml-business-understanding` in all user-facing messages
    - Contains guidance handling (`GUIDANCE` / `--guidance`)
    - Contains all step markers: Step 1 through Step 9
    - Contains headerless CSV check logic (from Step 3a.1)
    - Contains nbconvert HTML generation step
    - Contains STATE.md update step
  </acceptance_criteria>
  <verify>
    <automated>grep -c "doml-business-understanding" .claude/doml/workflows/business-understanding.md</automated>
  </verify>
  <done>business-understanding.md is a self-contained workflow with all 9 steps, guidance support, and no dependency on execute-phase.md.</done>
</task>

</tasks>

<verification>
1. File exists: `.claude/skills/doml-business-understanding/SKILL.md`
2. File exists: `.claude/doml/workflows/business-understanding.md`
3. No execute-phase.md dependency: `grep -c "execute-phase" .claude/doml/workflows/business-understanding.md` → 0
4. Skill references correct workflow: `grep -c "business-understanding.md" .claude/skills/doml-business-understanding/SKILL.md` → 1
5. Guidance supported: `grep -c "guidance\|GUIDANCE" .claude/doml/workflows/business-understanding.md` → nonzero
</verification>

<success_criteria>
- `doml-business-understanding` skill exists and is invokable
- `business-understanding.md` is fully self-contained with all BU executor logic
- Optional `--guidance` parameter supported
- All user-facing messages reference `/doml-business-understanding` not `/doml-execute-phase`
- STATE.md update step included
</success_criteria>

<output>
After completion, create `.planning/phases/08-phase-named-commands/08-01-SUMMARY.md`
</output>
