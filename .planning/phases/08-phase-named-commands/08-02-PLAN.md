---
phase: 08-phase-named-commands
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/doml-data-understanding/SKILL.md
  - .claude/doml/workflows/data-understanding.md
autonomous: true
requirements:
  - CMD-11
must_haves:
  truths:
    - "doml-data-understanding SKILL.md exists and is invokable"
    - "data-understanding.md workflow is self-contained (no dependency on execute-phase.md)"
    - "Workflow supports optional --guidance parameter"
    - "Workflow contains all steps from execute-phase.md Steps 3d–3f and 5e–5h"
    - "Error messages reference /doml-data-understanding not /doml-execute-phase"
  artifacts:
    - path: ".claude/skills/doml-data-understanding/SKILL.md"
      provides: "doml-data-understanding skill entry point"
    - path: ".claude/doml/workflows/data-understanding.md"
      provides: "Self-contained EDA phase workflow"
---

<objective>
Create the `doml-data-understanding` skill and its standalone workflow file.

This skill replaces `doml-execute-phase 2`. It runs the full Data Understanding (EDA) phase:
language detection → copy notebook template → headerless CSV check → execute notebook in Docker →
write EDA executive narrative → generate eda_report.html.

The workflow is extracted from `execute-phase.md` Steps 3d–3f and 5e–5h, with all
`/doml-execute-phase` references updated to `/doml-data-understanding`.
</objective>

<context>
@/home/bill/source/DoML/.planning/phases/08-phase-named-commands/08-CONTEXT.md
@/home/bill/source/DoML/.planning/ROADMAP.md
@/home/bill/source/DoML/CLAUDE.md

<interfaces>
<!-- Source: execute-phase.md Steps 3d.0, 3d, 3e, 3f, 5e, 5f, 5g, 5h (EDA executor) -->
<!-- All "run /doml-execute-phase 2 again" messages → "run /doml-data-understanding again" -->
<!-- --guidance parameter: if provided, append as context hint to the EDA interpretation cell -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create doml-data-understanding SKILL.md</name>
  <files>.claude/skills/doml-data-understanding/SKILL.md</files>
  <read_first>
    - .claude/skills/doml-execute-phase/SKILL.md (pattern to follow)
    - .planning/phases/08-phase-named-commands/08-CONTEXT.md
  </read_first>
  <action>
Create `.claude/skills/doml-data-understanding/SKILL.md`:

```markdown
---
name: doml-data-understanding
description: "Run the Data Understanding (EDA) phase end-to-end: generates notebooks/02_data_understanding.ipynb and reports/eda_report.html. Requires /doml-business-understanding to have been run first and Docker to be running."
argument-hint: "[--guidance \"analyst direction\"]"
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - AskUserQuestion
---

<objective>
Run the DoML Data Understanding (EDA) phase end-to-end:
1. Detect language (Python/R) from config.json
2. Copy the appropriate EDA notebook template
3. Check for headerless CSVs and prompt for column names if needed
4. Execute the notebook inside Docker
5. Write an EDA executive narrative as the first cell
6. Generate reports/eda_report.html with code hidden

Optional: pass --guidance "your direction" to provide analyst context that shapes
the EDA interpretation cell written after execution.
</objective>

<execution_context>
@.claude/doml/workflows/data-understanding.md
</execution_context>

<context>
Guidance (optional): $ARGUMENTS
</context>

<process>
Execute the data-understanding workflow from @.claude/doml/workflows/data-understanding.md end-to-end.
</process>
```
  </action>
  <acceptance_criteria>
    - File exists at `.claude/skills/doml-data-understanding/SKILL.md`
    - Contains `name: doml-data-understanding`
    - References `@.claude/doml/workflows/data-understanding.md`
    - Contains `--guidance` in argument-hint
  </acceptance_criteria>
  <verify>
    <automated>grep -c "doml-data-understanding" .claude/skills/doml-data-understanding/SKILL.md</automated>
  </verify>
  <done>SKILL.md created with correct name, workflow reference, and --guidance argument hint.</done>
</task>

<task type="auto">
  <name>Task 2: Create data-understanding.md workflow</name>
  <files>.claude/doml/workflows/data-understanding.md</files>
  <read_first>
    - .claude/doml/workflows/execute-phase.md (Steps 3d.0, 3d, 3e, 3f, 5e, 5f, 5g, 5h to extract)
    - .planning/phases/08-phase-named-commands/08-CONTEXT.md
    - CLAUDE.md
  </read_first>
  <action>
Create `.claude/doml/workflows/data-understanding.md` as a self-contained workflow.

Extract the Data Understanding executor from `execute-phase.md` (Steps 3d.0, 3d, 3e, 3f
and 5e, 5f, 5g, 5h). The workflow must be fully self-contained — no reference to or
dependency on `execute-phase.md`.

Structure:

```markdown
# DoML Data Understanding Workflow

## Purpose
Run the Data Understanding (EDA) phase end-to-end: language detection, notebook copy,
headerless CSV check, Docker execution, executive narrative, HTML report generation.

## Invoked by: /doml-data-understanding [--guidance "..."]

---

## Reproducibility Constraints (Non-Negotiable)
[Same as execute-phase.md — REPR-01, REPR-02, REPR-03]

---

## Workflow

### Step 1 — Validate project state
[Read .planning/STATE.md. If missing, display error and stop.]

### Step 2 — Read project config
[Read .planning/config.json. If missing, display error and stop.]
[Read optional --guidance from $ARGUMENTS. Save to GUIDANCE variable.]

### Step 3 — Check for headerless CSVs
[Step 3d.0 from execute-phase.md verbatim, with error messages updated to /doml-data-understanding]

### Step 4 — Detect language and copy notebook template
[Step 3d from execute-phase.md verbatim, with error messages updated to /doml-data-understanding]

### Step 5 — Execute notebook in Docker
[Step 3e from execute-phase.md verbatim, with error messages updated to /doml-data-understanding]

### Step 6 — Verify notebook output
[Step 3f from execute-phase.md verbatim]

### Step 7 — Write EDA executive narrative
[Step 5e from execute-phase.md verbatim]
[Add: if GUIDANCE was provided, append it as a context hint to the interpretation:
 "Analyst guidance: {GUIDANCE}"]

### Step 8 — Generate HTML report
[Steps 5f–5h from execute-phase.md verbatim, with error messages updated to /doml-data-understanding]

### Step 9 — Update STATE.md
Write to `.planning/STATE.md`:
- Update `current_focus` to "Phase 3 — Modelling (next)"
- Append decision: "[Phase 08]: doml-data-understanding completed — notebook + HTML report generated"

### Step 10 — Confirm
Display:
```
Data Understanding complete.

  Notebook: notebooks/02_data_understanding.ipynb
  Report:   reports/eda_report.html

Next step: /doml-modelling
```
```

Copy all step logic from execute-phase.md exactly — do not paraphrase. Only change:
1. All occurrences of `/doml-execute-phase 2` → `/doml-data-understanding`
2. All occurrences of "run /doml-execute-phase 2 again" → "run /doml-data-understanding again"
3. Add guidance handling in Step 7 as described above
  </action>
  <acceptance_criteria>
    - File exists at `.claude/doml/workflows/data-understanding.md`
    - Does NOT contain any reference to `execute-phase.md` as a dependency
    - Contains `doml-data-understanding` in all user-facing messages
    - Contains guidance handling (`GUIDANCE` / `--guidance`)
    - Contains headerless CSV check step (from Step 3d.0)
    - Contains language detection step (Python vs R)
    - Contains nbconvert HTML generation step
    - Contains STATE.md update step
    - Contains all steps 1–10
  </acceptance_criteria>
  <verify>
    <automated>grep -c "doml-data-understanding" .claude/doml/workflows/data-understanding.md</automated>
  </verify>
  <done>data-understanding.md is a self-contained workflow with all 10 steps, guidance support, and no dependency on execute-phase.md.</done>
</task>

</tasks>

<verification>
1. File exists: `.claude/skills/doml-data-understanding/SKILL.md`
2. File exists: `.claude/doml/workflows/data-understanding.md`
3. No execute-phase.md dependency: `grep -c "execute-phase" .claude/doml/workflows/data-understanding.md` → 0
4. Skill references correct workflow: `grep -c "data-understanding.md" .claude/skills/doml-data-understanding/SKILL.md` → 1
5. Guidance supported: `grep -c "guidance\|GUIDANCE" .claude/doml/workflows/data-understanding.md` → nonzero
</verification>

<success_criteria>
- `doml-data-understanding` skill exists and is invokable
- `data-understanding.md` is fully self-contained with all EDA executor logic
- Optional `--guidance` parameter supported
- All user-facing messages reference `/doml-data-understanding` not `/doml-execute-phase`
- Headerless CSV detection, language detection, HTML generation, STATE.md update all present
</success_criteria>

<output>
After completion, create `.planning/phases/08-phase-named-commands/08-02-SUMMARY.md`
</output>
