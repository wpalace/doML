---
phase: 08-phase-named-commands
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/doml-modelling/SKILL.md
  - .claude/doml/workflows/modelling.md
autonomous: true
requirements:
  - CMD-12
must_haves:
  truths:
    - "doml-modelling SKILL.md exists and is invokable"
    - "modelling.md workflow is self-contained (no dependency on execute-phase.md)"
    - "Workflow supports optional --guidance parameter"
    - "Workflow routes regression/classification to supervised path (preprocessing + modelling + HTML report)"
    - "Workflow routes clustering/dim_reduction to unsupervised path (modelling only, no HTML report)"
    - "Error messages reference /doml-modelling not /doml-execute-phase"
  artifacts:
    - path: ".claude/skills/doml-modelling/SKILL.md"
      provides: "doml-modelling skill entry point"
    - path: ".claude/doml/workflows/modelling.md"
      provides: "Self-contained modelling workflow for all four problem types"
  key_links:
    - from: "doml-modelling supervised path"
      to: "notebooks/03_preprocessing.ipynb + notebooks/04_modelling_{type}_v{N}.ipynb"
      via: "execute-phase.md Steps 3h–3m"
      pattern: "preprocessing.ipynb"
    - from: "doml-modelling unsupervised path"
      to: "notebooks/0{N}_modelling_{type}_v{M}.ipynb"
      via: "execute-phase.md Steps 3n–3t"
      pattern: "modelling_clustering\|modelling_dimreduction"
---

<objective>
Create the `doml-modelling` skill and its standalone workflow file.

This skill replaces both `doml-execute-phase 3` (supervised) and the Phase 7 executor
(unsupervised). It reads `config.json` `problem_type` and routes accordingly:

- regression / classification → preprocessing notebook + modelling notebook + model_report.html
- clustering / dimensionality_reduction → unsupervised modelling notebook only (no HTML report)

The workflow extracts Steps 3g–3m + 5i–5l (supervised path) and Steps 3n–3t (unsupervised
path) from execute-phase.md, unified under a single routing step.
</objective>

<context>
@/home/bill/source/DoML/.planning/phases/08-phase-named-commands/08-CONTEXT.md
@/home/bill/source/DoML/.planning/ROADMAP.md
@/home/bill/source/DoML/CLAUDE.md

<interfaces>
<!-- Supervised path: execute-phase.md Steps 3h–3m (preprocessing + modelling) + 5i–5l (HTML report) -->
<!-- Unsupervised path: execute-phase.md Steps 3n–3t (clustering / dim_reduction) -->
<!-- Routing: Step 3g (problem_type read) — split rather than redirect to Phase 7 -->
<!-- --guidance: if provided, append to interpretation cells in both paths -->
<!-- time_series: not yet supported — display message directing to /doml-forecasting (future phase) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create doml-modelling SKILL.md</name>
  <files>.claude/skills/doml-modelling/SKILL.md</files>
  <read_first>
    - .claude/skills/doml-execute-phase/SKILL.md (pattern to follow)
    - .planning/phases/08-phase-named-commands/08-CONTEXT.md
  </read_first>
  <action>
Create `.claude/skills/doml-modelling/SKILL.md`:

```markdown
---
name: doml-modelling
description: "Run the Modelling phase end-to-end for any problem type. Reads config.json problem_type and routes to the correct notebook: regression/classification (preprocessing + modelling + HTML report), clustering/dim_reduction (unsupervised modelling only). Requires /doml-data-understanding to have been run first and Docker to be running."
argument-hint: "[--guidance \"analyst direction\"]"
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - AskUserQuestion
---

<objective>
Run the DoML Modelling phase end-to-end for any problem type:

Supervised (regression / classification):
1. Copy and execute preprocessing notebook (notebooks/03_preprocessing.ipynb)
2. Copy and execute modelling notebook (notebooks/04_modelling_{type}_v{N}.ipynb)
3. Write interpretation cell and executive narrative
4. Generate reports/model_report.html

Unsupervised (clustering / dimensionality_reduction):
1. Copy and execute unsupervised modelling notebook
2. Write interpretation cell
(HTML report generated by a later dedicated reporting command)

Optional: pass --guidance "your direction" to provide analyst context that shapes
the interpretation cells written after execution.
</objective>

<execution_context>
@.claude/doml/workflows/modelling.md
</execution_context>

<context>
Guidance (optional): $ARGUMENTS
</context>

<process>
Execute the modelling workflow from @.claude/doml/workflows/modelling.md end-to-end.
</process>
```
  </action>
  <acceptance_criteria>
    - File exists at `.claude/skills/doml-modelling/SKILL.md`
    - Contains `name: doml-modelling`
    - References `@.claude/doml/workflows/modelling.md`
    - Contains `--guidance` in argument-hint
  </acceptance_criteria>
  <verify>
    <automated>grep -c "doml-modelling" .claude/skills/doml-modelling/SKILL.md</automated>
  </verify>
  <done>SKILL.md created with correct name, workflow reference, and --guidance argument hint.</done>
</task>

<task type="auto">
  <name>Task 2: Create modelling.md workflow</name>
  <files>.claude/doml/workflows/modelling.md</files>
  <read_first>
    - .claude/doml/workflows/execute-phase.md (Steps 3g–3m, 3n–3t, 5i–5l to extract)
    - .planning/phases/08-phase-named-commands/08-CONTEXT.md
    - CLAUDE.md
  </read_first>
  <action>
Create `.claude/doml/workflows/modelling.md` as a self-contained workflow.

The workflow must be fully self-contained — no reference to or dependency on `execute-phase.md`.

Structure:

```markdown
# DoML Modelling Workflow

## Purpose
Run the Modelling phase end-to-end for any problem type. Reads config.json to determine
the problem type and routes to the supervised or unsupervised execution path.

## Invoked by: /doml-modelling [--guidance "..."]

---

## Reproducibility Constraints (Non-Negotiable)
[Same as execute-phase.md — REPR-01, REPR-02, REPR-03]

---

## Workflow

### Step 1 — Validate project state
[Read .planning/STATE.md. If missing, display error and stop.]

### Step 2 — Read project config
[Read .planning/config.json. If missing, display error and stop.]
[Extract problem_type. Save optional --guidance from $ARGUMENTS to GUIDANCE variable.]

### Step 3 — Route by problem_type

Read PROBLEM_TYPE from config.json (same bash as execute-phase.md Step 3g).

Route:
- `regression` or `classification` → Step 4 (Supervised path)
- `clustering` or `dimensionality_reduction` → Step 7 (Unsupervised path)
- `time_series` → display message and stop:
  ```
  Time series detected. Use /doml-forecasting for time series problems.
  /doml-forecasting is planned for Phase 12.
  ```
- anything else → display unknown problem_type error and stop

### Step 4 — Supervised: Copy and execute preprocessing notebook
[Step 3h from execute-phase.md verbatim, with /doml-execute-phase 3 → /doml-modelling]

### Step 5 — Supervised: Copy, execute, and verify modelling notebook
[Steps 3i–3l from execute-phase.md verbatim, with /doml-execute-phase 3 → /doml-modelling]
[After Step 3m interpretation cell: if GUIDANCE was provided, append it as:
 "Analyst guidance: {GUIDANCE}"]

### Step 6 — Supervised: Generate HTML model report
[Steps 5i–5l from execute-phase.md verbatim, with /doml-execute-phase 3 → /doml-modelling]
[After HTML generation, jump to Step 9 (STATE.md update)]

### Step 7 — Unsupervised: Verify templates exist
Check that the appropriate template exists:
- clustering → `.claude/doml/templates/notebooks/modelling_clustering.ipynb`
- dimensionality_reduction → `.claude/doml/templates/notebooks/modelling_dimreduction.ipynb`

If missing:
```
Template not found: .claude/doml/templates/notebooks/modelling_{type}.ipynb
This template should have been created during the DoML framework setup.
```
Stop.

### Step 8 — Unsupervised: Copy, execute, verify, and interpret
[Steps 3n–3s from execute-phase.md verbatim, with /doml-execute-phase 7 → /doml-modelling]
[After Step 3s interpretation cell: if GUIDANCE was provided, append it as:
 "Analyst guidance: {GUIDANCE}"]
[Step 3t message updated: replace "/doml-execute-phase 9" with a note that HTML reports
 are generated by a future dedicated reporting command]

### Step 9 — Update STATE.md
Write to `.planning/STATE.md`:
- Update `current_focus` to next step (e.g., "Modelling complete — use /doml-iterate to refine")
- Append decision: "[Phase 08]: doml-modelling completed — {problem_type} notebook executed"

### Step 10 — Confirm

For supervised:
```
Modelling complete ({problem_type}).

  Preprocessing:  notebooks/03_preprocessing.ipynb
  Modelling:      notebooks/04_modelling_{type}_v{N}.ipynb
  Report:         reports/model_report.html
  Leaderboard:    models/leaderboard.csv

To iterate: /doml-iterate-model "your direction"
```

For unsupervised:
```
Modelling complete ({problem_type}).

  Notebook:       notebooks/0{N}_modelling_{type}_v{M}.ipynb
  Leaderboard:    models/unsupervised_leaderboard.csv

To iterate: /doml-iterate-unsupervised "your direction"
```
```

Copy all step logic from execute-phase.md exactly — do not paraphrase. Only change:
1. `/doml-execute-phase 3` and `/doml-execute-phase 7` → `/doml-modelling`
2. "run /doml-execute-phase 3 again" / "run /doml-execute-phase 7 again" → "run /doml-modelling again"
3. Add guidance handling in Steps 5 and 8 as described above
4. Step 3t note updated (no hard reference to execute-phase 9)
  </action>
  <acceptance_criteria>
    - File exists at `.claude/doml/workflows/modelling.md`
    - Does NOT contain any reference to `execute-phase.md` as a dependency
    - Contains `doml-modelling` in all user-facing messages
    - Contains routing step (regression/classification vs clustering/dim_reduction vs time_series)
    - Contains supervised path: preprocessing copy+execute, modelling copy+execute, HTML report
    - Contains unsupervised path: template check, copy+execute, interpretation
    - Contains guidance handling (`GUIDANCE` / `--guidance`)
    - Contains STATE.md update step
    - Contains all steps 1–10
  </acceptance_criteria>
  <verify>
    <automated>grep -c "doml-modelling" .claude/doml/workflows/modelling.md</automated>
  </verify>
  <done>modelling.md is a self-contained workflow routing all four problem types, with guidance support, and no dependency on execute-phase.md.</done>
</task>

</tasks>

<verification>
1. File exists: `.claude/skills/doml-modelling/SKILL.md`
2. File exists: `.claude/doml/workflows/modelling.md`
3. No execute-phase.md dependency: `grep -c "execute-phase" .claude/doml/workflows/modelling.md` → 0
4. Skill references correct workflow: `grep -c "modelling.md" .claude/skills/doml-modelling/SKILL.md` → 1
5. All four types routed: `grep -c "clustering\|dimensionality_reduction\|regression\|classification" .claude/doml/workflows/modelling.md` → nonzero
6. Guidance supported: `grep -c "guidance\|GUIDANCE" .claude/doml/workflows/modelling.md` → nonzero
</verification>

<success_criteria>
- `doml-modelling` skill exists and is invokable
- `modelling.md` is fully self-contained routing all four problem types
- Supervised path: preprocessing + modelling + HTML report
- Unsupervised path: modelling only, no HTML report
- Optional `--guidance` parameter supported in both paths
- All user-facing messages reference `/doml-modelling` not `/doml-execute-phase`
- STATE.md update step included
</success_criteria>

<output>
After completion, create `.planning/phases/08-phase-named-commands/08-03-SUMMARY.md`
</output>
