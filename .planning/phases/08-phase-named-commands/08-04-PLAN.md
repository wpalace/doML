---
phase: 08-phase-named-commands
plan: 04
type: execute
wave: 2
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - CLAUDE.md
  - .claude/doml/workflows/progress.md
  - .planning/STATE.md
autonomous: true
requirements:
  - CMD-10
  - CMD-11
  - CMD-12
must_haves:
  truths:
    - "doml-execute-phase skill directory deleted"
    - "doml-plan-phase skill directory deleted"
    - "execute-phase.md workflow deleted"
    - "plan-phase.md workflow deleted"
    - "CLAUDE.md references new command names (doml-business-understanding, doml-data-understanding, doml-modelling)"
    - "progress.md routes to new command names"
    - "STATE.md updated to reflect Phase 8 complete"
---

<objective>
Retire the old `doml-execute-phase` and `doml-plan-phase` commands and update all references
to point to the new CRISP-DM-named commands.

This is the cleanup plan for Phase 8. It runs after the three new skills (Plans 01–03) are
confirmed working.

Tasks:
1. Delete the old skill directories
2. Delete the old workflow files
3. Update CLAUDE.md to reference new command names
4. Update progress.md routing table to use new command names
5. Update STATE.md to mark Phase 8 complete
</objective>

<context>
@/home/bill/source/DoML/.planning/phases/08-phase-named-commands/08-CONTEXT.md
@/home/bill/source/DoML/CLAUDE.md
@/home/bill/source/DoML/.claude/doml/workflows/progress.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old skill directories</name>
  <files>.claude/skills/doml-execute-phase/ .claude/skills/doml-plan-phase/</files>
  <read_first>
    - .claude/skills/doml-execute-phase/SKILL.md (confirm it exists before deleting)
    - .claude/skills/doml-plan-phase/SKILL.md (confirm it exists before deleting)
    - .planning/phases/08-phase-named-commands/08-CONTEXT.md
  </read_first>
  <action>
Delete both old skill directories:

```bash
rm -rf .claude/skills/doml-execute-phase
rm -rf .claude/skills/doml-plan-phase
```

Verify they are gone:
```bash
test ! -d .claude/skills/doml-execute-phase && echo "doml-execute-phase deleted OK"
test ! -d .claude/skills/doml-plan-phase && echo "doml-plan-phase deleted OK"
```
  </action>
  <acceptance_criteria>
    - `.claude/skills/doml-execute-phase/` directory does not exist
    - `.claude/skills/doml-plan-phase/` directory does not exist
  </acceptance_criteria>
  <verify>
    <automated>test ! -d .claude/skills/doml-execute-phase && test ! -d .claude/skills/doml-plan-phase && echo "Both old skill dirs deleted"</automated>
  </verify>
  <done>Both old skill directories deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Delete old workflow files</name>
  <files>.claude/doml/workflows/execute-phase.md .claude/doml/workflows/plan-phase.md</files>
  <read_first>
    - .planning/phases/08-phase-named-commands/08-CONTEXT.md
  </read_first>
  <action>
Delete the old workflow files that have been superseded by the three new standalone workflows:

```bash
rm .claude/doml/workflows/execute-phase.md
rm .claude/doml/workflows/plan-phase.md
```

Verify they are gone:
```bash
test ! -f .claude/doml/workflows/execute-phase.md && echo "execute-phase.md deleted OK"
test ! -f .claude/doml/workflows/plan-phase.md && echo "plan-phase.md deleted OK"
```
  </action>
  <acceptance_criteria>
    - `.claude/doml/workflows/execute-phase.md` does not exist
    - `.claude/doml/workflows/plan-phase.md` does not exist
  </acceptance_criteria>
  <verify>
    <automated>test ! -f .claude/doml/workflows/execute-phase.md && test ! -f .claude/doml/workflows/plan-phase.md && echo "Both old workflow files deleted"</automated>
  </verify>
  <done>Both old workflow files deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md — new command names</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (read current DoML Framework section)
    - .planning/phases/08-phase-named-commands/08-CONTEXT.md
  </read_first>
  <action>
Update the "DoML Framework" section at the bottom of `CLAUDE.md`.

Replace the current command list:
```
- `/doml-new-project` — plan an analysis phase
- `/doml-plan-phase` — create detailed phase plan
- `/doml-execute-phase` — run a planned phase
- `/doml-progress` — show current project status
```

With the updated command list:
```
- `/doml-new-project` — guided project kickoff: interview → Docker generation → scaffold → planning artifacts
- `/doml-business-understanding` — run the Business Understanding phase (notebook + HTML report)
- `/doml-data-understanding` — run the Data Understanding / EDA phase (notebook + HTML report)
- `/doml-modelling` — run Modelling for any problem type (preprocessing + modelling + report where applicable)
- `/doml-iterate-model` — run a new supervised modelling iteration with analyst direction
- `/doml-iterate-unsupervised` — run a new unsupervised modelling iteration with analyst direction
- `/doml-progress` — show current project status, completed phases, and next recommended action
```

Use the Edit tool to find the DoML Framework section and update it precisely.
  </action>
  <acceptance_criteria>
    - CLAUDE.md no longer contains `/doml-execute-phase`
    - CLAUDE.md no longer contains `/doml-plan-phase`
    - CLAUDE.md contains `/doml-business-understanding`
    - CLAUDE.md contains `/doml-data-understanding`
    - CLAUDE.md contains `/doml-modelling`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "doml-execute-phase\|doml-plan-phase" CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md DoML Framework section updated to new command names. grep for old names returns 0.</done>
</task>

<task type="auto">
  <name>Task 4: Update progress.md routing table</name>
  <files>.claude/doml/workflows/progress.md</files>
  <read_first>
    - .claude/doml/workflows/progress.md (read Step 5 routing table)
    - .planning/phases/08-phase-named-commands/08-CONTEXT.md
  </read_first>
  <action>
Update the Step 5 routing table in `progress.md`.

Replace the current routing table in Step 5:

```
| Plan exists, not executed | Run `/doml-execute-phase [N]` to run Phase [N] |
| Interview complete, no plans | Run `/doml-plan-phase [N]` to plan Phase [N] |
```

With:

```
| Interview complete, BU not run | Run `/doml-business-understanding` to generate the Business Understanding notebook and report |
| BU complete, EDA not run | Run `/doml-data-understanding` to generate the EDA notebook and report |
| EDA complete, modelling not run | Run `/doml-modelling` to run preprocessing and modelling for your problem type |
| Modelling complete | Run `/doml-iterate-model "direction"` or `/doml-iterate-unsupervised "direction"` to refine |
```

Also update the "All M1 phases complete" row to reference `/doml-progress` and remove any
remaining `/doml-execute-phase` or `/doml-plan-phase` references.

Use the Edit tool to update the routing table precisely.
  </action>
  <acceptance_criteria>
    - progress.md no longer contains `/doml-execute-phase`
    - progress.md no longer contains `/doml-plan-phase`
    - progress.md contains `/doml-business-understanding`
    - progress.md contains `/doml-data-understanding`
    - progress.md contains `/doml-modelling`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "doml-execute-phase\|doml-plan-phase" .claude/doml/workflows/progress.md</automated>
  </verify>
  <done>progress.md routing table updated to new command names. grep for old names returns 0.</done>
</task>

<task type="auto">
  <name>Task 5: Update STATE.md — Phase 8 complete</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state)
  </read_first>
  <action>
Update `.planning/STATE.md` frontmatter and body:

1. Update `status` to `planning` (Phase 9 is next)
2. Update `stopped_at` to: `Phase 8 complete — doml-business-understanding, doml-data-understanding, doml-modelling created; doml-execute-phase and doml-plan-phase retired`
3. Update `last_updated` to `2026-04-10T00:00:00.000Z`
4. Update `last_activity` to `2026-04-10 -- Phase 8 complete; CRISP-DM-named commands created`
5. Update `progress.completed_phases` to 1 (1 of 5 M3 phases complete)
6. Update `progress.percent` to 20

Add to the Decisions section:
```
- [Phase 08]: doml-business-understanding created — replaces doml-execute-phase 1; standalone business-understanding.md workflow
- [Phase 08]: doml-data-understanding created — replaces doml-execute-phase 2; standalone data-understanding.md workflow
- [Phase 08]: doml-modelling created — replaces doml-execute-phase 3 and Phase 7 executor; routes all four problem types
- [Phase 08]: --guidance parameter supported by all three new commands
- [Phase 08]: doml-execute-phase and doml-plan-phase skill directories deleted; execute-phase.md and plan-phase.md deleted
- [Phase 08]: CLAUDE.md and progress.md updated to reference new command names
```

Update Current Position section:
```
## Current Position

Milestone 3 (Refinement) — Phase 9 is next.
Status: Planning — no plans created yet for Phase 9.
Last activity: 2026-04-10 -- Phase 8 complete; CRISP-DM-named commands created

Progress: [██░░░░░░░░] 20% (Milestone 3, 5 phases)
```
  </action>
  <acceptance_criteria>
    - STATE.md `completed_phases` updated to 1
    - STATE.md `stopped_at` mentions Phase 8 complete
    - STATE.md Decisions section contains Phase 08 entries
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Phase 08" .planning/STATE.md</automated>
  </verify>
  <done>STATE.md updated: Phase 8 marked complete, decisions recorded, Phase 9 set as next.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | rm -rf on skill directories | mitigate | Verify new skills exist (Plans 01–03) before running Plan 04 |
| T-08-02 | Availability | execute-phase.md deleted while still referenced | mitigate | Plans 01–03 create standalone replacements before Plan 04 deletes source |
</threat_model>

<verification>
1. Old skills gone: `test ! -d .claude/skills/doml-execute-phase && test ! -d .claude/skills/doml-plan-phase && echo "OK"`
2. Old workflows gone: `test ! -f .claude/doml/workflows/execute-phase.md && test ! -f .claude/doml/workflows/plan-phase.md && echo "OK"`
3. CLAUDE.md clean: `grep -c "doml-execute-phase\|doml-plan-phase" CLAUDE.md` → 0
4. progress.md clean: `grep -c "doml-execute-phase\|doml-plan-phase" .claude/doml/workflows/progress.md` → 0
5. New skills exist: `ls .claude/skills/doml-business-understanding/ .claude/skills/doml-data-understanding/ .claude/skills/doml-modelling/`
6. STATE.md updated: `grep -c "Phase 08" .planning/STATE.md` → nonzero
</verification>

<success_criteria>
- `doml-execute-phase` and `doml-plan-phase` skill directories deleted
- `execute-phase.md` and `plan-phase.md` workflow files deleted
- `CLAUDE.md` references only new command names in the DoML Framework section
- `progress.md` routing table uses new command names
- `STATE.md` marks Phase 8 complete with all decisions recorded
</success_criteria>

<output>
After completion, create `.planning/phases/08-phase-named-commands/08-04-SUMMARY.md`
</output>
