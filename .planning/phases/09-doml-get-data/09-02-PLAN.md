---
phase: 09-doml-get-data
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .claude/doml/workflows/new-project.md
autonomous: false
requirements:
  - DATA-03

must_haves:
  truths:
    - "Running /doml-new-project when data/raw/ is empty presents an AskUserQuestion choice instead of exiting with an error"
    - "Choosing 'Get data now' runs the doml-get-data flow inline and then re-scans data/raw/"
    - "Choosing 'Add files manually' prints format instructions, then pauses for user confirmation, then re-scans"
    - "If re-scan still finds no files, the choice loops back — it never silently continues with empty data"
  artifacts:
    - path: ".claude/doml/workflows/new-project.md"
      provides: "Modified Step 3 with inline AskUserQuestion fallback instead of SystemExit(1) on empty data/raw/"
      contains: "Get data now"
  key_links:
    - from: ".claude/doml/workflows/new-project.md Step 3"
      to: ".claude/doml/workflows/get-data.md"
      via: "inline invocation of get-data workflow when user chooses 'Get data now'"
      pattern: "get-data"
---

<objective>
Modify new-project.md so that when `data/raw/` is empty, instead of stopping with an error,
it presents an inline AskUserQuestion giving the user a choice: acquire data now via doml-get-data,
or add files manually. After either path, the Step 3 scan re-runs and the interview continues.

Purpose: DATA-03 requires doml-new-project to invoke doml-get-data automatically when data/raw/ is empty.

Output: Modified new-project.md Step 3 with the inline choice loop.
</objective>

<execution_context>
@/home/bill/source/DoML/.claude/get-shit-done/workflows/execute-plan.md
@/home/bill/source/DoML/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-doml-get-data/09-CONTEXT.md
@.planning/phases/09-doml-get-data/09-RESEARCH.md
@.planning/phases/09-doml-get-data/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace empty-data error exit in new-project.md Step 3 with inline choice loop</name>
  <files>.claude/doml/workflows/new-project.md</files>
  <read_first>
    - .claude/doml/workflows/new-project.md (full file — must read entire Step 3 to understand current error path)
    - .planning/phases/09-doml-get-data/09-CONTEXT.md (exact AskUserQuestion text and loop behavior locked in decisions)
    - .claude/doml/workflows/get-data.md (created in Plan 01 — inline invocation reference)
  </read_first>
  <action>
In `.claude/doml/workflows/new-project.md`, locate Step 3 "Data folder validation and scan".

Find the block that handles the empty directory case. Currently it reads:
```python
if not files:
    print('\nError: data/raw/ is empty. Add at least one CSV, Parquet, or XLSX file before running the interview.\n')
    raise SystemExit(1)
```

And the surrounding instruction text:
```
If the command exits with non-zero status (container not running, data/raw missing, empty directory, .xls file), display the error message printed by the script and STOP.
```

**Do NOT change:**
- The DuckDB scan code itself (column introspection, schema reporting, JSON output)
- The `.xls` file check (which should still exit with error — that is a format error, not an acquisition issue)
- The `data/raw/ directory not found` check (which should still exit with error)
- Step 4 and all subsequent steps

**Change the empty-files handling only:**

Replace the "raise SystemExit(1)" for the empty-files case with a sentinel output, and add a new
**"Step 3b — Data acquisition fallback"** subsection immediately after the scan block.

The new Step 3b text must be:

---

**Step 3b — Data acquisition fallback (when data/raw/ is empty)**

If the Step 3 scan Python script exits with non-zero AND the output contains the empty-directory
message (not the `.xls` error or directory-not-found error), do NOT stop. Instead, enter the
acquisition loop:

**Present AskUserQuestion:**
```
data/raw/ is empty. No datasets found.

How would you like to get your data?
  → Get data now (Kaggle or URL)
  → Add files manually
```

**If user chooses "Get data now":**

Run the full doml-get-data acquisition flow inline:
1. Ask using AskUserQuestion: "Enter your data source:"
   - For Kaggle: `kaggle owner/dataset-name` (e.g. `kaggle titanic/titanic-dataset`)
   - For URL: `url https://example.com/data.csv`
2. Parse the response as `SOURCE_TYPE` (first word) and `SOURCE_VALUE` (remainder).
3. Execute the get-data workflow steps (Steps 1–8 of @.claude/doml/workflows/get-data.md) inline.
4. After get-data completes, re-run the Step 3 DuckDB scan.
5. If scan now finds files → display the scan report and continue to Step 4.
6. If scan still finds no files → loop back to the AskUserQuestion above.

**If user chooses "Add files manually":**

Display:
```
Add your dataset files to the ./data/raw/ directory on your host machine.

Supported formats: CSV (.csv), TSV (.tsv), Parquet (.parquet), Excel (.xlsx)
Note: Legacy .xls files are not supported — convert to .xlsx or .csv first.

Docker mounts data/raw/ automatically — no container restart needed after adding files.
```

Then ask using AskUserQuestion:
```
I've added my files to data/raw/ — continue
```

After the user confirms, re-run the Step 3 DuckDB scan.
- If scan finds files → display the scan report and continue to Step 4.
- If scan still finds no files → loop back to the top AskUserQuestion ("Get data now" / "Add files manually").

**What still stops with an error (unchanged):**
- `data/raw/` directory does not exist → stop with error
- `.xls` files found → stop with format error
- Container not running → stop with error

---

The Python scan script in Step 3 must also be updated so the empty-files case emits a recognizable
sentinel output (instead of SystemExit(1)) so the agent can distinguish "empty" from "error":

Change:
```python
if not files:
    print('\nError: data/raw/ is empty. Add at least one CSV, Parquet, or XLSX file before running the interview.\n')
    raise SystemExit(1)
```

To:
```python
if not files:
    print('\nEMPTY_DATA_DIR\n')
    raise SystemExit(2)
```

Then in the Step 3 parse instructions, add:
- If exit code is 2 AND output contains `EMPTY_DATA_DIR` → trigger Step 3b (acquisition loop)
- If exit code is 1 → display the error message and stop (directory not found, .xls error, etc.)
- If exit code is 0 → scan succeeded, continue to Step 4
  </action>
  <verify>
    Read `.claude/doml/workflows/new-project.md` and confirm:
    - `grep -c "EMPTY_DATA_DIR" new-project.md` returns at least 2 (sentinel definition + check)
    - `grep -c "Get data now" new-project.md` returns at least 1
    - `grep -c "Add files manually" new-project.md` returns at least 1
    - `grep -c "get-data.md" new-project.md` returns at least 1
    - `grep -c "raise SystemExit(1)" new-project.md` returns 1 (only the .xls error remains with exit 1; empty-dir now uses exit 2)
    - `grep -c "SystemExit(2)" new-project.md` returns 1
  </verify>
  <acceptance_criteria>
    - `.claude/doml/workflows/new-project.md` contains `EMPTY_DATA_DIR` sentinel (used in both Python script and parse instructions)
    - File contains `SystemExit(2)` for the empty-files case
    - File contains `SystemExit(1)` only for the .xls or directory-missing error cases
    - File contains `Step 3b` subsection with "Data acquisition fallback" heading
    - File contains `Get data now` AskUserQuestion option
    - File contains `Add files manually` AskUserQuestion option
    - File contains reference to `get-data.md` workflow for inline invocation
    - File contains loop-back instruction ("loop back to the AskUserQuestion above") for still-empty re-scan
    - File does NOT contain `raise SystemExit(1)` for the empty-files case (only for .xls and directory-not-found)
    - All original Step 4 and later content is unchanged (verify by checking that Q1 business question prompt still exists)
  </acceptance_criteria>
  <done>new-project.md Step 3 presents an inline choice when data/raw/ is empty instead of stopping with an error. The loop continues until files are present or the user explicitly exits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 9 doml-get-data complete:
    - .claude/skills/doml-get-data/SKILL.md — skill entry point
    - .claude/doml/workflows/get-data.md — full 8-step acquisition workflow
    - .claude/doml/templates/docker-compose.yml — KAGGLE env var slots added
    - requirements.in — kaggle package added
    - .gitignore — data/raw/.stage/ excluded
    - .claude/doml/workflows/new-project.md — empty data/raw/ now offers inline acquisition choice
  </what-built>
  <how-to-verify>
    1. Check SKILL.md exists and has correct structure:
       Read `.claude/skills/doml-get-data/SKILL.md` — verify frontmatter has `name: doml-get-data`, `argument-hint`, and execution_context points to get-data.md.

    2. Check get-data.md coverage:
       Read `.claude/doml/workflows/get-data.md` — scan for all 8 steps.
       Verify Step 4 has credential check with MISSING_KAGGLE_CREDS sentinel.
       Verify docker cp uses `:/tmp/doml-stage/.` (trailing /.).
       Verify Step 7 appends to README.md with provenance block format matching CONTEXT.md.

    3. Check config file changes:
       - `grep "KAGGLE_USERNAME" .claude/doml/templates/docker-compose.yml` → should return the env var line
       - `grep "^kaggle" requirements.in` → should return `kaggle`
       - `grep "data/raw/.stage/" .gitignore` → should return the gitignore entry

    4. Check new-project.md Step 3 modification:
       Read `.claude/doml/workflows/new-project.md` Step 3.
       Verify EMPTY_DATA_DIR sentinel is used.
       Verify Step 3b exists with "Get data now" and "Add files manually" options.
       Verify the .xls error case still uses SystemExit(1) (unchanged).

    5. Optional live test (if Docker is running and you have a Kaggle key):
       Run `/doml-get-data kaggle` (no slug) → should display usage.
       Run `/doml-get-data` (no args) → should display usage.
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → new-project.md interview | User chooses "Get data now" or "Add files manually"; source slug/URL entered by user is passed to get-data workflow |
| get-data workflow (inline) → host filesystem | Same boundaries as Plan 01 — see T-09-01 through T-09-07 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Denial of Service | Infinite loop on persistent empty scan | mitigate | AskUserQuestion presents explicit "Get data now / Add files manually" choice each iteration; user can exit Claude Code at any point; loop only continues if user explicitly re-confirms — never spins autonomously |
| T-09-09 | Tampering | User bypasses data validation via "Add files manually" and provides unsupported format | accept | Step 3 scan already catches .xls files with an error (unchanged); other unsupported formats are ignored by the file filter — they won't prevent the interview from proceeding, but they won't be analyzed either |
| T-09-10 | Spoofing | User types malformed source when prompted in Step 3b inline get-data | mitigate | Step 1 of get-data.md validates that first token is "kaggle" or "url" and displays usage if not — same validation applies when invoked inline |
</threat_model>

<verification>
After plan execution:

1. `grep "EMPTY_DATA_DIR" .claude/doml/workflows/new-project.md` returns at least 2 matches
2. `grep "Step 3b" .claude/doml/workflows/new-project.md` returns 1 match
3. `grep "Get data now" .claude/doml/workflows/new-project.md` returns at least 1 match
4. `grep "SystemExit(2)" .claude/doml/workflows/new-project.md` returns 1 match
5. Human checkpoint approved
</verification>

<success_criteria>
1. /doml-new-project with empty data/raw/ presents a user choice — no longer stops with an error
2. Choosing "Get data now" runs doml-get-data inline and continues the interview after successful download
3. Choosing "Add files manually" pauses and waits for user confirmation before re-scanning
4. Neither path silently continues with empty data — the loop enforces a file is present before proceeding
5. All other new-project.md behavior (Docker setup, .xls errors, interview Q1–Q7) is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/09-doml-get-data/09-02-SUMMARY.md` using the summary template.
</output>
