---
phase: 13-deployment-workflow-skeleton
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - .claude/doml/workflows/progress.md
autonomous: true
requirements:
  - DEPLOY-01

must_haves:
  truths:
    - "progress.md routes users to /doml-deploy-model after modelling is complete"
  artifacts:
    - path: ".claude/doml/workflows/progress.md"
      provides: "Updated next-action routing to include deployment step"
      contains: "doml-deploy-model"
  key_links:
    - from: "progress.md Step 5"
      to: "doml-deploy-model"
      via: "next action routing table"
      pattern: "doml-deploy-model"
---

<objective>
Update the `doml-progress` workflow to surface `doml-deploy-model` as the next recommended action after modelling is complete.

Purpose: Without this update, users who have completed modelling will see "Run /doml-iterate" as the next action permanently, with no guidance toward deploying their model. The routing table in progress.md Step 5 must be extended with a deployment row.

Output: Updated `.claude/doml/workflows/progress.md` with deployment next-action entry.
</objective>

<execution_context>
@/home/bill/source/DoML/.claude/get-shit-done/workflows/execute-plan.md
@/home/bill/source/DoML/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-deployment-workflow-skeleton/13-01-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure workflow documentation update — no file I/O, no user input processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering | progress.md edit | accept | Read-then-write pattern; no user input; documentation file only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Update progress.md next-action routing table</name>
  <files>.claude/doml/workflows/progress.md</files>
  <read_first>
    - .claude/doml/workflows/progress.md (current routing table in Step 5 — read before editing)
  </read_first>
  <action>
Read `.claude/doml/workflows/progress.md`. Locate the routing table in **Step 5 — Determine next action**. The table currently has these rows (among others):

```
| Modelling complete | Run `/doml-iterate "direction"` to refine — reads `problem_type` from `config.json` and routes to the correct iteration pipeline automatically |
| Phase in progress | Continue with the current phase — check STATE.md `stopped_at` |
| All phases complete | Run `/doml-progress` to review status, or check `reports/` for stakeholder outputs |
```

Insert a new row between "Modelling complete" and "Phase in progress":

```
| Deployment not yet run (no src/ directory) | Run `/doml-deploy-model` to scaffold the deployment directory for the best leaderboard model. Choose from CLI binary, web service, or ONNX/WASM targets. |
```

The STATE.md condition to detect "deployment not yet run" is: modelling has been completed (leaderboard file exists) AND `src/` directory does not exist or contains no model slug subdirectory.

Also add `doml-deploy-model` to the list of available commands displayed at the end of Step 5 (or wherever the existing workflow shows command examples), if such a list exists in the file. Only add it if a commands-list section already exists — do not create one.

Write the updated file back using the Write tool with the full modified content.
  </action>
  <verify>
    <automated>grep -q "doml-deploy-model" /home/bill/source/DoML/.claude/doml/workflows/progress.md && echo "progress.md updated"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "doml-deploy-model" .claude/doml/workflows/progress.md` exits 0
    - `grep -q "Deployment not yet run"` OR `grep -q "src/"` in the routing table context exits 0
    - Existing rows in the routing table are unchanged (no other rows modified or deleted)
    - File is valid Markdown (no broken table syntax — pipes aligned, header separator row present)
  </acceptance_criteria>
  <done>progress.md contains a routing row that surfaces /doml-deploy-model as the next action when modelling is complete but no deployment directory exists yet.</done>
</task>

</tasks>

<verification>
After task completes:

1. `grep -q "doml-deploy-model" .claude/doml/workflows/progress.md` — exits 0
2. `grep -q "| Modelling complete |" .claude/doml/workflows/progress.md` — exits 0 (original row preserved)
3. `grep -q "| Deployment not yet run" .claude/doml/workflows/progress.md` — exits 0 (new row added)
</verification>

<success_criteria>
`doml-progress` will surface `/doml-deploy-model` as the recommended next action once modelling artifacts exist but `src/` has not yet been populated — completing the DEPLOY-01 discoverability requirement.
</success_criteria>

<output>
After completion, create `.planning/phases/13-deployment-workflow-skeleton/13-02-SUMMARY.md`
</output>
