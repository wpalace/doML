---
phase: 14-cli-binary-target
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/doml/templates/docker-compose.yml
  - .claude/doml/templates/requirements.in
autonomous: true
requirements:
  - CLI-01
  - CLI-06

must_haves:
  truths:
    - "The Docker container can see and write files under src/ on the host filesystem"
    - "pyinstaller is declared in the requirements template and will be installed on docker compose build"
    - "Any new DoML project scaffolded from the templates will inherit both changes"
  artifacts:
    - path: ".claude/doml/templates/docker-compose.yml"
      provides: "Volume mount for src/ in Jupyter container"
      contains: "./src:/home/jovyan/work/src"
    - path: ".claude/doml/templates/requirements.in"
      provides: "pyinstaller declared as unpinned top-level dep"
      contains: "pyinstaller"
  key_links:
    - from: ".claude/doml/templates/docker-compose.yml"
      to: "src/<modelname>/vN/"
      via: "volume mount ./src:/home/jovyan/work/src"
      pattern: "./src:/home/jovyan/work/src"
    - from: ".claude/doml/templates/requirements.in"
      to: "container pyinstaller binary"
      via: "pip-compile → requirements.txt → docker compose build"
      pattern: "pyinstaller"
---

<objective>
Add the infrastructure prerequisites that make the CLI binary build possible: (1) mount `src/` into
the Jupyter Docker container so PyInstaller can read and write `src/<modelname>/vN/` files inside
the container, and (2) declare `pyinstaller` in the `requirements.in` template so it is installed
on the next `docker compose build`.

Purpose: The PyInstaller build must run inside the existing Jupyter container (D-06), but `src/` has
no Docker volume mount today — the container cannot see those files. Without `pyinstaller` in
`requirements.in`, adding it ad-hoc violates REPR-04 (pinned dependencies). Both blockers must be
cleared before the workflow can run.

Output: Updated `docker-compose.yml` template (with `./src:/home/jovyan/work/src` writable mount)
and updated `requirements.in` template (with `pyinstaller` appended).
</objective>

<execution_context>
@/home/bill/source/DoML/.claude/get-shit-done/workflows/execute-plan.md
@/home/bill/source/DoML/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-cli-binary-target/14-CONTEXT.md
@.planning/phases/14-cli-binary-target/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add src/ volume mount to docker-compose.yml template</name>
  <files>.claude/doml/templates/docker-compose.yml</files>
  <read_first>
    - .claude/doml/templates/docker-compose.yml — read current volumes list before modifying; must append after the `models` mount line
  </read_first>
  <action>
Read `.claude/doml/templates/docker-compose.yml`. In the `volumes:` block, after the line:

```
      - ./models:/home/jovyan/work/models
```

Insert the following line (same indentation — six spaces):

```
      - ./src:/home/jovyan/work/src
```

The resulting volumes block must have exactly these entries in order:
  - `./data/raw:/home/jovyan/work/data/raw:ro`
  - `./data/external:/home/jovyan/work/data/external:ro`
  - `./.planning:/home/jovyan/work/.planning:ro`
  - `./data/processed:/home/jovyan/work/data/processed`
  - `./notebooks:/home/jovyan/work/notebooks`
  - `./reports:/home/jovyan/work/reports`
  - `./models:/home/jovyan/work/models`
  - `./src:/home/jovyan/work/src`

No other changes to the file.

Note: This is the DoML project TEMPLATE file (not a live analysis project). The template is stamped
into new analysis projects by /doml-new-project. Existing analysis projects must add this mount
manually — the workflow (Plan 02) will detect and warn if the mount is missing in the live
docker-compose.yml and instruct the user to add it.
  </action>
  <verify>
    <automated>grep -c "./src:/home/jovyan/work/src" /home/bill/source/DoML/.claude/doml/templates/docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/doml/templates/docker-compose.yml` contains the line `./src:/home/jovyan/work/src` (exact string, writable — no `:ro` suffix)
    - The `./models:/home/jovyan/work/models` line is still present (not replaced)
    - The file has no other changes (all other lines unchanged)
  </acceptance_criteria>
  <done>docker-compose.yml template has `./src:/home/jovyan/work/src` writable volume mount after the models mount</done>
</task>

<task type="auto">
  <name>Task 2: Add pyinstaller to requirements.in template</name>
  <files>.claude/doml/templates/requirements.in</files>
  <read_first>
    - .claude/doml/templates/requirements.in — read current contents so pyinstaller is appended, not duplicated
  </read_first>
  <action>
Read `.claude/doml/templates/requirements.in`. Check whether a line `pyinstaller` already exists.

If it does NOT exist, append the following line to the end of the file:

```
pyinstaller
```

Add it in the same unpinned style as other entries (no version constraint — `pip-compile` resolves
the exact version). Place it in the deployment tools section if such a section exists, or append
at the end of the file with a blank line separator if it does not.

Note: This template is the canonical source. `requirements.txt` in the template is the pip-compile
output. The workflow in Plan 02 instructs the user to run `docker compose run --rm jupyter
pip-compile requirements.in` and then `docker compose build` to install pyinstaller into the
container. The plan does not run the build — that is a live project operation performed in context
by the deploy-cli workflow.
  </action>
  <verify>
    <automated>grep -c "^pyinstaller$" /home/bill/source/DoML/.claude/doml/templates/requirements.in</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/doml/templates/requirements.in` contains a line that is exactly `pyinstaller` (no version pin, no leading spaces)
    - The line appears exactly once in the file (grep -c returns 1)
    - All previously existing lines in the file are preserved
  </acceptance_criteria>
  <done>requirements.in template declares pyinstaller as an unpinned top-level dependency</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template files → new analysis projects | Templates are stamped into user-created projects; a malicious or malformed template could inject unexpected mounts or dependencies into every new DoML project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | docker-compose.yml template | accept | Template is in-repo, version-controlled; changes are reviewed via git diff; no external input accepted |
| T-14-01-02 | Information Disclosure | `./src` mount (writable) | accept | `src/` contains only generated inference artifacts (predict.py, dist/); no secrets; same risk level as existing `./models` writable mount |
| T-14-01-03 | Elevation of Privilege | requirements.in (new pyinstaller dep) | accept | pyinstaller is a well-known packaging tool on PyPI; no network access or privilege escalation beyond building binaries from local Python files; REPR-04 compliant because declared in requirements.in |
</threat_model>

<verification>
After both tasks complete:

1. `grep "./src:/home/jovyan/work/src" .claude/doml/templates/docker-compose.yml` returns the mount line
2. `grep "^pyinstaller$" .claude/doml/templates/requirements.in` returns `pyinstaller`
3. `grep "./models:/home/jovyan/work/models" .claude/doml/templates/docker-compose.yml` still present (no regression)
4. The docker-compose.yml file parses as valid YAML (no indentation errors): `python3 -c "import yaml; yaml.safe_load(open('.claude/doml/templates/docker-compose.yml'))"`
</verification>

<success_criteria>
- `./src:/home/jovyan/work/src` is present in the docker-compose.yml template (writable, no `:ro`)
- `pyinstaller` is present as a bare unpinned line in requirements.in template
- Both files are otherwise unchanged from their pre-task state
- docker-compose.yml template is valid YAML
</success_criteria>

<output>
After completion, create `.planning/phases/14-cli-binary-target/14-01-SUMMARY.md`
</output>
