---
phase: 14-cli-binary-target
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - .claude/skills/doml-deploy-cli/SKILL.md
  - .claude/doml/workflows/deploy-cli.md
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05
  - CLI-06

must_haves:
  truths:
    - "User can invoke /doml-deploy-cli to build a Linux binary from the current deployment"
    - "The binary accepts a JSON string and prints a prediction to stdout"
    - "The binary accepts a CSV or JSON file path and prints batch predictions to stdout"
    - "The binary writes batch predictions to a file when --output is supplied"
    - "--help shows all feature names, their types, and example values from the first preprocessed row"
    - "The binary exits 0 on success, 1 on input error, 2 on model error"
    - "deployment_metadata.json is updated with platform: linux-x86_64 after a successful build"
  artifacts:
    - path: ".claude/skills/doml-deploy-cli/SKILL.md"
      provides: "/doml-deploy-cli command entry point"
      contains: "deploy-cli"
    - path: ".claude/doml/workflows/deploy-cli.md"
      provides: "Full 10-step CLI build workflow"
      contains: "pyinstaller"
  key_links:
    - from: ".claude/skills/doml-deploy-cli/SKILL.md"
      to: ".claude/doml/workflows/deploy-cli.md"
      via: "@.claude/doml/workflows/deploy-cli.md execution_context reference"
      pattern: "deploy-cli.md"
    - from: ".claude/doml/workflows/deploy-cli.md"
      to: "src/<modelname>/vN/predict.py"
      via: "Step 4 — Write generates the file from embedded template"
      pattern: "predict.py"
    - from: ".claude/doml/workflows/deploy-cli.md"
      to: "src/<modelname>/vN/dist/predict/predict"
      via: "Step 7 — docker compose exec jupyter pyinstaller predict.spec"
      pattern: "pyinstaller predict.spec"
    - from: "src/<modelname>/vN/predict.py"
      to: "models/best_model.pkl"
      via: "sys._MEIPASS bundle path at runtime"
      pattern: "sys._MEIPASS"
---

<objective>
Implement `/doml-deploy-cli` — a SKILL.md entry point and full `deploy-cli.md` workflow that
generates `predict.py` and `predict.spec` inside `src/<modelname>/vN/`, runs the PyInstaller build
inside the existing Jupyter Docker container, and updates `deployment_metadata.json` with
`"platform": "linux-x86_64"` on success.

Purpose: Delivers the CLI binary deployment target for Phase 14, satisfying all six CLI requirements.
The workflow reads `deployment_metadata.json` (produced by Phase 13), reads example values from the
first preprocessed row (D-03), generates a fully-featured predict.py with classification probabilities
(D-04) and batch output (D-05), and packages it as a `--onedir` binary (D-01) using the project's
own Docker container (D-06).

Output:
- `.claude/skills/doml-deploy-cli/SKILL.md` — command entry point
- `.claude/doml/workflows/deploy-cli.md` — 10-step workflow
</objective>

<execution_context>
@/home/bill/source/DoML/.claude/get-shit-done/workflows/execute-plan.md
@/home/bill/source/DoML/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-cli-binary-target/14-CONTEXT.md
@.planning/phases/14-cli-binary-target/14-RESEARCH.md
@.planning/phases/14-cli-binary-target/14-01-SUMMARY.md
@.claude/skills/doml-deploy-model/SKILL.md
@.claude/doml/workflows/deploy-model.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from Phase 13 outputs. -->

From src/<modelname>/vN/deployment_metadata.json (Phase 13 output):
```json
{
  "model_file": "models/best_model.pkl",
  "model_name": "RandomForestClassifier",
  "target": "cli_binary",
  "build_date": "2026-04-14T19:00:00+00:00",
  "version": "v1",
  "feature_schema": [
    {"name": "age", "type": "int64"},
    {"name": "income", "type": "float64"}
  ]
}
```

From data/processed/preprocessed_*.csv (first row used for example values, D-03):
- Header row gives column names (must match feature_schema names)
- First data row gives example values hardcoded into predict.py --help epilog

PyInstaller onedir output structure (D-01):
  src/<modelname>/vN/dist/predict/         ← the artifact directory
  src/<modelname>/vN/dist/predict/predict  ← the executable
  src/<modelname>/vN/dist/predict/_internal/ ← .so files, model.pkl bundle

sys._MEIPASS (PyInstaller 6.x onedir):
  When frozen: sys._MEIPASS points to dist/predict/_internal/
  When plain Python: use os.path.dirname(os.path.abspath(__file__))
  Model bundled as data file: accessible via os.path.join(sys._MEIPASS, 'best_model.pkl')

predict.py exit codes (CLI-06):
  sys.exit(0)  ← success
  sys.exit(1)  ← input validation error (ValueError, KeyError, pd.errors.ParserError)
  sys.exit(2)  ← model error (any Exception from joblib.load or model.predict)

SKILL.md structure (from doml-deploy-model/SKILL.md):
  ---
  name: <command-name>
  description: "..."
  argument-hint: "[--args]"
  allowed-tools: [Read, Write, Bash, Glob, Grep, AskUserQuestion]
  ---
  <objective>...</objective>
  <execution_context>@workflow</execution_context>
  <context>Arguments: $ARGUMENTS</context>
  <process>Execute the workflow end-to-end.</process>

Docker container working directory: /home/jovyan/work
Host src/ mount (after Plan 01): ./src:/home/jovyan/work/src
PyInstaller build command: docker compose exec jupyter bash -c "cd /home/jovyan/work/src/{MODEL_SLUG}/v{VERSION} && pyinstaller predict.spec"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SKILL.md entry point for /doml-deploy-cli</name>
  <files>.claude/skills/doml-deploy-cli/SKILL.md</files>
  <read_first>
    - .claude/skills/doml-deploy-model/SKILL.md — reference for exact SKILL.md format (frontmatter fields, section names, section order)
  </read_first>
  <action>
Create directory `.claude/skills/doml-deploy-cli/` and write `SKILL.md` with the following exact
content (follow the doml-deploy-model SKILL.md structure exactly):

```yaml
# SKILL.md frontmatter (--- delimiters required in the actual file)
# ↓ start frontmatter
name: doml-deploy-cli
description: "Build a self-contained Linux CLI binary from a deployed model. Reads src/<modelname>/vN/deployment_metadata.json (created by /doml-deploy-model), generates predict.py with argparse input handling and predict.spec with PyInstaller onedir packaging, then runs the build inside the existing Jupyter Docker container to produce dist/predict/predict. Supports --deploy-dir path/to/vN to target a specific deployment directory."
argument-hint: "[--deploy-dir src/<modelname>/vN]"
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - AskUserQuestion
# ↑ end frontmatter

<objective>
Generate a self-contained Linux CLI binary for the DoML deployed model:

1. Validate project state and locate the deployment directory
2. Read deployment_metadata.json for model_file, model_name, version, feature_schema, problem_type
3. Read the first data row of data/processed/preprocessed_*.csv for example values (D-03)
4. Check that src/ is mounted in docker-compose.yml; warn and instruct if not
5. Verify pyinstaller is installed in the container; instruct how to install if not
6. Generate predict.py from embedded template (argparse, --input JSON/file, --output, exit codes 0/1/2)
7. Generate predict.spec from embedded template (collect_all sklearn + joblib, onedir, bundle model)
8. Run PyInstaller build: docker compose exec jupyter bash -c "cd .../vN && pyinstaller predict.spec"
9. Verify dist/predict/predict exists after build
10. Update deployment_metadata.json with "platform": "linux-x86_64"
</objective>

<execution_context>
@.claude/doml/workflows/deploy-cli.md
</execution_context>

<context>
Arguments (optional): $ARGUMENTS
</context>

<process>
Execute the deploy-cli workflow from @.claude/doml/workflows/deploy-cli.md end-to-end.
</process>
```
  </action>
  <verify>
    <automated>grep -c "doml-deploy-cli" /home/bill/source/DoML/.claude/skills/doml-deploy-cli/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.claude/skills/doml-deploy-cli/SKILL.md`
    - File contains `name: doml-deploy-cli` in YAML frontmatter
    - File contains `argument-hint: "[--deploy-dir src/<modelname>/vN]"`
    - File contains `@.claude/doml/workflows/deploy-cli.md` in the execution_context block
    - File contains `allowed-tools:` section listing Read, Write, Bash, Glob, Grep, AskUserQuestion
    - File has `<objective>`, `<execution_context>`, `<context>`, `<process>` sections
  </acceptance_criteria>
  <done>SKILL.md exists with correct name, argument-hint, allowed-tools, and workflow reference</done>
</task>

<task type="auto">
  <name>Task 2: Write deploy-cli.md workflow (10 steps, full predict.py + spec templates)</name>
  <files>.claude/doml/workflows/deploy-cli.md</files>
  <read_first>
    - .claude/doml/workflows/deploy-model.md — reference for step header format (`### Step N —`), bash pattern conventions, AskUserQuestion usage, STATE.md update pattern
    - .claude/skills/doml-deploy-cli/SKILL.md — just created in Task 1; confirms the workflow is the sole execution target
    - .planning/phases/14-cli-binary-target/14-CONTEXT.md — D-01 through D-06 (all locked decisions must appear)
    - .planning/phases/14-cli-binary-target/14-RESEARCH.md — full predict.py template structure, spec template, exit code patterns, sys._MEIPASS pattern, pitfalls
  </read_first>
  <action>
Create `.claude/doml/workflows/deploy-cli.md` with exactly the following content.

All CRITICAL implementation details are specified below. The executor must write this file verbatim,
substituting nothing. Shell variables are written as `${VAR}` — they are evaluated at workflow
runtime, not at file-write time. Shell variables written as `${VAR}` are evaluated at workflow runtime.

```markdown
# DoML Deploy CLI Workflow

## Purpose
Generate predict.py and predict.spec, run PyInstaller inside the Jupyter Docker container, and
produce src/<modelname>/vN/dist/predict/predict — a self-contained Linux binary requiring no
Python on the target machine.

## Invoked by: /doml-deploy-cli [--deploy-dir src/<modelname>/vN]

## Workflow

### Step 1 — Validate project state

Read `.planning/STATE.md`. If it does not exist, display and stop:
```
No DoML project found. Run /doml-new-project first.
```

### Step 2 — Locate deployment directory

Parse `--deploy-dir` flag from `$ARGUMENTS`:
```bash
DEPLOY_DIR=""
if echo "$ARGUMENTS" | grep -q -- '--deploy-dir'; then
  DEPLOY_DIR=$(echo "$ARGUMENTS" | sed 's/.*--deploy-dir[[:space:]]\+\([^[:space:]]*\).*/\1/')
fi
```

If `DEPLOY_DIR` is empty, find the most recent deployment directory:
```bash
if [ -z "$DEPLOY_DIR" ]; then
  DEPLOY_DIR=$(python3 -c "
import glob, re, sys, os
dirs = glob.glob('src/*/*')
versioned = [(d, int(re.search(r'/v(\d+)$', d).group(1))) for d in dirs if re.search(r'/v(\d+)$', d) and os.path.isfile(d + '/deployment_metadata.json')]
if not versioned:
    print('')
else:
    latest = sorted(versioned, key=lambda x: x[1], reverse=True)[0][0]
    print(latest)
")
  if [ -z "$DEPLOY_DIR" ]; then
    echo "No deployment directory found. Run /doml-deploy-model first."
    exit 1
  fi
  echo "Using deployment directory: $DEPLOY_DIR"
fi
```

Validate the deployment directory exists and contains deployment_metadata.json:
```bash
if [ ! -f "$DEPLOY_DIR/deployment_metadata.json" ]; then
  echo "deployment_metadata.json not found in $DEPLOY_DIR"
  echo "Run /doml-deploy-model first to scaffold the deployment directory."
  exit 1
fi
```

### Step 3 — Read deployment_metadata.json

```bash
python3 -c "
import json, sys, os

deploy_dir = '${DEPLOY_DIR}'
with open(deploy_dir + '/deployment_metadata.json') as f:
    meta = json.load(f)

model_file = meta.get('model_file', 'models/best_model.pkl')
model_name = meta.get('model_name', 'model')
version = meta.get('version', 'v1')
feature_schema = meta.get('feature_schema', [])
target = meta.get('target', 'cli_binary')

if target != 'cli_binary':
    print(f'ERROR: deployment_metadata.json target is \"{target}\", not \"cli_binary\".')
    print('This deployment was not created for the CLI target.')
    print('Run /doml-deploy-model and select \"CLI binary\" to create a CLI deployment.')
    sys.exit(1)

# Write env vars to a temp file for shell to read
with open('/tmp/doml_cli_meta.sh', 'w') as f:
    f.write(f'MODEL_FILE=\"{model_file}\"\n')
    f.write(f'MODEL_NAME=\"{model_name}\"\n')
    f.write(f'VERSION=\"{version}\"\n')
    f.write(f'FEATURE_SCHEMA_JSON={json.dumps(json.dumps(feature_schema))}\n')

print(f'Model: {model_name} ({model_file}), version: {version}')
print(f'Feature schema: {len(feature_schema)} features')
"
if [ $? -ne 0 ]; then exit 1; fi
source /tmp/doml_cli_meta.sh
```

Read problem_type from config.json:
```bash
PROBLEM_TYPE=$(python3 -c "import json; c=json.load(open('.planning/config.json')); print(c.get('problem_type','regression'))")
echo "Problem type: $PROBLEM_TYPE"
```

### Step 4 — Read example values from first preprocessed row (D-03)

```bash
python3 -c "
import glob, json, sys

processed_files = sorted(glob.glob('data/processed/preprocessed_*'))
feature_schema = json.loads('${FEATURE_SCHEMA_JSON}')

if processed_files:
    import pandas as pd
    df = pd.read_csv(processed_files[0], nrows=1)
    if len(df) == 0:
        # No data rows — fall back to type-based placeholders
        examples = {f['name']: ('0' if 'int' in f['type'] else '0.0' if 'float' in f['type'] else 'value') for f in feature_schema}
    else:
        first_row = df.iloc[0]
        examples = {}
        for f in feature_schema:
            col = f['name']
            if col in first_row.index:
                val = first_row[col]
                examples[col] = str(val.item() if hasattr(val, 'item') else val)
            else:
                examples[col] = '0'
else:
    examples = {f['name']: ('0' if 'int' in f['type'] else '0.0' if 'float' in f['type'] else 'value') for f in feature_schema}

# Merge examples into feature_schema
enriched = []
for f in feature_schema:
    enriched.append({'name': f['name'], 'type': f['type'], 'example': examples.get(f['name'], '0')})

print(json.dumps(enriched))
" > /tmp/doml_feature_schema_enriched.json
if [ $? -ne 0 ]; then
  echo "Failed to read example values from preprocessed data."
  exit 1
fi
ENRICHED_SCHEMA=$(cat /tmp/doml_feature_schema_enriched.json)
echo "Example values loaded."
```

### Step 5 — Check src/ volume mount and pyinstaller availability

Check that docker-compose.yml has the src/ volume mount:
```bash
if ! grep -q "./src:/home/jovyan/work/src" docker-compose.yml 2>/dev/null; then
  echo ""
  echo "SETUP REQUIRED: The src/ directory is not mounted in your Docker container."
  echo ""
  echo "Add this line to the volumes: block in docker-compose.yml (after the models mount):"
  echo "      - ./src:/home/jovyan/work/src"
  echo ""
  echo "Then run: docker compose down && docker compose up -d"
  echo ""
  echo "Re-run /doml-deploy-cli after the container restarts."
  exit 1
fi
```

Check that the container is running:
```bash
if ! docker compose ps jupyter 2>/dev/null | grep -q "Up\|running"; then
  echo "Jupyter container is not running. Start it with: docker compose up -d"
  exit 1
fi
```

Check pyinstaller is available in the container:
```bash
if ! docker compose exec jupyter pyinstaller --version 2>/dev/null; then
  echo ""
  echo "SETUP REQUIRED: pyinstaller is not installed in the Docker container."
  echo ""
  echo "1. Add 'pyinstaller' to requirements.in"
  echo "2. Run: docker compose run --rm jupyter pip-compile requirements.in"
  echo "3. Run: docker compose build"
  echo "4. Run: docker compose up -d"
  echo ""
  echo "Re-run /doml-deploy-cli after the image is rebuilt."
  exit 1
fi
echo "PyInstaller available in container."
```

### Step 6 — Generate predict.py

Derive the container path for the deployment directory (host ./src maps to /home/jovyan/work/src):
```bash
DEPLOY_DIR_CONTAINER="/home/jovyan/work/$(echo "$DEPLOY_DIR" | sed 's|^\./||')"
```

Read the enriched schema into Python for embedding:
```bash
python3 << 'PYEOF'
import json, os, sys

deploy_dir = os.environ.get('DEPLOY_DIR', '')
problem_type = os.environ.get('PROBLEM_TYPE', 'regression')
model_name = os.environ.get('MODEL_NAME', 'model')
version = os.environ.get('VERSION', 'v1')

with open('/tmp/doml_feature_schema_enriched.json') as f:
    enriched_schema = json.load(f)

# Build the feature schema epilog lines for argparse RawDescriptionHelpFormatter
epilog_lines = ["\nFeature schema (preprocessed-schema format):"]
for feat in enriched_schema:
    epilog_lines.append(f"  {feat['name']:<24} ({feat['type']:<12}  e.g. {feat['example']})")
epilog_lines.append("")
epilog_lines.append("Input must be in preprocessed format (same columns as data/processed/preprocessed_*).")
epilog_lines.append("The model pipeline handles encoding and scaling internally.")
feature_help_epilog = "\n".join(epilog_lines)

predict_py = f'''#!/usr/bin/env python3
"""
DoML prediction CLI — generated by /doml-deploy-cli
Model: {model_name}
Deployment: {version}
Platform: linux-x86_64
"""

import argparse
import json
import os
import sys

import joblib
import pandas as pd

# --- Injected at generation time (D-02, D-03) ---
PROBLEM_TYPE = {json.dumps(problem_type)}
MODEL_NAME = {json.dumps(model_name)}
FEATURE_SCHEMA = {json.dumps(enriched_schema)}

# --- Model loading (D-06, Pitfall 4) ---
# Bundle best_model.pkl as data file in spec; load from _MEIPASS when frozen
if getattr(sys, "frozen", False):
    _BASE = sys._MEIPASS
else:
    _BASE = os.path.dirname(os.path.abspath(__file__))
MODEL_PATH = os.path.join(_BASE, "best_model.pkl")


def _feature_help_epilog():
    lines = ["\\nFeature schema (preprocessed-schema format):"]
    for feat in FEATURE_SCHEMA:
        lines.append(f"  {{feat[\\'name\\']:<24}} ({{feat[\\'type\\']:<12}}  e.g. {{feat[\\'example\\']}})")
    lines.append("")
    lines.append("Input must be in preprocessed format (same columns as data/processed/preprocessed_*).")
    lines.append("The model pipeline handles encoding and scaling internally.")
    return "\\n".join(lines)


def _parse_args():
    parser = argparse.ArgumentParser(
        description=f"DoML model inference — {{MODEL_NAME}}",
        formatter_class=argparse.RawDescriptionHelpFormatter,
        epilog=_feature_help_epilog(),
    )
    parser.add_argument(
        "--input",
        required=True,
        help="JSON string \\'{{\\\"feature\\\": value}}\\' or path to .csv / .json file",
    )
    parser.add_argument(
        "--output",
        default=None,
        help="Output file path for batch predictions (CSV); if omitted, prints JSON to stdout",
    )
    return parser.parse_args()


def _load_input(input_str):
    """Attempt json.loads() first; fall back to file path detection (Specifics note)."""
    try:
        parsed = json.loads(input_str)
        if isinstance(parsed, dict):
            return pd.DataFrame([parsed]), "single"
        elif isinstance(parsed, list):
            return pd.DataFrame(parsed), "batch"
        else:
            raise ValueError("JSON input must be an object or array of objects")
    except (json.JSONDecodeError, ValueError) as json_err:
        if not os.path.isfile(input_str):
            raise ValueError(
                f"Input is neither valid JSON nor an existing file path: {{input_str}}"
            ) from json_err

    ext = os.path.splitext(input_str)[1].lower()
    if ext == ".csv":
        return pd.read_csv(input_str), "batch"
    elif ext == ".json":
        with open(input_str) as fh:
            data = json.load(fh)
        if isinstance(data, list):
            return pd.DataFrame(data), "batch"
        return pd.DataFrame([data]), "single"
    else:
        raise ValueError(f"Unsupported file extension \\'{{ext}}\\'; use .csv or .json")


def _json_safe(obj):
    """Convert numpy scalars to Python native types for JSON serialisation (Pitfall 5)."""
    if hasattr(obj, "item"):
        return obj.item()
    raise TypeError(f"Not JSON serialisable: {{type(obj)}}")


def _format_single(prediction, model, problem_type):
    """Format a single prediction result (D-04)."""
    result = {}
    if problem_type in ("regression", "time_series"):
        val = prediction[0]
        result["prediction"] = val.item() if hasattr(val, "item") else val
    elif problem_type in ("classification", "binary_classification", "multiclass_classification"):
        label = prediction[0]
        result["prediction"] = label.item() if hasattr(label, "item") else label
        if hasattr(model, "predict_proba"):
            proba = model.predict_proba(None)  # placeholder — called in main
            classes = model.classes_
            result["probabilities"] = {{
                str(cls): float(p)
                for cls, p in zip(classes, proba[0])
            }}
    elif problem_type == "clustering":
        val = prediction[0]
        result["prediction"] = int(val.item() if hasattr(val, "item") else val)
    else:
        val = prediction[0]
        result["prediction"] = val.item() if hasattr(val, "item") else val
    return result


def main():
    args = _parse_args()

    # --- Input validation (CLI-06: exit 1) ---
    try:
        df, input_type = _load_input(args.input)
    except (ValueError, KeyError, pd.errors.ParserError) as e:
        print(f"Input error: {{e}}", file=sys.stderr)
        sys.exit(1)

    # --- Model execution (CLI-06: exit 2) ---
    try:
        model = joblib.load(MODEL_PATH)
        predictions = model.predict(df)
        proba = None
        if PROBLEM_TYPE in ("classification", "binary_classification", "multiclass_classification"):
            if hasattr(model, "predict_proba"):
                proba = model.predict_proba(df)
    except Exception as e:
        print(f"Model error: {{e}}", file=sys.stderr)
        sys.exit(2)

    # --- Format output (D-04, D-05) ---
    if input_type == "single":
        result = {{}}
        val = predictions[0]
        result["prediction"] = val.item() if hasattr(val, "item") else val
        if proba is not None:
            classes = model.classes_
            result["probabilities"] = {{
                str(cls): float(p)
                for cls, p in zip(classes, proba[0])
            }}
        print(json.dumps(result, default=_json_safe))
    else:
        # Batch output (D-05): input columns + prediction + optional prob_<class> columns
        out_df = df.copy()
        out_df["prediction"] = [
            (v.item() if hasattr(v, "item") else v) for v in predictions
        ]
        if proba is not None:
            classes = model.classes_
            for i, cls in enumerate(classes):
                out_df[f"prob_{{cls}}"] = proba[:, i].tolist()

        if args.output:
            out_df.to_csv(args.output, index=False)
            print(f"Predictions written to {{args.output}} ({{len(out_df)}} rows)")
        else:
            print(out_df.to_json(orient="records", default_handler=str))

    sys.exit(0)


if __name__ == "__main__":
    main()
'''

out_path = os.path.join(deploy_dir, "predict.py")
with open(out_path, "w") as f:
    f.write(predict_py)
print(f"Generated: {out_path}")
PYEOF
export DEPLOY_DIR MODEL_NAME PROBLEM_TYPE VERSION
if [ $? -ne 0 ]; then
  echo "Failed to generate predict.py"
  exit 1
fi
```

### Step 7 — Generate predict.spec

```bash
python3 << 'PYEOF'
import json, os

deploy_dir = os.environ.get('DEPLOY_DIR', '')
model_file = os.environ.get('MODEL_FILE', 'models/best_model.pkl')

# Relative path from src/<modelname>/vN/ to the model file (three levels up to project root)
model_rel = os.path.relpath(model_file, deploy_dir)

spec_content = f"""# predict.spec — generated by /doml-deploy-cli
# D-01: --onedir packaging (faster startup, no temp-dir extraction)
# CLI-01: collects all sklearn + joblib C-extensions (Pitfall 3)

from PyInstaller.utils.hooks import collect_all

sklearn_datas, sklearn_binaries, sklearn_hiddenimports = collect_all('sklearn')
joblib_datas, joblib_binaries, joblib_hiddenimports = collect_all('joblib')

a = Analysis(
    ['predict.py'],
    pathex=[],
    binaries=sklearn_binaries + joblib_binaries,
    datas=sklearn_datas + joblib_datas + [('{model_rel}', '.')],
    hiddenimports=sklearn_hiddenimports + joblib_hiddenimports,
    hookspath=[],
    hooksconfig={{}},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
)

pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,   # REQUIRED for onedir — if False this becomes onefile
    name='predict',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)

coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='predict',   # produces dist/predict/ directory
)
"""

out_path = os.path.join(deploy_dir, "predict.spec")
with open(out_path, "w") as f:
    f.write(spec_content)
print(f"Generated: {out_path}")
PYEOF
export DEPLOY_DIR MODEL_FILE
if [ $? -ne 0 ]; then
  echo "Failed to generate predict.spec"
  exit 1
fi
```

### Step 8 — Run PyInstaller build inside Docker container

```bash
# Derive container-side path
DEPLOY_DIR_CONTAINER="/home/jovyan/work/$(echo "$DEPLOY_DIR" | sed 's|^\./||')"
echo "Building inside container at: $DEPLOY_DIR_CONTAINER"
echo "This may take 2-5 minutes..."

docker compose exec jupyter bash -c "cd '$DEPLOY_DIR_CONTAINER' && pyinstaller predict.spec --noconfirm"
BUILD_EXIT=$?

if [ $BUILD_EXIT -ne 0 ]; then
  echo ""
  echo "PyInstaller build failed (exit code $BUILD_EXIT)."
  echo ""
  echo "Common causes:"
  echo "  - sklearn C-extension missing: ensure predict.spec uses collect_all('sklearn')"
  echo "  - src/ not writable in container: check ./src:/home/jovyan/work/src mount in docker-compose.yml"
  echo "  - pyinstaller version mismatch: rebuild with docker compose build"
  echo ""
  echo "Re-run /doml-deploy-cli after fixing the issue."
  exit 1
fi
echo "PyInstaller build complete."
```

### Step 9 — Verify binary and update deployment_metadata.json (SC-7)

```bash
PREDICT_BIN="$DEPLOY_DIR/dist/predict/predict"
if [ ! -f "$PREDICT_BIN" ]; then
  echo "Build appeared to succeed but dist/predict/predict not found at $PREDICT_BIN"
  echo "Check PyInstaller output above for unexpected directory name."
  exit 1
fi

echo "Binary produced: $PREDICT_BIN"

python3 -c "
import json, os

deploy_dir = '${DEPLOY_DIR}'
meta_path = os.path.join(deploy_dir, 'deployment_metadata.json')

with open(meta_path) as f:
    meta = json.load(f)

meta['platform'] = 'linux-x86_64'
meta['binary_path'] = os.path.join(deploy_dir, 'dist', 'predict', 'predict')

with open(meta_path, 'w') as f:
    json.dump(meta, f, indent=2)

print(f'deployment_metadata.json updated with platform: linux-x86_64')
"
if [ $? -ne 0 ]; then
  echo "Failed to update deployment_metadata.json"
  exit 1
fi
```

### Step 10 — Update STATE.md and confirm

Read `.planning/STATE.md`. In the `### Decisions` section, append:
```
[Phase 14]: doml-deploy-cli completed — {MODEL_NAME} {VERSION} → dist/predict/predict (linux-x86_64)
```

Update `last_activity` frontmatter field with today's date (YYYY-MM-DD format).
Update `stopped_at` to: `Phase 14 CLI binary built — {MODEL_NAME} {VERSION}`.

Write the updated STATE.md in-place using the Read then Write tool pattern.

Display:
```
CLI binary build complete.

  Model:      {MODEL_FILE} ({MODEL_NAME})
  Version:    {VERSION}
  Binary:     {DEPLOY_DIR}/dist/predict/predict
  Platform:   linux-x86_64
  Packaging:  --onedir (dist/predict/ directory)

To run predictions:
  Single:   ./dist/predict/predict --input '{"feature": value, ...}'
  Batch:    ./dist/predict/predict --input data.csv --output results.csv
  Help:     ./dist/predict/predict --help

The binary is self-contained. Copy dist/predict/ to any Linux x86_64 machine and run it.
No Python installation required.

Next step: run /doml-deploy-web (Phase 15) or /doml-iterate-deployment.
```

## Anti-Patterns (do NOT do these)

- NEVER build with `--onefile` — D-01 locks `--onedir`; onefile is slower and less reliable
- NEVER omit `collect_all('sklearn')` from spec — C-extensions will be missing (Pitfall 3)
- NEVER build from the host — binary must match Linux x86_64 container architecture (D-06)
- NEVER assume src/ is mounted — Step 5 checks explicitly and instructs the user if not
- NEVER shell-interpolate untrusted user input — DEPLOY_DIR is validated via file existence check
- NEVER hardcode model path in predict.py — use sys._MEIPASS for frozen binary portability
- NEVER call predict_proba without hasattr check — not all estimators support it
```
  </action>
  <verify>
    <automated>grep -c "pyinstaller predict.spec" /home/bill/source/DoML/.claude/doml/workflows/deploy-cli.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.claude/doml/workflows/deploy-cli.md`
    - File contains `### Step 1 —` through `### Step 10 —` (10 steps)
    - File contains `pyinstaller predict.spec --noconfirm` (the build command)
    - File contains `collect_all('sklearn')` and `collect_all('joblib')` in the spec template
    - File contains `exclude_binaries=True` in the EXE() call (required for onedir)
    - File contains `COLLECT(` call (required to produce the dist/predict/ directory)
    - File contains `sys._MEIPASS` (frozen binary model path resolution)
    - File contains `sys.exit(1)` and `sys.exit(2)` (CLI-06 exit codes)
    - File contains `predict_proba` (D-04 classification probabilities)
    - File contains `prob_{cls}` (D-05 batch probability columns)
    - File contains `"platform": "linux-x86_64"` (SC-7 metadata update)
    - File contains `./src:/home/jovyan/work/src` check (Pitfall 1 guard)
    - File contains `--deploy-dir` flag parsing (Step 2)
    - File contains `deployment_metadata.json` target validation (`target != 'cli_binary'` check)
    - File does NOT contain `--onefile` as a recommended option
  </acceptance_criteria>
  <done>deploy-cli.md workflow exists with all 10 steps, full predict.py template, full spec template, Docker build command, and deployment_metadata.json platform update</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `--input` flag (user/external data) → model.predict() | User-supplied input crosses from untrusted CLI args into the model; malformed data could cause unhandled exceptions |
| `--deploy-dir` flag → filesystem path | User-supplied path is used to open files; path traversal could access arbitrary host files |
| `--input` file path → filesystem read | User-supplied file path is opened for batch input; path traversal possible |
| Docker container → host `src/` mount | Host files under `src/` are writable from inside the container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | predict.py `--input` JSON string | mitigate | Input is passed to `json.loads()` then `pd.DataFrame()`; only feature keys the model expects are used; extra keys are silently ignored by ColumnTransformer; validated by try/except with exit 1 |
| T-14-02-02 | Denial of Service | predict.py `--input` large CSV file | accept | DoML is a data science tool, not a public API; operator controls what files are passed; no request-rate concern |
| T-14-02-03 | Information Disclosure | predict.py exits with model error traceback on stderr | mitigate | deploy-cli.md template uses `print(f"Model error: {e}", file=sys.stderr)` — exposes only the exception message, not a full traceback (sys.exit(2) short-circuits traceback printing) |
| T-14-02-04 | Elevation of Privilege | `--deploy-dir` path traversal | mitigate | Step 2 validates path via `os.path.isfile(deploy_dir + '/deployment_metadata.json')` before any file read; relative paths only (no absolute paths accepted via the flag); if the metadata file is absent, the workflow stops |
| T-14-02-05 | Tampering | docker-compose exec injects DEPLOY_DIR_CONTAINER into shell | mitigate | DEPLOY_DIR_CONTAINER is derived from DEPLOY_DIR which is validated to have deployment_metadata.json; the `cd` is single-quoted in the bash -c string to prevent word splitting; workflow Anti-Patterns section explicitly forbids shell-interpolating untrusted input |
| T-14-02-06 | Repudiation | No audit of what model was used | accept | deployment_metadata.json records model_file, model_name, build_date, version; sufficient traceability for DoML's scope (REPR requirements) |
| T-14-02-07 | Information Disclosure | best_model.pkl bundled inside binary | accept | Model is already on the host filesystem (models/); bundling into binary does not increase exposure beyond what the model author controls; DoML has no multi-tenant model hosting |
</threat_model>

<verification>
After both tasks complete:

1. Skill exists: `ls .claude/skills/doml-deploy-cli/SKILL.md`
2. Workflow exists: `ls .claude/doml/workflows/deploy-cli.md`
3. SKILL.md has correct name: `grep "name: doml-deploy-cli" .claude/skills/doml-deploy-cli/SKILL.md`
4. Workflow has all 10 steps: `grep -c "^### Step" .claude/doml/workflows/deploy-cli.md` returns 10
5. Spec template uses collect_all: `grep "collect_all" .claude/doml/workflows/deploy-cli.md` matches sklearn and joblib
6. Exit codes present: `grep -c "sys.exit" .claude/doml/workflows/deploy-cli.md` returns ≥ 3 (0, 1, 2)
7. Platform update present: `grep "linux-x86_64" .claude/doml/workflows/deploy-cli.md`
8. Anti-Patterns section present: `grep "Anti-Patterns" .claude/doml/workflows/deploy-cli.md`
9. predict_proba present: `grep "predict_proba" .claude/doml/workflows/deploy-cli.md`
10. MEIPASS present: `grep "_MEIPASS" .claude/doml/workflows/deploy-cli.md`
</verification>

<success_criteria>
- `/doml-deploy-cli` is recognised as an invokable Claude Code command
- SKILL.md references the correct workflow file
- deploy-cli.md implements all 10 steps: locate deploy dir, read metadata, read examples, check docker, generate predict.py, generate predict.spec, run PyInstaller, verify binary, update metadata with platform, update STATE.md + confirm
- predict.py template satisfies CLI-01 through CLI-06:
  - CLI-01: binary runs on Linux with no Python (via PyInstaller onedir)
  - CLI-02: `--input '{"f": v}'` JSON string → single prediction to stdout
  - CLI-03: `--input data.csv` and `--input data.json` → batch predictions
  - CLI-04: `--output path.csv` writes batch results to file
  - CLI-05: `--help` shows feature names, types, example values
  - CLI-06: exits 0/1/2 for success/input error/model error
- deployment_metadata.json gains `"platform": "linux-x86_64"` after successful build (SC-7)
</success_criteria>

<output>
After completion, create `.planning/phases/14-cli-binary-target/14-02-SUMMARY.md`
</output>
