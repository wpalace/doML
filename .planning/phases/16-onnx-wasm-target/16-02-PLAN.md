---
phase: 16-onnx-wasm-target
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - .claude/doml/templates/requirements.in
  - .claude/doml/workflows/progress.md
autonomous: true
requirements:
  - WASM-01

must_haves:
  truths:
    - "skl2onnx appears in .claude/doml/templates/requirements.in under a Deployment tools comment"
    - "progress.md Step 5 routing table mentions /doml-deploy-wasm as a valid deployment option"
  artifacts:
    - path: ".claude/doml/templates/requirements.in"
      provides: "skl2onnx dependency for new projects"
      contains: "skl2onnx"
    - path: ".claude/doml/workflows/progress.md"
      provides: "Updated routing to surface /doml-deploy-wasm as next action"
      contains: "doml-deploy-wasm"
  key_links:
    - from: ".claude/doml/templates/requirements.in"
      to: "requirements.in (in analysis projects)"
      via: "/doml-new-project copies this template on project creation"
      pattern: "skl2onnx"
---

<objective>
Two small infrastructure updates that enable the ONNX/WASM target for new DoML projects:

1. Add `skl2onnx` to `.claude/doml/templates/requirements.in` so new projects generated by
   `/doml-new-project` include it in their Docker environment (same pattern as Phase 14 adding `pyinstaller`)

2. Update `.claude/doml/workflows/progress.md` Step 5 routing table to mention `/doml-deploy-wasm`
   as a valid deployment option alongside the CLI and web service targets
</objective>

<execution_context>
@/home/bill/source/DoML/.claude/get-shit-done/workflows/execute-plan.md
@/home/bill/source/DoML/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-onnx-wasm-target/16-CONTEXT.md
@.claude/doml/templates/requirements.in
@.claude/doml/workflows/progress.md
</context>

<interfaces>
## Current requirements.in tail (Deployment tools section):
```
# --- Deployment tools ---
pyinstaller
```

## Target requirements.in tail (after this plan):
```
# --- Deployment tools ---
pyinstaller
skl2onnx
```

## Current progress.md Step 5 routing (relevant rows):
```
| Deployment not yet run (no src/ directory) | Run `/doml-deploy-model` to scaffold the deployment directory for the best leaderboard model. Choose from CLI binary, web service, or ONNX/WASM targets. |
```

## Target progress.md Step 5 routing (after this plan):
The row is already correct — it mentions "ONNX/WASM targets". Verify it is present as-is.
If the row instead says only "CLI binary" or "web service" without mentioning ONNX/WASM, update it to:
```
| Deployment not yet run (no src/ directory) | Run `/doml-deploy-model` to scaffold the deployment directory for the best leaderboard model. Choose from CLI binary (`/doml-deploy-cli`), web service (`/doml-deploy-web`), or ONNX/WASM (`/doml-deploy-wasm`) targets. |
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add skl2onnx to .claude/doml/templates/requirements.in</name>
  <files>.claude/doml/templates/requirements.in</files>
  <read_first>
    - .claude/doml/templates/requirements.in — read full file; locate the "# --- Deployment tools ---" section; confirm pyinstaller is present; insert skl2onnx after it
  </read_first>
  <action>
Read `.claude/doml/templates/requirements.in`.

Locate the `# --- Deployment tools ---` section and the `pyinstaller` line below it.

Add `skl2onnx` on a new line immediately after `pyinstaller`:

**Before:**
```
# --- Deployment tools ---
pyinstaller
```

**After:**
```
# --- Deployment tools ---
pyinstaller
skl2onnx
```

No other lines in the file are changed.
  </action>
  <verify>
    <automated>grep -c "skl2onnx" /home/bill/source/DoML/.claude/doml/templates/requirements.in</automated>
  </verify>
  <acceptance_criteria>
    - `skl2onnx` appears in requirements.in on its own line
    - `skl2onnx` is under the `# --- Deployment tools ---` section
    - `pyinstaller` is still present and unchanged
    - All other lines in requirements.in are unchanged
    - No trailing whitespace or blank lines introduced in the deployment section
  </acceptance_criteria>
  <done>skl2onnx added to requirements.in template</done>
</task>

<task type="auto">
  <name>Task 2: Verify (and update if needed) progress.md Step 5 routing for /doml-deploy-wasm</name>
  <files>.claude/doml/workflows/progress.md</files>
  <read_first>
    - .claude/doml/workflows/progress.md — read Step 5 routing table; check whether ONNX/WASM and /doml-deploy-wasm are already mentioned in the deployment row
  </read_first>
  <action>
Read `.claude/doml/workflows/progress.md`.

Locate Step 5 (`### Step 5 — Determine next action`) and the routing table within it.

Find the row whose condition is about deployment not yet run. It should already mention "ONNX/WASM targets" (added when the progress.md was last updated).

**If the row already contains "/doml-deploy-wasm" or mentions all three deployment targets explicitly:**
No change needed — mark task complete.

**If the row does NOT mention ONNX/WASM or /doml-deploy-wasm:**
Update that row's "Next action message" column to read:

```
Run `/doml-deploy-model` to scaffold the deployment directory for the best leaderboard model. Choose from CLI binary (`/doml-deploy-cli`), web service (`/doml-deploy-web`), or ONNX/WASM (`/doml-deploy-wasm`) targets.
```

All other rows in the routing table and all other sections of progress.md are unchanged.
  </action>
  <verify>
    <automated>grep -c "doml-deploy-wasm\|ONNX/WASM" /home/bill/source/DoML/.claude/doml/workflows/progress.md</automated>
  </verify>
  <acceptance_criteria>
    - progress.md Step 5 routing table contains at least one reference to "doml-deploy-wasm" or "ONNX/WASM"
    - The deployment routing row mentions all three deployment options (CLI, web service, ONNX/WASM)
    - No other rows or sections in progress.md are changed
  </acceptance_criteria>
  <done>progress.md updated to surface /doml-deploy-wasm as deployment option</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirements.in template → new project requirements.in | Adding skl2onnx to the template means new projects will include it; no security implications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | Supply Chain | skl2onnx added as unpinned dep in requirements.in | accept | requirements.in is intentionally unpinned (pinned at pip-compile time per REPR-04); same pattern as all other deps in this file |
</threat_model>

<verification>
After both tasks complete:

1. `grep "skl2onnx" .claude/doml/templates/requirements.in` — returns one match on its own line
2. `grep "pyinstaller" .claude/doml/templates/requirements.in` — still present (not removed)
3. `grep "doml-deploy-wasm\|ONNX/WASM" .claude/doml/workflows/progress.md` — returns at least one match
4. File structure of requirements.in is intact: `grep "# ---" .claude/doml/templates/requirements.in` — all section headers still present
</verification>

<success_criteria>
- New projects created by `/doml-new-project` will have `skl2onnx` in their `requirements.in` and can run ONNX conversion inside Docker without any manual package installation
- `/doml-progress` will surface `/doml-deploy-wasm` as a deployment option after modelling is complete
</success_criteria>

<output>
After completion, create `.planning/phases/16-onnx-wasm-target/16-02-SUMMARY.md`
</output>
