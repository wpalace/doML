---
phase: 17-performance-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.in
  - .claude/doml/templates/notebooks/deployment_report.ipynb
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-05
  - PERF-06
  - PERF-07

must_haves:
  truths:
    - "deployment_report.ipynb template exists and can be executed by nbconvert without error for any target"
    - "Single-prediction latency benchmark runs 5 warm-up then 1000 timed iterations, reports mean and std in ms"
    - "Batch latency benchmark covers BATCH_SIZES = [10, 100, 1000, 10_000]"
    - "Parity test raises AssertionError (halting notebook) when deployed predictions diverge from in-memory model"
    - "Model load and per-prediction memory deltas captured via psutil RSS"
    - "Throughput projection computed as 1000.0 / mean_latency_ms req/s"
    - "Cell 12 is a placeholder markdown cell ready for narrative injection by the workflow"
    - "onnxruntime is listed in requirements.in so it is available inside Docker"
  artifacts:
    - path: "requirements.in"
      provides: "onnxruntime dependency declaration"
      contains: "onnxruntime"
    - path: ".claude/doml/templates/notebooks/deployment_report.ipynb"
      provides: "Benchmark notebook template (nbformat v4)"
      exports: ["13 cells: md header + 11 code cells + md placeholder"]
  key_links:
    - from: "Cell 2 (metadata load)"
      to: "deployment_metadata.json"
      via: "json.load — extracts target, model_file, feature_schema"
      pattern: "deployment_metadata\\.json"
    - from: "Cell 5 (target routing)"
      to: "DOML_WEB_SERVICE_URL env var"
      via: "os.environ.get('DOML_WEB_SERVICE_URL', 'http://localhost:8080')"
      pattern: "DOML_WEB_SERVICE_URL"
    - from: "Cell 8 (parity test)"
      to: "joblib pipeline in-memory"
      via: "pipeline.predict(test_df)"
      pattern: "pipeline\\.predict"
---

<objective>
Write the `deployment_report.ipynb` notebook template as a static nbformat v4 JSON file and add
`onnxruntime` to `requirements.in` so the ONNX/WASM benchmark cells work inside Docker.

Purpose: Provide the executable notebook template that the deploy-model.md Step 12 workflow
will copy, execute, and convert to HTML after every deployment.

Output:
- `requirements.in` with `onnxruntime` added
- `.claude/doml/templates/notebooks/deployment_report.ipynb` — 13-cell nbformat v4 notebook
</objective>

<execution_context>
@/home/bill/source/DoML/.claude/get-shit-done/workflows/execute-plan.md
@/home/bill/source/DoML/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/bill/source/DoML/.planning/PROJECT.md
@/home/bill/source/DoML/.planning/ROADMAP.md
@/home/bill/source/DoML/.planning/REQUIREMENTS.md
@/home/bill/source/DoML/.planning/phases/17-performance-report/17-CONTEXT.md
@/home/bill/source/DoML/.planning/phases/17-performance-report/17-RESEARCH.md
@/home/bill/source/DoML/.planning/phases/17-performance-report/17-VALIDATION.md

<interfaces>
<!-- Key contracts from upstream phases that the notebook cells must conform to. -->

From deployment_metadata.json (written by deploy-model.md Step 9):
```json
{
  "model_file": "models/best_model.pkl",
  "model_name": "XGBRegressor",
  "target": "cli_binary | web_service | onnx_wasm",
  "problem_type": "regression | classification | clustering",
  "build_date": "<ISO 8601>",
  "version": "v1",
  "feature_schema": [
    {"name": "col", "type": "float64", "example": "1.0", "categories": null},
    {"name": "cat_col", "type": "object", "example": "A", "categories": ["A", "B", "C"]}
  ]
}
```

From model_metadata.json (written by /doml-modelling):
```json
{
  "model_file": "models/best_model.pkl",
  "model_name": "XGBRegressor",
  "problem_type": "regression",
  "feature_names": ["col1", "col2"]
}
```

CLI binary invocation (Phase 14 output):
- Binary path: `src/<modelname>/vN/dist/predict/predict`
- Single: `predict --input '{"col": 1.0}'` -> prints JSON to stdout
- Batch: loop over rows calling run_single()

Web service invocation (Phase 15 output, D-02 revised):
- Container started/stopped by workflow (Step 12), NOT inside notebook
- Notebook receives `DOML_WEB_SERVICE_URL` env var (default http://localhost:8080)
- Single: `POST /predict` with `{"col": 1.0}` JSON body
- Health endpoint: `GET /health` -> `{"status": "ok"}`

ONNX model (Phase 16 output):
- Path: `src/<modelname>/vN/model.onnx`
- category_map reconstructed from feature_schema.categories in deployment_metadata.json
- Input: numpy float32 array, column order from feature_schema
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add onnxruntime to requirements.in</name>
  <files>requirements.in</files>
  <action>
Read requirements.in. Find the ML stack section (where scikit-learn, xgboost, lightgbm, etc. are
listed). Add `onnxruntime` on its own line in that section (per research finding A2 — onnxruntime
is confirmed absent from requirements.in and requirements.txt).

Do NOT run pip-compile — that requires Docker and is the user's responsibility per CLAUDE.md REPR-04.
Add a comment after the line: `# ONNX runtime for WASM target benchmarking (Phase 17 — PERF-01)`
  </action>
  <verify>
    <automated>grep -c 'onnxruntime' /home/bill/source/DoML/requirements.in</automated>
  </verify>
  <done>requirements.in contains `onnxruntime` in the ML stack section with a comment. File is not otherwise modified.</done>
</task>

<task type="auto">
  <name>Task 2: Write deployment_report.ipynb notebook template</name>
  <files>.claude/doml/templates/notebooks/deployment_report.ipynb</files>
  <action>
Write `.claude/doml/templates/notebooks/deployment_report.ipynb` as a valid nbformat v4 JSON file.
Use the Write tool with the complete JSON content.

The notebook MUST follow REPR-01 (SEED = 42) and REPR-02 (PROJECT_ROOT from env var).

Cell layout — 13 cells:

CELL 0 (markdown): Header
  source: "# Deployment Performance Report\n\n*Generated by DoML — Do Machine Learning*"

CELL 1 (code): Imports + REPR-01 + REPR-02 + WEB_SERVICE_URL
  - import os, random, json, glob, time, subprocess, warnings
  - import numpy as np, pandas as pd, psutil, joblib
  - import matplotlib (Agg backend), matplotlib.pyplot as plt
  - from pathlib import Path
  - from IPython.display import Markdown, display
  - SEED = 42; random.seed(SEED); np.random.seed(SEED)
  - PROJECT_ROOT = Path(os.environ.get('PROJECT_ROOT', '/home/jovyan/work'))
  - WEB_SERVICE_URL = os.environ.get('DOML_WEB_SERVICE_URL', 'http://localhost:8080')
  - warnings.filterwarnings('ignore')
  - print PROJECT_ROOT and WEB_SERVICE_URL

CELL 2 (code): Load deployment_metadata.json
  - glob src/*/*/deployment_metadata.json, take last (meta_files[-1])
  - Raise FileNotFoundError if no file found (clear message)
  - Extract: target, model_file, feature_schema, problem_type, model_name, version
  - resolve model_path = PROJECT_ROOT / model_file
  - Reconstruct category_map: {name: categories} for entries where categories is not None
  - Print all extracted values

CELL 3 (code): Load test data (D-06)
  - glob data/processed/preprocessed_*.csv, take last
  - Raise FileNotFoundError if none found
  - pd.read_csv, tail(100), reset_index
  - Select columns per feature_schema (available intersection)
  - print shape and source path; display head(3)

CELL 4 (code): Load in-memory model + load memory delta (PERF-05)
  - process = psutil.Process()
  - rss_before_load = process.memory_info().rss
  - pipeline = joblib.load(model_path)
  - rss_after_load = process.memory_info().rss
  - model_load_memory_mb = (rss_after_load - rss_before_load) / (1024**2)
  - print model path and load memory delta in MB

CELL 5 (code): Target routing — define run_single(row) and run_batch(df)
  This cell has THREE branches gated on `target` (the variable from Cell 2):

  if target == 'cli_binary':
    - binary_path = deploy_dir / 'dist' / 'predict' / 'predict'
    - Raise FileNotFoundError if not exists
    - run_single(row): subprocess.run([binary, '--input', json.dumps(row)], capture_output=True,
      text=True, check=True) -> json.loads(result.stdout.strip())
    - run_batch(df): [run_single(r.to_dict()) for _, r in df.iterrows()]

  elif target == 'web_service':
    - import requests
    - Verify health: requests.get(WEB_SERVICE_URL + '/health', timeout=5), raise_for_status()
    - If health check fails, raise RuntimeError with clear message that workflow should have started container
    - run_single(row): requests.post(WEB_SERVICE_URL + '/predict', json=row, timeout=30).json()
    - run_batch(df): [run_single(r.to_dict()) for _, r in df.iterrows()]
    - NO docker or subprocess calls — container managed by workflow

  elif target == 'onnx_wasm':
    - import onnxruntime as _ort
    - onnx_path = deploy_dir / 'model.onnx'; raise FileNotFoundError if missing
    - _sess = _ort.InferenceSession(str(onnx_path))
    - _input_name = _sess.get_inputs()[0].name
    - _encode_row(row): encode dict to float32 array using category_map ordinal encoding
      (for categorical features: index in categories list; for numeric: float(val))
    - run_single(row): _sess.run(None, {_input_name: _encode_row(row)})[0].tolist()
    - run_batch(df): [run_single(r.to_dict()) for _, r in df.iterrows()]
    - print note: "ONNX target: benchmarked via Python onnxruntime (approximation). Browser performance may vary."

  else:
    - raise ValueError(f"Unknown target: {target}")

  print(f"Target routing configured: {target}")

CELL 6 (code): Single-prediction latency benchmark (PERF-02)
  - WARMUP_RUNS = 5; TIMED_RUNS = 1000
  - sample_row = test_df.iloc[0].to_dict()
  - warm-up loop (not timed)
  - timed loop: time.perf_counter() before/after each run_single(sample_row)
  - latencies_ms = [(t1-t0)*1000 for each run]
  - mean_latency_ms, std_latency_ms = np.mean(...), np.std(...)
  - p50, p95, p99 = np.percentile(..., [50, 95, 99])
  - Print mean +/- std and percentiles
  - Plot histogram (steelblue, 50 bins) with axvline at mean; save to notebooks/latency_distribution.png

CELL 7 (code): Batch latency benchmark (PERF-03)
  - BATCH_SIZES = [10, 100, 1000, 10_000]
  - For each bs: repeat test_df to fill bs rows, time run_batch(), compute total_ms and per_item_ms
  - Print each result; display DataFrame of results

CELL 8 (code): Parity test — HARD AssertionError (PERF-04)
  - in_memory_preds = pipeline.predict(test_df)
  - deployed_raw = run_batch(test_df)
  - _flatten(raw): handle dict (extract 'prediction' or 'result' key or first value),
    list (unwrap single-element), or scalar
  - deployed_preds = _flatten(deployed_raw) as np.array
  - if problem_type == 'regression':
      assert np.allclose(in_memory_preds, deployed_preds, atol=1e-4), f"PARITY FAILURE..."
      print f"Parity PASSED (regression): max abs diff = ..."
  - elif problem_type in ('classification', 'binary_classification', 'multiclass_classification'):
      assert np.array_equal(in_memory_preds.astype(str), deployed_preds.astype(str))
      print f"Parity PASSED (classification): all N labels match"
  - elif problem_type == 'clustering':
      assert np.array_equal(in_memory_preds.astype(str), deployed_preds.astype(str))
      print f"Parity PASSED (clustering): all N cluster IDs match"
  - else:
      print f"Parity test skipped for problem_type=..."

CELL 9 (code): Per-prediction memory delta (PERF-05)
  - MEMORY_RUNS = 10
  - Loop: rss_before, run_single(sample_row), rss_after; delta_mb = (rss_after - rss_before)/(1024**2)
  - mean_pred_memory_mb = np.mean(memory_deltas_mb)
  - Print model_load_memory_mb and mean_pred_memory_mb

CELL 10 (code): Throughput projection (PERF-06)
  - throughput_rps = 1000.0 / mean_latency_ms   (uses mean_latency_ms from Cell 6)
  - Print projected throughput and note about concurrency

CELL 11 (code): Summary table
  - Build dict with metrics: target, model, model load MB, mean/std/p95 latency ms,
    per-prediction memory MB, throughput req/s, parity result
  - display(pd.DataFrame(summary).style.hide(axis='index'))
  - Print batch results DataFrame

CELL 12 (markdown): Narrative placeholder
  source: "## Performance Summary\n\n*This section will be replaced by a Claude-generated narrative by the deploy-model.md Step 12 workflow.*"

JSON format rules:
- Top-level: {"nbformat": 4, "nbformat_minor": 5, "metadata": {...}, "cells": [...]}
- kernelspec: {"display_name": "Python 3", "language": "python", "name": "python3"}
- language_info: {"name": "python", "version": "3.11.0"}
- Code cells: {"cell_type": "code", "execution_count": null, "id": "<8-hex-chars>", "metadata": {}, "outputs": [], "source": [...]}
- Markdown cells: {"cell_type": "markdown", "id": "<8-hex-chars>", "metadata": {}, "source": [...]}
- source is a JSON array of strings; each line ends with "\n" EXCEPT the last line
- All cell ids must be unique 8-character hex strings

CRITICAL CONSTRAINTS:
- No `import anthropic` anywhere (D-04: narrative is workflow-injected)
- No docker/subprocess docker calls in web_service branch (D-02 revised: container managed by workflow)
- REPR-01: SEED = 42 in Cell 1
- REPR-02: PROJECT_ROOT = Path(os.environ.get('PROJECT_ROOT', '/home/jovyan/work')) in Cell 1
- Cell 12 exact placeholder text must be: "## Performance Summary\n\n*This section will be replaced by a Claude-generated narrative by the deploy-model.md Step 12 workflow.*"
  </action>
  <verify>
    <automated>python3 -c "import json; nb=json.load(open('/home/bill/source/DoML/.claude/doml/templates/notebooks/deployment_report.ipynb')); assert nb['nbformat']==4; assert len(nb['cells'])==13; code_cells=[c for c in nb['cells'] if c['cell_type']=='code']; assert len(code_cells)==11; print('nbformat v4 OK, 13 cells, 11 code cells')"</automated>
  </verify>
  <done>
- `.claude/doml/templates/notebooks/deployment_report.ipynb` exists as valid nbformat v4 JSON
- 13 cells present: cells[0] and cells[12] are markdown, cells[1..11] are code
- Cell 1 contains SEED = 42 and PROJECT_ROOT from os.environ
- Cell 2 loads deployment_metadata.json and reconstructs category_map
- Cell 5 has if/elif for cli_binary / web_service / onnx_wasm with no docker subprocess calls
- Cell 6 performs 5 warm-up + 1000 timed runs, computes mean and std
- Cell 7 covers BATCH_SIZES = [10, 100, 1000, 10_000]
- Cell 8 raises AssertionError on parity failure for all three problem types
- Cell 12 is the exact placeholder markdown string
- python3 validation command exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notebook -> filesystem | Reads deployment_metadata.json and model artifacts from project directories |
| notebook -> web service | HTTP POST to localhost:8080 (container managed externally by workflow) |
| notebook -> CLI binary | subprocess call to local binary resolved from deploy_dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | deployment_metadata.json | accept | Internal tool; file written by trusted workflow in same session. No user-supplied input path. |
| T-17-02 | Information Disclosure | model artifacts in src/ | accept | Local filesystem only; no PII in benchmark data. Data is post-preprocessing and used for timing. |
| T-17-03 | Denial of Service | BATCH_SIZES = [10_000] HTTP loop | accept | Internal benchmarking only; no public endpoint. 10k rows against local container is bounded. |
| T-17-04 | Elevation of Privilege | subprocess CLI binary | accept | Binary path resolved from deploy_dir (written by trusted workflow); not user-supplied input. |
</threat_model>

<verification>
After both tasks complete:

1. `grep -c 'onnxruntime' /home/bill/source/DoML/requirements.in` returns >= 1
2. `python3 -c "import json; nb=json.load(open('/home/bill/source/DoML/.claude/doml/templates/notebooks/deployment_report.ipynb')); assert nb['nbformat']==4; assert len(nb['cells'])==13; print('OK')"` exits 0
3. `grep -c 'anthropic' /home/bill/source/DoML/.claude/doml/templates/notebooks/deployment_report.ipynb` returns 0
4. `grep -c 'docker' /home/bill/source/DoML/.claude/doml/templates/notebooks/deployment_report.ipynb` returns 0
5. `grep -c 'DOML_WEB_SERVICE_URL' /home/bill/source/DoML/.claude/doml/templates/notebooks/deployment_report.ipynb` returns >= 1
6. `grep -c 'AssertionError' /home/bill/source/DoML/.claude/doml/templates/notebooks/deployment_report.ipynb` returns >= 1
</verification>

<success_criteria>
- requirements.in includes `onnxruntime`
- deployment_report.ipynb is valid nbformat v4 with exactly 13 cells (11 code + 2 markdown)
- Notebook reads all config from deployment_metadata.json at runtime — no hardcoded paths
- Web service cells assume container is already running — no docker subprocess calls
- Parity cell raises AssertionError on failure (causes nbconvert to exit non-zero)
- Cell 12 placeholder text is present for workflow narrative injection
- No anthropic SDK usage in notebook (narrative injected by workflow per D-04)
- REPR-01 and REPR-02 compliant
</success_criteria>

<output>
After completion, create `.planning/phases/17-performance-report/17-01-SUMMARY.md`
</output>
