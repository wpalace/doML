---
phase: 22-pre-flight-wheel-validation-lockfile-bootstrap
status: complete
completed: 2026-04-30
python_version: 3.14
scipy_notebook_tag: 2026-04-27
auto_fallback_fired: false
go_no_go: GO
---

# Phase 22 Summary — Pre-flight Wheel Validation & Lockfile Bootstrap

**Decision: GO** — pre-flight gate green; Phase 23 (Dockerfile rebuild + uv migration) is cleared to proceed against the resolved Python version (Python 3.14) and scipy-notebook tag (`2026-04-27`).

**Python version: 3.14** (locked at end of Phase 22 pre-flight via auto-fallback rig per D-22-06/D-22-07)

## Resolved Targets

| Target | Value |
|--------|-------|
| Python | 3.14 |
| Base image | `quay.io/jupyter/scipy-notebook:2026-04-27` |
| uv | 0.11.8 (per STACK.md, vendored from ghcr.io/astral-sh/uv:0.11.8 in Phase 23) |

## Auto-fallback Record (D-22-07)

- Python 3.14 attempt: **PASS** (compile + install + import smoke all green on first try)
- Python 3.13 attempt: not needed
- **Auto-fallback fired:** No

Pre-flight rig executed `uv pip compile --python-version 3.14 --generate-hashes` (resolved 100 packages in 560ms), then `uv pip install --system` (45 packages installed in 20.4s prepared + 0.3s installed) inside a throwaway `quay.io/jupyter/scipy-notebook:2026-04-27` container, then ran the 9-import smoke (`duckdb, papermill, shap, prophet, lightgbm, xgboost, onnxruntime, optuna, umap`) — all imports succeeded with exit 0. Auto-fallback to 3.13 was therefore not invoked.

## Deliverables

- [x] `.claude/doml/templates/requirements.in` — ydata-profiling removed (commit 1, plan 22-01)
- [x] `.claude/doml/templates/requirements.txt` — ydata-profiling==4.18.1 pin removed (commit 1, plan 22-01)
- [x] `.claude/doml/templates/notebooks/data_understanding_python.ipynb` — DuckDB SUMMARIZE cell added (commit 1, plan 22-01)
- [x] `requirements.in` (root) — pip-tools removed, numpy<2.4 added, mistune<3 retained, header references uv pip compile (commit 2, plan 22-02)
- [x] `requirements.txt` (root) — regenerated by `uv pip compile … --generate-hashes` (1959 lines, 1644 sha256 hashes, 100 packages) (commit 2, plan 22-02)
- [x] `Dockerfile` (root) — FROM line bumped to `quay.io/jupyter/scipy-notebook:2026-04-27` (commit 3, plan 22-03)
- [x] `.claude/doml/templates/Dockerfile` — FROM line bumped to `quay.io/jupyter/scipy-notebook:2026-04-27` (commit 3, plan 22-03)
- [x] `22-AUDIT.md` — mamba list (387 lines) + conflict diff (4 packages) + uv pip compile diff (2097 lines) (commit 3, plan 22-03)
- [x] `22-SUMMARY.md` — this document (commit 3, plan 22-03)
- [x] `PROJECT.md` D-03 updated to reflect resolved Python version (3.14) (commit 3, plan 22-03)
- [x] `REQUIREMENTS.md` PY-FUT-01 removed per D-22-08 (3.14 won; follow-up requirement resolved) (commit 3, plan 22-03)

## Requirements Coverage (PY-01..PY-05, CONT-06)

| Requirement | Disposition |
|-------------|-------------|
| PY-01 (Python 3.13.x pinned) | **Upgraded to Python 3.14** — pre-flight cleared 3.14 first try; D-03 re-locked from 3.13 → 3.14 per D-22-06; PY-FUT-01 retired per D-22-08 |
| PY-02 (cp31x wheels, no source builds) | Satisfied — verified by `uv pip compile` + install in throwaway container; all 100 packages resolved cleanly |
| PY-03 (mistune<3 retained) | Satisfied — pin preserved in `requirements.in` (root); resolved transitively in template lockfile |
| PY-04 (pip-tools removed) | Satisfied — removed from `requirements.in` (root); regenerate header rewritten to reference `uv pip compile` |
| PY-05 (numpy<2.4 pinned) | Satisfied — added to `requirements.in` (root) ML-stack block to prevent ABI breakage from numpy 2.4 |
| CONT-06 (uv pip compile --generate-hashes) | Satisfied — root `requirements.txt` has 1644 sha256 hashes spread across 100 resolved packages |

## Phase Boundary Discipline

Per CONTEXT.md `<specifics>`, this phase touched ONLY the Dockerfile FROM line. The install layer (`mamba install r-*` block, `pip install` line, kaggle-CLI layer in template) is untouched and remains Phase 23's scope. The line-1 comment (`# DoML analysis environment — Python 3.13 + R 4.x + DuckDB + ML stack`) is also untouched — Phase 23/24 will sweep it during the install-layer rewrite and R removal respectively.

Verified by `git diff --stat Dockerfile .claude/doml/templates/Dockerfile` showing exactly 1 insertion + 1 deletion per file (single FROM-line replacement).

## Decisions Made

- **D-22-06 outcome:** Python 3.14 wins pre-flight first try; D-03 re-locked from 3.13 → 3.14 in this phase
- **D-22-08 outcome:** PY-FUT-01 (Python 3.14 follow-up) retired entirely from REQUIREMENTS.md Future Requirements (no longer pending)
- **D-22-13 / D-22-15 outcome:** scipy-notebook dated tag resolved to `2026-04-27` via image-id matching against `:latest` (image content hash invariant across tag references)
- **D-22-14 phase boundary discipline:** ONLY the Dockerfile FROM line edited; mamba install r-* block, pip install line, kaggle layer all preserved verbatim — Phase 23 owns those
- **D-22-16 / D-22-17:** 22-AUDIT.md is a single Markdown file with three required H2 sections (Mamba List / Conflict Diff / uv pip compile Diff), no `audit/` subdirectory
- **D-22-18 commit 3 message verbatim:** `docs(22): pre-flight audit + scipy-notebook tag pin`

## Handoff to Phase 23

Phase 23 inherits the following artifacts committed to source control by this phase:

- **Pinned scipy-notebook dated tag** (`quay.io/jupyter/scipy-notebook:2026-04-27`) in both Dockerfiles' FROM lines — Phase 23 starts from a Dockerfile that already targets the correct base image
- **Lockfile with sha256 hashes** (`requirements.txt` regenerated by uv with 1644 hashes / 100 packages) — CONT-06 already satisfied; Phase 23 just consumes it
- **Validated Python target** (Python 3.14) — PY-01/PY-02 already validated end-to-end inside the actual base image; Phase 23's BuildKit cache mount install will reproduce the same resolution
- **PROJECT.md D-03** already reflects Python 3.14 (no further D-03 update needed in Phase 23)
- **22-AUDIT.md** committed for permanent reviewability — anyone investigating Phase 23 build failures can cross-reference the Phase 22 conda↔uv shadow surface

**Phase 23's job (out of scope for this phase):** rewrite the install layer — delete `mamba install r-*` block, replace `pip install` with `uv pip install --system` under BuildKit cache mount, add import-smoke layer (`python -c "import duckdb, papermill, shap, prophet, lightgbm, xgboost, onnxruntime, optuna, umap, skl2onnx, pyinstaller"`), update CLAUDE.md regen instructions, update install.sh/install.ps1 to set `DOCKER_BUILDKIT=1`, mirror all template Dockerfile changes per CONT-08.

---
*Phase 22 completed: 2026-04-30*
