---
phase: 22-pre-flight-wheel-validation-lockfile-bootstrap
verified: 2026-04-30T00:00:00Z
status: human_needed
score: 5/5 must-haves verified
human_verification:
  - test: "Verify cold-cache docker compose build still succeeds against the new lockfile + scipy-notebook:2026-04-27 base"
    expected: "docker compose build --no-cache produces a working JupyterLab image (Phase 23 contract — but Phase 22 already changed the FROM line on the live root Dockerfile, so any build run today consumes the new base before Phase 23's install-layer rewrite lands)"
    why_human: "Out-of-band container build is heavyweight and was explicitly out of scope for Phase 22 (D-22-02: validation runs in throwaway container, no Docker-image artifacts committed). User should confirm before Phase 23 begins."
  - test: "Open .claude/doml/templates/notebooks/data_understanding_python.ipynb in JupyterLab and run the new SUMMARIZE cell against a fixture dataset"
    expected: "DuckDB SUMMARIZE returns the column_name / column_type / min / max / approx_unique / avg / std / q25 / q50 / q75 / count / null_percentage table per file; no kernel errors"
    why_human: "Notebook cell behaviour against actual data cannot be verified by grep alone — needs a live kernel and fixture data. Plan 22-01's SUMMARY explicitly notes 'no notebook smoke run was performed in this plan (out of scope — papermill execution belongs to phase verification, not template edits)'."
  - test: "Confirm the scipy-notebook:2026-04-27 dated tag still resolves on quay.io"
    expected: "docker pull quay.io/jupyter/scipy-notebook:2026-04-27 succeeds without error"
    why_human: "Tag-only pin (D-22-15) accepts the immutability guarantee from the Jupyter Docker Stacks team; verifying the tag is still pullable just before Phase 23 begins is a quick sanity check that catches a registry-side surprise early."
---

# Phase 22: Pre-flight Wheel Validation & Lockfile Bootstrap — Verification Report

**Phase Goal:** De-risk the v1.6 stack before any Dockerfile rewrite. Confirm Python 3.13 + uv + scipy-notebook resolve cleanly for all current deps, and produce the new `requirements.txt` lockfile with hashes — committed but not yet wired into Docker.
**Verified:** 2026-04-30
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (from ROADMAP Success Criteria)

| #   | Truth                                                                                                                                                                              | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `uv pip compile requirements.in --python-version <3.13 or 3.14> --generate-hashes -o requirements.txt` succeeds with all deps resolved to cpython wheels (no source builds)        | ✓ VERIFIED | `requirements.txt` line 2 records the exact uv invocation `uv pip compile requirements.in --python-version 3.14 --generate-hashes -o /out/requirements-3.14.txt`. 1959 lines, 1644 sha256 hashes spanning 100 packages. Per design decision D-22-08 the rig attempted 3.14 first and won (`/tmp/preflight-22/python-version.txt` = `3.14`). PROJECT.md D-03 re-locked accordingly.                          |
| 2   | `requirements.in` no longer contains `pip-tools`; explicitly pins `mistune<3` and `numpy<2.4`                                                                                       | ✓ VERIFIED | `grep -c "^pip-tools" /home/bill/source/DoML/requirements.in` returns `0`. Line 14: `mistune<3`. Line 22: `numpy<2.4`. Header comment lines 1-5 reference `uv pip compile ... --generate-hashes -o requirements.txt`.                                                                                                                                                                                       |
| 3   | New `requirements.txt` is committed with hashes; pip-tools-format header replaced with uv format (single isolated commit)                                                            | ✓ VERIFIED | Header line 1: `# This file was autogenerated by uv via the following command:`. 1644 sha256 hash entries. Commit `83671ac` `chore: switch lockfile to uv format (Python 3.14)` touches exactly two files (requirements.in + requirements.txt) — single isolated commit per PITFALLS.md §6 / D-22-18 commit 2.                                                                                              |
| 4   | Audit log of `quay.io/jupyter/scipy-notebook:<dated-tag>` package list vs `requirements.in` is produced; final base image dated tag is selected and recorded                        | ✓ VERIFIED | `22-AUDIT.md` exists (2526 lines) with all three required H2 sections: `## Mamba List`, `## Conflict Diff` (4 packages: jinja2, mistune, numpy, statsmodels), `## uv pip compile Diff`. Header records `Resolved scipy-notebook tag: quay.io/jupyter/scipy-notebook:2026-04-27`. Both Dockerfiles' line-3 FROM is `FROM quay.io/jupyter/scipy-notebook:2026-04-27` (root + template, byte-identical via diff). |
| 5   | ydata-profiling go/no-go decision documented in phase SUMMARY (per D-22-09/D-22-11, satisfied by DROPPING the dep entirely)                                                          | ✓ VERIFIED | `22-SUMMARY.md` deliverables checklist line 35-37 records: `.claude/doml/templates/requirements.in — ydata-profiling removed`, `.claude/doml/templates/requirements.txt — ydata-profiling==4.18.1 pin removed`, `data_understanding_python.ipynb — DuckDB SUMMARIZE cell added`. `grep -rn "ydata_profiling\|ydata-profiling" .claude/` returns 0 matches. SUMMARIZE cell present (3 references in notebook). |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact                                                                                | Expected                                                                                                                                | Status     | Details                                                                                                                                                                                                                |
| --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Dockerfile`                                                                            | FROM line bumped to `quay.io/jupyter/scipy-notebook:2026-04-27`; install layer untouched (Phase 23 boundary)                            | ✓ VERIFIED | Line 3 = `FROM quay.io/jupyter/scipy-notebook:2026-04-27`. `mamba install` block still present (`grep -c "mamba install"` = 1). 29 lines (no change vs HEAD~1 baseline besides line 3).                                |
| `.claude/doml/templates/Dockerfile`                                                     | Mirrors root Dockerfile FROM line                                                                                                       | ✓ VERIFIED | Line 3 = `FROM quay.io/jupyter/scipy-notebook:2026-04-27`. `diff <(sed -n '3p' Dockerfile) <(sed -n '3p' .claude/doml/templates/Dockerfile)` returns 0 (identical). 38 lines (unchanged vs baseline besides line 3).    |
| `requirements.in` (root)                                                                | Drops pip-tools, adds numpy<2.4, retains mistune<3, header references `uv pip compile`                                                  | ✓ VERIFIED | pip-tools count = 0; `mistune<3` at line 14; `numpy<2.4` at line 22; header line 3: `docker compose run --rm jupyter uv pip compile requirements.in --generate-hashes -o requirements.txt`.                            |
| `requirements.txt` (root)                                                               | uv-format header, sha256 hashes for every dep, 100 packages resolved                                                                    | ✓ VERIFIED | 1959 lines, 1644 sha256 hashes; header references uv invocation; lockfile generated against scipy-notebook:2026-04-27 in throwaway container.                                                                          |
| `.claude/doml/templates/requirements.in`                                                | Drops `ydata-profiling`                                                                                                                 | ✓ VERIFIED | `grep -c "ydata-profiling" .claude/doml/templates/requirements.in` = 0.                                                                                                                                                |
| `.claude/doml/templates/requirements.txt`                                               | Drops `ydata-profiling==4.18.1` pin                                                                                                     | ✓ VERIFIED | `grep -c "^ydata-profiling==" .claude/doml/templates/requirements.txt` = 0.                                                                                                                                            |
| `.claude/doml/templates/notebooks/data_understanding_python.ipynb`                      | Contains DuckDB SUMMARIZE cell; no `ydata_profiling`/`ProfileReport` references                                                         | ✓ VERIFIED | `grep -c "SUMMARIZE"` = 3 (cell + comment + display markdown). `grep -rn "ydata_profiling\|ydata-profiling" .claude/` = 0 matches across entire `.claude/` tree.                                                       |
| `.planning/phases/22-pre-flight-wheel-validation-lockfile-bootstrap/22-AUDIT.md`        | Three H2 sections (Mamba List / Conflict Diff / uv pip compile Diff); resolved tag + Python version in header                            | ✓ VERIFIED | 2526 lines. Header records Python 3.14 + scipy-notebook:2026-04-27. All three required H2 headers present. Conflict Diff identifies 4 shadowing packages.                                                              |
| `.planning/phases/22-pre-flight-wheel-validation-lockfile-bootstrap/22-SUMMARY.md`      | Frontmatter `python_version: 3.14`, `go_no_go: GO`, `auto_fallback_fired: false`; deliverables + requirements coverage + Phase 23 handoff | ✓ VERIFIED | All four frontmatter fields present and correct. Body has all four required sections. 86 lines.                                                                                                                        |
| `.planning/phases/22-pre-flight-wheel-validation-lockfile-bootstrap/22-01-SUMMARY.md`   | Plan 22-01 record (ydata-profiling drop)                                                                                                | ✓ VERIFIED | 156 lines, frontmatter complete, commit `1a7708b` recorded.                                                                                                                                                            |
| `.planning/phases/22-pre-flight-wheel-validation-lockfile-bootstrap/22-02-SUMMARY.md`   | Plan 22-02 record (root requirements + lockfile)                                                                                        | ✓ VERIFIED | 192 lines, frontmatter complete, commit `83671ac` recorded.                                                                                                                                                            |
| `.planning/phases/22-pre-flight-wheel-validation-lockfile-bootstrap/22-03-SUMMARY.md`   | Plan 22-03 record (Dockerfile + audit + docs updates)                                                                                   | ✓ VERIFIED | 202 lines, frontmatter complete, commit `5da3eea` recorded.                                                                                                                                                            |
| `.planning/PROJECT.md` D-03                                                             | Mentions Python 3.14 (re-locked from 3.13)                                                                                              | ✓ VERIFIED | Line 32: `**D-03:** Python 3.14 (locked at end of Phase 22 pre-flight; ydata-profiling dropped per D-22-09 unblocking 3.14, all wheels resolved cleanly via uv pip compile).`                                          |
| `.planning/REQUIREMENTS.md` PY-FUT retired + PY-01..PY-05 + CONT-06 marked Complete     | PY-FUT-01/02 removed; traceability rows flipped from Pending → Complete                                                                 | ✓ VERIFIED | Line 50: `### Python 3.14 (PY-FUT) — RESOLVED in Phase 22 (2026-04-30)`. Line 52 documents the retirement. `grep -c "PY-FUT-01.*Upgrade.*ydata-profiling.*lifts"` = 0. Traceability table: PY-01..PY-05 + CONT-06 all `Complete`. |

**All 14 required artifacts verified.**

### Key Link Verification

| From                                                  | To                                                            | Via                                                                                       | Status   | Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----------------------------------------------------- | ------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `requirements.in` (root)                              | `requirements.txt` (root)                                     | `uv pip compile --generate-hashes` against `quay.io/jupyter/scipy-notebook:2026-04-27`     | ✓ WIRED  | `requirements.txt` header line 2 captures the exact invocation: `uv pip compile requirements.in --python-version 3.14 --generate-hashes -o /out/requirements-3.14.txt`. Hashes present (1644). Pre-flight log `/tmp/preflight-22/run-3.14.log` ends with `PASS: Python 3.14 pre-flight green`.                                                                                                                                                                                                       |
| Pre-flight tag-resolution                             | Dockerfile FROM line (root + template)                        | tag string substitution from `/tmp/preflight-22/scipy-notebook-tag.txt`                   | ✓ WIRED  | `/tmp/preflight-22/scipy-notebook-tag.txt` = `2026-04-27`. Both Dockerfile line-3 FROMs read `quay.io/jupyter/scipy-notebook:2026-04-27` and are byte-identical (diff exit 0).                                                                                                                                                                                                                                                                                                                       |
| Pre-flight Python-version resolution                  | PROJECT.md D-03 + REQUIREMENTS.md PY-FUT-01                   | conditional doc update branch (3.14 path = D-22-08 path 1)                                 | ✓ WIRED  | `/tmp/preflight-22/python-version.txt` = `3.14`. PROJECT.md D-03 says Python 3.14. REQUIREMENTS.md PY-FUT block retired (resolved header + paragraph). PY-01..PY-05 + CONT-06 traceability rows flipped to Complete.                                                                                                                                                                                                                                                                                  |
| Pre-flight raw artifacts                              | `22-AUDIT.md` (mamba list / conflict diff / compile diff)     | inline embed as fenced code blocks                                                         | ✓ WIRED  | All three required H2 sections present and substantive: Mamba List section embeds `mamba list` output (387 source lines per 22-02 SUMMARY); Conflict Diff lists 4 shadowing packages (`jinja2`, `mistune`, `numpy`, `statsmodels`); uv pip compile Diff embeds the v1.5→v1.6 lockfile diff.                                                                                                                                                                                                          |
| Notebook DuckDB SUMMARIZE cell                        | `duckdb` (already in template requirements.in)                 | `duckdb.connect()` + `con.execute("SUMMARIZE …").df()`                                      | ✓ WIRED  | Notebook contains 3 SUMMARIZE references (cell source + display markdown). `duckdb` listed in template `requirements.in` (and root); cell uses pre-existing `get_read_fn` helper from the prior cell (per Plan 22-01 deliberate placement at index 5).                                                                                                                                                                                                                                              |

### Data-Flow Trace (Level 4)

This phase produces lockfile + Dockerfile + planning artifacts — not runtime application code rendering dynamic data. Level 4 (data flowing into a UI/component) does not apply directly. The closest analogue is the SUMMARIZE notebook cell: it is wired to live `duckdb` data through pre-existing `files` and `get_read_fn` from the helper cell, and would render real DataFrames at runtime (verifiable only by running the kernel — flagged as human verification item 2).

| Artifact                                                      | Data Variable | Source                                              | Produces Real Data | Status   |
| ------------------------------------------------------------- | ------------- | --------------------------------------------------- | ------------------ | -------- |
| `data_understanding_python.ipynb` SUMMARIZE cell              | `summary_df`  | `duckdb.connect()` + `SUMMARIZE SELECT * FROM <fn>` | Yes (at runtime)   | ✓ FLOWING (static — needs kernel run for live verification, flagged for human) |
| `requirements.txt` (root)                                     | (lockfile)    | `uv pip compile … --generate-hashes`                | Yes                | ✓ FLOWING (1644 hashes, 100 packages, generated against live scipy-notebook:2026-04-27 base) |

### Behavioral Spot-Checks

This phase produces no runnable entry points (lockfile + Dockerfile FROM-line bump + planning docs only). However, three behaviors WERE exercised by the pre-flight rig itself in plan 22-02 and the artifacts persist for review.

| Behavior                                                        | Command (executed during plan 22-02)                                                                                | Result                                                                | Status |
| --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ------ |
| `uv pip compile` succeeds against `requirements.in` for Python 3.14 | `uv pip compile requirements.in --python-version 3.14 --generate-hashes -o /out/requirements-3.14.txt`              | 100 packages resolved in 560ms; lockfile written; 1644 sha256 hashes  | ✓ PASS |
| `uv pip install --system` succeeds in throwaway scipy-notebook container | `uv pip install --system -r /tmp/req.txt` inside `quay.io/jupyter/scipy-notebook:2026-04-27`                       | 45 packages installed (20.4s prepared + 0.3s installed); no source builds | ✓ PASS |
| Import smoke green for heavy compilation-prone wheels           | `python -c "import duckdb, papermill, shap, prophet, lightgbm, xgboost, onnxruntime, optuna, umap"`                  | Exit 0; benign onnxruntime GPU-detection warning (no GPU in container) | ✓ PASS |
| `requirements.txt` parseable as valid uv lockfile (regression)   | `head -2 requirements.txt`                                                                                          | Header line 1 starts with `# This file was autogenerated by uv`; line 2 is the exact uv command — both present today | ✓ PASS |
| Both Dockerfile FROM lines byte-identical                        | `diff <(sed -n '3p' Dockerfile) <(sed -n '3p' .claude/doml/templates/Dockerfile)`                                   | Exit 0 (identical)                                                    | ✓ PASS |

### Requirements Coverage

| Requirement                                                           | Source Plan | Description                                                                                       | Status      | Evidence                                                                                                                                                                                                                                                                                                       |
| --------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **PY-01** (Container Python pinned to Python 3.13.x)                  | 22-02, 22-03 | Note: per D-22-08 the pre-flight cleared 3.14, so this requirement was *upgraded* to Python 3.14   | ✓ SATISFIED | PROJECT.md D-03 re-locked to Python 3.14; REQUIREMENTS.md traceability marks PY-01 Complete; pre-flight rig validated 3.14 end-to-end (`/tmp/preflight-22/python-version.txt` = `3.14`; run-3.14.log ends with PASS).                                                                                            |
| **PY-02** (cp31x wheels, no source builds)                            | 22-02       | All packages resolve to wheels (no source builds during cold-cache install)                        | ✓ SATISFIED | uv pip install --system inside scipy-notebook:2026-04-27 reported "Prepared 45 packages in 20.4s" (no compilation phase); verified by zero `Building wheel for` lines in run-3.14.log; lockfile has hashes for all 100 packages.                                                                                |
| **PY-03** (mistune<3 retained in requirements.in)                     | 22-01, 22-02 | Pin preserved to avoid breaking conda-managed nbconvert                                           | ✓ SATISFIED | Root `requirements.in` line 14 contains `mistune<3`; verified via `grep -c "^mistune<3$"` = 1.                                                                                                                                                                                                                  |
| **PY-04** (pip-tools removed from requirements.in)                    | 22-02       | Replaced by uv per D-02                                                                            | ✓ SATISFIED | `grep -c "^pip-tools" /home/bill/source/DoML/requirements.in` = 0; header references `uv pip compile` instead of `pip-compile`.                                                                                                                                                                                 |
| **PY-05** (numpy<2.4 pinned in requirements.in)                       | 22-02       | Prevent ABI breakage from a future numpy 2.4 release                                              | ✓ SATISFIED | Root `requirements.in` line 22 contains `numpy<2.4`; verified via `grep -c "^numpy<2.4$"` = 1.                                                                                                                                                                                                                  |
| **CONT-06** (requirements.txt regenerated with `uv pip compile … --generate-hashes`) | 22-02 | Lockfile hashed for supply-chain protection                                                       | ✓ SATISFIED | Root `requirements.txt` has 1644 sha256 hashes spanning 100 packages; header line 2 captures the exact uv invocation including `--generate-hashes`.                                                                                                                                                            |

**All 6 phase requirements satisfied.** No orphaned requirements: REQUIREMENTS.md traceability table maps PY-01..PY-05 + CONT-06 to Phase 22 — every ID claimed by a plan in this phase appears in the table; no additional phase-22 IDs from REQUIREMENTS.md are unclaimed.

### Anti-Patterns Found

| File                                                            | Line | Pattern                                                                                       | Severity | Impact                                                                                                                                                                                                                                                                                                          |
| --------------------------------------------------------------- | ---- | --------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Dockerfile`                                                    | 1    | Comment `# DoML analysis environment — Python 3.13 + R 4.x + DuckDB + ML stack` is now stale (Python 3.14 + R will be removed in Phase 24) | ℹ️ Info  | Per D-22-14 phase boundary discipline: ONLY the FROM line was edited in Phase 22; the line-1 comment is owned by Phases 23/24 (R removal sweep). The plan's `<interfaces>` block in 22-03-PLAN explicitly says "DO NOT edit that comment in this phase. Phase 23 / Phase 24 will sweep it (R removal)." Documented as a known phase-boundary holdover, not a bug. Plan 22-03's executor SUMMARY also flagged this. |
| `.claude/doml/templates/Dockerfile`                             | 1    | Same stale comment (`Python 3.13 + R 4.x …`)                                                  | ℹ️ Info  | Same disposition — will be swept by Phase 23/24 mirror.                                                                                                                                                                                                                                                         |
| Plan 22-03 SUMMARY documented deviation #4 (commit-chain interleaving) | n/a  | Acceptance-criterion mismatch: `git log --oneline -3` does NOT show three `chore-1 → chore-2 → docs-3` because orchestrator interleaved `docs(22-01)` and `docs(22-02)` plan-summary commits | ℹ️ Info  | Cosmetic — full chain visible in `git log --oneline -10` (verified: `1a7708b → 348fe27 → 83671ac → c6050a4 → 5da3eea → db59f1c`); the three D-22-18 chore/docs commits all landed with verbatim messages. Documented in 22-03-SUMMARY for future plan-template clarity. No remediation required.            |

No blocker or warning anti-patterns. No TODO/FIXME/placeholder/stub patterns in any phase-modified file (verified by spot-check against the 6 files touched in commit `5da3eea`).

### Human Verification Required

See frontmatter `human_verification:` block. Three items flagged:

1. **Cold-cache `docker compose build` against new lockfile + scipy-notebook:2026-04-27** — out of Phase 22 scope (D-22-02), but worth a sanity check before Phase 23 begins.
2. **Notebook SUMMARIZE cell live execution** — Plan 22-01 explicitly noted "no notebook smoke run was performed in this plan (out of scope — papermill execution belongs to phase verification, not template edits)." The cell's wiring is correct (uses pre-existing `get_read_fn` helper + `files` variable), but kernel-level confirmation requires fixture data and a running JupyterLab.
3. **`scipy-notebook:2026-04-27` tag still pullable** — Per D-22-15 (tag-only pin, no digest pin), there is a small accept-disposition risk that the registry tag could mutate; quickest mitigation is a `docker pull` smoke.

### Gaps Summary

No gaps blocking goal achievement. All five ROADMAP success criteria are satisfied. All six declared requirements (PY-01..PY-05 + CONT-06) are SATISFIED with evidence in the codebase and committed history. The three-commit chain prescribed by D-22-18 is on `main`:

- `1a7708b` `chore: drop ydata-profiling from template + replace EDA profiling cell` (commit 1, plan 22-01)
- `83671ac` `chore: switch lockfile to uv format (Python 3.14)` (commit 2, plan 22-02 — single isolated commit per PITFALLS.md §6)
- `5da3eea` `docs(22): pre-flight audit + scipy-notebook tag pin` (commit 3, plan 22-03)

Phase 22's de-risk function performed exactly as designed: the auto-fallback rig (D-22-08) attempted Python 3.14 first and won on first try, allowing PROJECT.md D-03 to re-lock from 3.13 → 3.14 and PY-FUT-01/02 to retire entirely. The lockfile + scipy-notebook tag + Python version triple is now committed for Phase 23 to consume.

Status is `human_needed` rather than `passed` because three phase-completion checks intentionally fall outside automated verification: a real container build, a live notebook kernel run, and a registry tag-availability check. None of these are gaps — they are explicitly out-of-scope for Phase 22 per its own design (D-22-02: "no Docker-image artifacts committed"; Plan 22-01: "no notebook smoke run was performed … out of scope"; D-22-15: tag-only pin with accept disposition).

---

*Verified: 2026-04-30*
*Verifier: Claude (gsd-verifier)*
