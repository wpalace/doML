---
phase: 23-dockerfile-rebuild-uv-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/doml/templates/requirements.in
autonomous: true
requirements: [CONT-08]
commit_strategy:
  message: "chore(23): mirror template requirements + drop pip-tools, add numpy<2.4"
  rationale: "Mirrors Phase 22 root requirements.in cleanup into the template; isolated from lockfile regen (Plan 02) and Dockerfile rewrite (Plans 03-04)."

must_haves:
  truths:
    - "Template requirements.in no longer contains pip-tools (replaced by uv per D-02)"
    - "Template requirements.in pins numpy<2.4 to prevent ABI breakage"
    - "Template requirements.in regen-comment header references uv pip compile (matches root)"
    - "Template requirements.in retains kaggle, prophet, pmdarima, onnxruntime, mistune<3, pyinstaller, skl2onnx (no functional removals)"
  artifacts:
    - path: ".claude/doml/templates/requirements.in"
      provides: "Template top-level deps for /doml-new-project scaffolds"
      contains: "numpy<2.4"
      does_not_contain: "pip-tools"
  key_links:
    - from: ".claude/doml/templates/requirements.in"
      to: ".claude/doml/templates/requirements.txt"
      via: "uv pip compile (Plan 02)"
      pattern: "lockfile regen consumes this file"
    - from: ".claude/doml/templates/requirements.in"
      to: "requirements.in (root)"
      via: "mirror discipline (CONT-08)"
      pattern: "templates always match root"
---

<objective>
Mirror Phase 22's root requirements.in cleanup into the template requirements.in so `/doml-new-project` scaffolds new user projects with the v1.6 dependency surface (no pip-tools, explicit numpy<2.4 pin, regen-command pointing at uv pip compile).

Purpose: CONT-08 ("templates always match root"). Phase 22 cleaned root requirements.in but explicitly deferred the template mirror to Phase 23. This is a 4-line edit in a single file — landed as an isolated commit so Plan 02's lockfile-format-noisy diff stays alone.

Output: `.claude/doml/templates/requirements.in` updated to match root requirements.in's structural cleanup. No lockfile regen yet (Plan 02 owns that).
</objective>

<execution_context>
@/home/bill/source/DoML/.claude/get-shit-done/workflows/execute-plan.md
@/home/bill/source/DoML/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-dockerfile-rebuild-uv-migration/23-CONTEXT.md
@.planning/research/STACK.md
@.planning/phases/22-pre-flight-wheel-validation-lockfile-bootstrap/22-SUMMARY.md
@requirements.in
@.claude/doml/templates/requirements.in

<interfaces>
<!-- Reference: structural shape root requirements.in lines 1-43 (committed by Phase 22). -->
<!-- Template requirements.in current state lines 1-41 (to be edited). -->

Root `requirements.in` (Phase 22 final state — copy structural cues, NOT verbatim — kaggle stays in template):

```
# DoML top-level dependencies (unpinned)
# Regenerate requirements.txt with:
#   docker compose run --rm jupyter uv pip compile requirements.in --generate-hashes -o requirements.txt
#
# Do NOT edit requirements.txt by hand — it is generated from this file.

# --- Core DB / notebook tooling ---
duckdb
papermill
# nbconvert and nbformat are provided by the conda base image (quay.io/jupyter/scipy-notebook)
# Do NOT pip-install nbconvert — it conflicts with the conda-managed version
jinja2
# mistune 3.x breaks the conda-managed nbconvert — pin to 2.x
mistune<3

# --- Reproducibility ---
nbstripout
pre-commit
pytest

# --- ML stack ---
numpy<2.4
shap
optuna
umap-learn
xgboost
lightgbm
statsmodels
prophet
onnxruntime  # ONNX runtime for WASM target benchmarking (Phase 17 — PERF-01)

# --- Time series forecasting ---
pmdarima

# --- Visualization / profiling ---
plotly

# --- Data acquisition ---
# Note: after adding kaggle here, regenerate requirements.txt with:
#   docker compose run --rm jupyter uv pip compile requirements.in --generate-hashes -o requirements.txt
# Then rebuild the image: docker compose build
kaggle
```

Template `.claude/doml/templates/requirements.in` current state (43 lines) keeps the same package set EXCEPT it currently has `pip-tools` on line 17 and lacks `numpy<2.4`, and the regen-command headers still say `pip-compile`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update template requirements.in — drop pip-tools, add numpy<2.4, switch regen command to uv pip compile</name>
  <read_first>
    - .claude/doml/templates/requirements.in (current 41-line state — see what is actually there before editing)
    - requirements.in (root — Phase 22 final state — copy structural cues from `<interfaces>` block above)
    - .planning/phases/23-dockerfile-rebuild-uv-migration/23-CONTEXT.md (D-23-B1 lock decision)
  </read_first>
  <files>.claude/doml/templates/requirements.in</files>
  <action>
    Edit `.claude/doml/templates/requirements.in` to mirror root `requirements.in`'s Phase 22 cleanup. Make exactly four classes of change:

    **Change 1 — Top-of-file regen comment (current lines 2-3):** Replace
    ```
    # Regenerate requirements.txt with:
    #   docker compose run --rm jupyter pip-compile requirements.in
    ```
    with
    ```
    # Regenerate requirements.txt with:
    #   docker compose run --rm jupyter uv pip compile requirements.in --generate-hashes -o requirements.txt
    ```

    **Change 2 — Remove pip-tools (current line 17):** Delete the line `pip-tools` entirely from the `--- Reproducibility ---` block. Per D-02 + D-23-B1, uv replaces pip-tools.

    **Change 3 — Add numpy<2.4 (insert into ML stack block):** Add `numpy<2.4` as the first entry under the `# --- ML stack ---` header (currently line 20), immediately before `shap`. Per D-23-B1 + PY-05 + PITFALLS #12: explicit numpy pin prevents uv resolver picking numpy 2.4 (ABI breakage on cp31x C-extensions).

    **Change 4 — Bottom-of-file kaggle regen comment (current lines 33-35):** Replace
    ```
    # kaggle CLI for /doml-get-data. Regenerate requirements.txt after any change here:
    #   docker compose run --rm jupyter pip-compile requirements.in
    # Then rebuild: docker compose build
    ```
    with
    ```
    # kaggle CLI for /doml-get-data. Regenerate requirements.txt after any change here:
    #   docker compose run --rm jupyter uv pip compile requirements.in --generate-hashes -o requirements.txt
    # Then rebuild: docker compose build
    ```

    Do NOT touch any other line. Do NOT remove kaggle (it stays — `/doml-get-data` consumes it). Do NOT add mistune<3 to template (template historically did not pin it; root pinned it because root's Dockerfile builds the analysis image; template's lockfile resolves transitively).

    Per D-23-B1: "Mirror Phase 22's root requirements.in cleanup into .claude/doml/templates/requirements.in — drop pip-tools, add numpy<2.4. Keeps /doml-new-project scaffolds in lockstep with the v1.6 root."
  </action>
  <acceptance_criteria>
    - `grep -c '^pip-tools' .claude/doml/templates/requirements.in` returns `0`
    - `grep -c '^numpy<2.4' .claude/doml/templates/requirements.in` returns `1`
    - `grep -c 'uv pip compile requirements.in --generate-hashes -o requirements.txt' .claude/doml/templates/requirements.in` returns `2` (top header + kaggle-block comment)
    - `grep -c 'pip-compile requirements.in' .claude/doml/templates/requirements.in` returns `0` (every old `pip-compile` regen comment replaced)
    - `grep -c '^kaggle' .claude/doml/templates/requirements.in` returns `1` (kaggle preserved)
    - `grep -c '^prophet' .claude/doml/templates/requirements.in` returns `1` (no functional removals)
    - `grep -c '^pyinstaller\|^skl2onnx' .claude/doml/templates/requirements.in` returns `2` (deployment tools preserved)
    - File ends with a trailing newline (`tail -c1 .claude/doml/templates/requirements.in | xxd` shows `0a`)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; cd /home/bill/source/DoML; grep -q "^numpy<2.4" .claude/doml/templates/requirements.in && ! grep -q "^pip-tools" .claude/doml/templates/requirements.in && [ "$(grep -c "uv pip compile requirements.in --generate-hashes" .claude/doml/templates/requirements.in)" -eq 2 ] && ! grep -q "pip-compile requirements.in" .claude/doml/templates/requirements.in'</automated>
  </verify>
  <done>Template requirements.in mirrors root requirements.in's Phase 22 cleanup; pip-tools removed; numpy<2.4 added under ML stack; both regen-command comments point at uv pip compile with --generate-hashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none added in this plan) | Plan 01 is a 4-line edit to a source-of-truth requirements.in. No new trust boundary; lockfile regen (Plan 02) and Docker install layer (Plan 03) are the actual supply-chain surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | `.claude/doml/templates/requirements.in` | accept | Source-of-truth file; checked into git; reviewable diff. No external input flows here. |
| T-23-02 | Information Disclosure | requirements.in regen-command comment | accept | Comment includes `uv pip compile … --generate-hashes` — public command. No secrets. |
| T-23-03 | Elevation of Privilege | numpy<2.4 pin | mitigate | Explicit ABI pin (PY-05) prevents transitive numpy 2.4 from breaking C-extension wheels. Documented threat addressed by D-23-B1. |
</threat_model>

<verification>
Plan 01 verification:
- `git diff .claude/doml/templates/requirements.in` shows exactly the 4-class edit (regen-comment ×2, pip-tools removal, numpy<2.4 addition).
- `git diff --stat .claude/doml/templates/requirements.in` shows ~1-2 line insertion / 1-line deletion (regen comments are line-level replacements).
- No other file modified by this plan.
- Commit message: `chore(23): mirror template requirements + drop pip-tools, add numpy<2.4` (per commit_strategy frontmatter).
</verification>

<success_criteria>
- `.claude/doml/templates/requirements.in` no longer contains `pip-tools` anywhere
- `.claude/doml/templates/requirements.in` contains exactly one `numpy<2.4` line under the ML stack block
- Both regen-command comments in the file point at `uv pip compile requirements.in --generate-hashes -o requirements.txt`
- File parses as a valid pip requirements file (no syntax errors)
- All acceptance_criteria checks pass
- CONT-08 partial coverage advanced (template mirror requirement; lockfile regen completes coverage in Plan 02; Dockerfiles in Plans 03-04)
</success_criteria>

<output>
After completion, no SUMMARY needed for this single-task structural-cleanup plan. The phase-level 23-SUMMARY.md (written in Plan 04) records this commit as part of the phase deliverables.

Commit message: `chore(23): mirror template requirements + drop pip-tools, add numpy<2.4`
Files in commit: `.claude/doml/templates/requirements.in` only.
</output>
