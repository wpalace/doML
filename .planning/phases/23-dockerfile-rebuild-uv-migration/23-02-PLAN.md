---
phase: 23-dockerfile-rebuild-uv-migration
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - .claude/doml/templates/requirements.txt
autonomous: true
requirements: [CONT-06, CONT-08]
commit_strategy:
  message: "chore(23): regenerate template lockfile via uv (isolated format diff)"
  rationale: "PITFALLS #6: uv pip compile output differs from pip-compile output (header format, comment style, ordering, trailing markers). The first regen produces a 100+ line noisy diff with no semantic change. Land as an isolated commit so Plan 03's Dockerfile rewrite diff stays reviewable."

must_haves:
  truths:
    - "Template requirements.txt regenerated via uv pip compile with --generate-hashes"
    - "Lockfile contains sha256 hashes for every resolved package (supply chain safety)"
    - "Lockfile header signature is uv pip compile (not pip-compile)"
    - "Resolution targets Python 3.14 (matches Phase 22 outcome / PROJECT.md D-03)"
    - "Lockfile resolves cleanly with no fallback to source builds (matches Phase 22 root outcome)"
  artifacts:
    - path: ".claude/doml/templates/requirements.txt"
      provides: "Hash-pinned lockfile consumed by template Dockerfile install layer"
      contains: "uv pip compile"
      contains_regex: "--hash=sha256:"
      min_lines: 1000
  key_links:
    - from: ".claude/doml/templates/requirements.in"
      to: ".claude/doml/templates/requirements.txt"
      via: "uv pip compile --generate-hashes"
      pattern: "lockfile is regenerated from updated requirements.in (Plan 01)"
    - from: ".claude/doml/templates/requirements.txt"
      to: ".claude/doml/templates/Dockerfile"
      via: "COPY requirements.txt + uv pip install --system (Plan 03)"
      pattern: "Dockerfile install layer consumes this lockfile"
---

<objective>
Fully regenerate `.claude/doml/templates/requirements.txt` via `uv pip compile --generate-hashes` against the cleaned-up template `requirements.in` from Plan 01. Lands as an isolated commit per PITFALLS #6 — the format-only diff between pip-compile and uv pip compile is large and noisy, and we want it isolated from semantically meaningful diffs (Plan 03's Dockerfile rewrite).

Purpose: CONT-06 (uv-driven lockfile with hashes) for the template path. CONT-08 (templates match root). Phase 22 did this for root; Plan 02 does it for the template — using exactly the same compile command and Python target (3.14) so the two lockfiles stay in lockstep.

Output: `.claude/doml/templates/requirements.txt` regenerated with sha256 hashes, uv-format header, ready for Plan 03's Dockerfile install layer to consume.
</objective>

<execution_context>
@/home/bill/source/DoML/.claude/get-shit-done/workflows/execute-plan.md
@/home/bill/source/DoML/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-dockerfile-rebuild-uv-migration/23-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@.planning/phases/22-pre-flight-wheel-validation-lockfile-bootstrap/22-SUMMARY.md
@.claude/doml/templates/requirements.in
@requirements.txt

<interfaces>
<!-- Reference command: Phase 22 root regen. Match this exactly except for the file paths. -->
<!-- Phase 22's pre-flight rig (per 22-SUMMARY.md line 31) ran the compile + install + smoke -->
<!-- inside a throwaway `quay.io/jupyter/scipy-notebook:2026-04-27` container. Plan 02 differs -->
<!-- in that the *compile step alone* runs in the standalone uv image — see Step 2 rationale. -->
<!-- The dry-run install smoke (Step 4) DOES run inside scipy-notebook, mirroring Phase 22 exactly. -->

Root `requirements.txt` (Phase 22 final state — line 1-2 header signature):
```
# This file was autogenerated by uv via the following command:
#    uv pip compile requirements.in --python-version 3.14 --generate-hashes -o /out/requirements-3.14.txt
```

Resolved package count (root, Phase 22): 100 packages, 1644 sha256 hashes, 1959 lines total.
Template package set is similar (no ydata-profiling, no fastapi, but same core ML stack); expect comparable line count (~1500-2000 lines, ~1000-1700 hashes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate template requirements.txt via uv pip compile + dry-run install smoke inside scipy-notebook</name>
  <read_first>
    - .claude/doml/templates/requirements.in (Plan 01 output — verify it has numpy<2.4 and no pip-tools before regen)
    - .claude/doml/templates/requirements.txt (current state — pip-compile-format from before; will be overwritten)
    - .planning/research/PITFALLS.md (§6 — isolated commit rationale; §3 — uv pip install vs sync)
    - .planning/phases/22-pre-flight-wheel-validation-lockfile-bootstrap/22-SUMMARY.md (root regen command + auto-fallback record — match the same Python version 3.14 and base image 2026-04-27)
  </read_first>
  <files>.claude/doml/templates/requirements.txt</files>
  <action>
    Regenerate `.claude/doml/templates/requirements.txt` using uv pip compile, then dry-run-install the resulting lockfile inside `quay.io/jupyter/scipy-notebook:2026-04-27` to mirror Phase 22's actual install target environment.

    **Step 1 — Pre-flight check** (verify Plan 01 ran):
    ```bash
    cd /home/bill/source/DoML
    grep -q '^numpy<2.4' .claude/doml/templates/requirements.in || { echo "ABORT: Plan 01 (template requirements.in cleanup) has not run"; exit 1; }
    ! grep -q '^pip-tools' .claude/doml/templates/requirements.in || { echo "ABORT: pip-tools still present"; exit 1; }
    ```

    **Step 2 — Regenerate the lockfile** by running `uv pip compile` inside the standalone `ghcr.io/astral-sh/uv:0.11.8` image. Mount the templates directory and run the exact command Phase 22 used for root:
    ```bash
    docker run --rm \
      -v "$(pwd)/.claude/doml/templates:/work" \
      -v "$(pwd):/host:ro" \
      -w /work \
      ghcr.io/astral-sh/uv:0.11.8 \
      pip compile requirements.in \
        --python-version 3.14 \
        --generate-hashes \
        -o requirements.txt
    ```

    Notes on the command:
    - Use `ghcr.io/astral-sh/uv:0.11.8` directly for the compile step. The standalone uv image is equivalent to scipy-notebook for *compile* purposes because `uv pip compile` resolves dependencies from PyPI metadata — it does NOT touch local conda site-packages or the runtime Python interpreter. The `--python-version 3.14` flag forces the resolution target explicitly, so the marker evaluation is identical regardless of which container the compile runs in. This is faster than spinning up scipy-notebook + installing uv. The dry-run install (Step 4) DOES run inside scipy-notebook to verify against the actual install target environment. Per D-23-A4 / STACK.md: uv 0.11.8 is the locked version.
    - `--python-version 3.14` matches the resolved target locked at end of Phase 22 (D-22-06 outcome — see 22-SUMMARY.md line 16).
    - `--generate-hashes` produces sha256 hashes per package (CONT-06).
    - Do NOT use `uv pip sync` (PITFALLS #3 — would break the conda kernel if applied; we are only generating the lockfile, but the same install model rule applies in Plan 03).

    **Step 3 — Verify the regen worked:**
    ```bash
    head -5 .claude/doml/templates/requirements.txt
    # Expected: line 1 contains "uv" (autogenerated by uv); contains "uv pip compile"
    grep -c -- '--hash=sha256:' .claude/doml/templates/requirements.txt
    # Expected: >= 1000 (Phase 22 root has 1644; template is similarly sized)
    wc -l .claude/doml/templates/requirements.txt
    # Expected: >= 1000
    ```

    **Step 4 — Smoke install** to confirm the lockfile is actually consumable by uv pip install in the actual install target environment. Run inside `quay.io/jupyter/scipy-notebook:2026-04-27` (mirrors Phase 22 methodology — Phase 22's pre-flight rig ran inside scipy-notebook per 22-SUMMARY.md line 31 — and binds the dry-run hash check to a real cp314 conda Python interpreter, so we catch lockfile-install failures here rather than discovering them during Plan 04's `docker compose build --no-cache`):
    ```bash
    docker run --rm \
      -v "$(pwd)/.claude/doml/templates:/work:ro" \
      -v "$(pwd):/uvbin:ro" \
      -w /work \
      --user root \
      quay.io/jupyter/scipy-notebook:2026-04-27 \
      bash -lc 'curl -fsSL https://github.com/astral-sh/uv/releases/download/0.11.8/uv-installer.sh | env UV_INSTALL_DIR=/tmp/uvbin sh && /tmp/uvbin/uv pip install --system --dry-run -r requirements.txt'
    ```
    Equivalent shorter form using a sidecar uv binary copy if the network fetch is undesirable:
    ```bash
    docker run --rm \
      --entrypoint bash \
      -v "$(pwd)/.claude/doml/templates:/work:ro" \
      -w /work \
      quay.io/jupyter/scipy-notebook:2026-04-27 \
      -lc 'docker_skip=1; pip install --quiet uv==0.11.8 && uv pip install --system --dry-run -r requirements.txt'
    ```
    Either form must exit 0. If non-zero, abort (do NOT commit a broken lockfile). The dry-run inside scipy-notebook is the early gate that verifies the resolved hashes bind correctly against the cp314 conda Python the production build will use.

    **DO NOT** modify any other file. Do NOT touch root requirements.txt (Phase 22 owns that — already regenerated, do not re-touch). Do NOT touch the Dockerfiles (Plans 03-04). Do NOT touch CLAUDE.md / AGENTS.md (Plan 04). The commit must contain exactly one file change: `.claude/doml/templates/requirements.txt`.

    Per D-23-B2 + PITFALLS #6: this is an isolated commit — format-only diff is noisy enough that bundling with Plan 03's Dockerfile diff would make review impossible.
  </action>
  <acceptance_criteria>
    - `head -1 .claude/doml/templates/requirements.txt` contains the substring `uv` (uv-format header, not `pip-compile`)
    - `head -5 .claude/doml/templates/requirements.txt` contains `uv pip compile` (the recorded regen command in the auto-generated header — load-bearing positive check; the absence of the `pip-compile` regen string is implicit in the positive `uv pip compile` match)
    - `head -5 .claude/doml/templates/requirements.txt` contains `--python-version 3.14` (resolution target matches Phase 22 / D-03)
    - `grep -c -- '--hash=sha256:' .claude/doml/templates/requirements.txt` returns >= 1000 (hash format is `--hash=sha256:...`, machine-readable; Phase 22 root has 1644, so 1000 is a snug floor that catches partial-resolution lockfiles)
    - `wc -l .claude/doml/templates/requirements.txt | awk '{print $1}'` returns >= 1000
    - `grep -E '^numpy==' .claude/doml/templates/requirements.txt | head -1` matches a `numpy==2.[0-3]` version (numpy<2.4 constraint honored)
    - `grep -E '^kaggle==' .claude/doml/templates/requirements.txt` returns a match (kaggle resolved through unified install — D-23-B3 setup)
    - `grep -E '^prophet==' .claude/doml/templates/requirements.txt` returns a match (prophet wheel resolved, no source build)
    - `grep -E '^pyinstaller==|^skl2onnx==' .claude/doml/templates/requirements.txt` returns 2 matches
    - `! grep -E '^pip-tools==' .claude/doml/templates/requirements.txt` (pip-tools no longer in resolved tree)
    - `git diff --name-only HEAD` shows exactly one file: `.claude/doml/templates/requirements.txt`
    - Smoke install dry-run (`uv pip install --system --dry-run -r requirements.txt`) exits 0 inside `quay.io/jupyter/scipy-notebook:2026-04-27` (binds hash check to cp314 conda Python — mirrors Phase 22 methodology)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; cd /home/bill/source/DoML; head -5 .claude/doml/templates/requirements.txt | grep -q "uv pip compile" && head -5 .claude/doml/templates/requirements.txt | grep -q -- "--python-version 3.14" && [ "$(grep -c -- "--hash=sha256:" .claude/doml/templates/requirements.txt)" -ge 1000 ] && [ "$(wc -l < .claude/doml/templates/requirements.txt)" -ge 1000 ] && grep -qE "^numpy==2\.[0-3]" .claude/doml/templates/requirements.txt && ! grep -qE "^pip-tools==" .claude/doml/templates/requirements.txt && grep -qE "^kaggle==" .claude/doml/templates/requirements.txt && grep -qE "^prophet==" .claude/doml/templates/requirements.txt'</automated>
  </verify>
  <done>Template requirements.txt regenerated via uv pip compile against Python 3.14, header signature is `uv pip compile`, all packages have --hash=sha256:... entries (>=1000), numpy resolved < 2.4, kaggle / prophet / pyinstaller / skl2onnx all present, dry-run install inside scipy-notebook:2026-04-27 succeeds. Single-file commit isolated from Plan 03 changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> uv resolver | Public PyPI package indexes feed uv resolution. Hash pinning (`--generate-hashes`) is the integrity check at this boundary. |
| ghcr.io/astral-sh/uv:0.11.8 image | Vendored binary. Image registry trust (pinned tag, not `:latest`). |
| quay.io/jupyter/scipy-notebook:2026-04-27 image | Dry-run smoke target. Pinned dated tag (Phase 22). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Tampering | PyPI wheel content | mitigate | `--generate-hashes` writes sha256 of every resolved wheel into requirements.txt. Plan 03's `uv pip install --system` verifies hashes at install time (uv's default behavior with hash-pinned lockfile). |
| T-23-05 | Tampering | uv binary supply chain | mitigate | Pinned `ghcr.io/astral-sh/uv:0.11.8` (not `:latest`). Phase 22 already used the same pinned tag — consistency is verifiable. |
| T-23-06 | Information Disclosure | requirements.txt comments | accept | Generated header includes container path `/work/requirements.txt` — no secrets. Acceptable. |
| T-23-07 | Denial of Service | uv resolver compile time | accept | Phase 22 root regen took ~560ms for 100 packages; template is similar size. No realistic DoS surface. |
| T-23-08 | Repudiation | which command/version produced the lockfile | mitigate | uv writes the exact command into the file's auto-generated header. Plus `git log` of the isolated commit is the audit trail. |
</threat_model>

<verification>
Plan 02 verification:
- `git diff --stat HEAD` shows exactly one file changed: `.claude/doml/templates/requirements.txt`
- The diff is large (1000+ lines) but format-only — no semantic content change beyond what Plan 01 introduced (drop pip-tools, add numpy<2.4 transitively resolves)
- Commit message: `chore(23): regenerate template lockfile via uv (isolated format diff)` (per commit_strategy frontmatter)
- Body of commit message should briefly reference PITFALLS #6 ("noisy format diff isolated per research").
- The next plan (Plan 03) consumes this lockfile via `COPY requirements.txt /tmp/requirements.txt` in the template Dockerfile — verify lockfile is on disk and committed before Plan 03 runs.
</verification>

<success_criteria>
- Lockfile header is uv-format (`uv pip compile`) not pip-compile-format
- Every package has `--hash=sha256:...` entries (>=1000 hashes total — Phase 22 root has 1644)
- Resolution targets Python 3.14 (matches Phase 22 / PROJECT.md D-03)
- numpy resolved at < 2.4 (PY-05 transitively honored via Plan 01's numpy<2.4 in requirements.in)
- pip-tools NOT in resolved tree (PY-04 transitively honored)
- kaggle, prophet, pmdarima, pyinstaller, skl2onnx, onnxruntime all present in resolved tree (no functional regression)
- Dry-run install inside scipy-notebook:2026-04-27 succeeds (Phase 22-equivalent install-target gate)
- Single-file commit (no other files modified — isolation discipline per PITFALLS #6)
- CONT-06 partial coverage advanced (template lockfile; root already complete from Phase 22)
- CONT-08 partial coverage advanced (template now mirrors root for both .in and .txt)
</success_criteria>

<output>
After completion, no SUMMARY needed for this single-task lockfile regen plan. The phase-level 23-SUMMARY.md (written in Plan 04) records this commit as part of the phase deliverables.

Commit message: `chore(23): regenerate template lockfile via uv (isolated format diff)`
Files in commit: `.claude/doml/templates/requirements.txt` only.
</output>
</output>
